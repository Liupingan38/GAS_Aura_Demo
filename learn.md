# TODO

1. C++快速开发，太空大战 （7天）✔
2. 刷题左程云（图、DP）早上
3. 大论文（一个月）
4. VR开发
5. UE框架、网络同步、GAS、对象池整理
6. 其余问题整理
7. 项目整理复盘

# UE

## 1、Gameplay框架

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251102142138112.png" style="zoom: 50%;" />

1. 目的：解决什么逻辑应该写在哪个类里的问题

2. 总结：

	1. UObject提供了反射、垃圾回收、序列化与反序列化等功能，是所有蓝图可见类型的根；
	2. AActor通过UActorComponent和USceneComponent获得逻辑与空间的能力；
	3. AInfo提供针对当前关卡的管理与数据支持；
	4. UGameInstance管理跨越关卡的全局数据；
	5. UWorld承载并调度ULevel，而ULevel则持有关卡中所有Actor的引用；
	6. 表示玩家角色的APawn或ACharacter则由APlayerController控制，AI角色则由AIController控制；
	7. 游戏规则由AGameModeBase执行，公共状态通过AGameStateBase在多端同步；
	8. UI由UUserWidget构建，AHUD则用来管理UI的显示；

3. **UObject：**它是所有蓝图可见类型的根，通常派生自UObject的类会以U作为开头。它提供了反射、垃圾回收、序列化与反序列化等一系列基础功能。

	- 反射：UHT工具在编译前进行扫描，生成描述类的元数据和用来访问类成员的函数代码。运行时就可以通过这些静态反射的代码，来按名字访问特定类型的成员。<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251102142909483.png" style="zoom:50%;" />

		- 垃圾回收（GC）：自动回收那些占用内存但不会被使用的对象，释放内存空间（如被调用Destory函数的Actor、从视口移除后的UMG控件、通过NewObject构造的临时对象）


	  - 序列化与反序列化：序列化是指把内存中的对象保存到可持久存储文件的硬盘中，而反序列化是指把内存中的对象重新还原到内存里。

4. **AActor：**它是派生自UObject的第二大基类，它通常表示的是游戏里看得见、听的到、摸得着的独立物体，通常派生自AActor的类型都会以A开头，它是几乎所有可放入关卡（World）中对象的基类。还有一类存在在世界中，但是没有物理碰撞、无须渲染的如AActor还派生了AInfo，用来表示存在在世界中，但是没有物理交互、无须渲染显示的Actor，充当特定关卡的管理者或数据源的类型，如AInfo又派生了AGameModeBase、AGameStateBase、APlayerState、AWorldSettings。

	1. 提供的核心功能是**挂载组件**的能力，通过挂载不同的组件UActorComponent来获得不同的功能（如Capsule和别的物体碰撞的能力、Mesh播放模型动画的能力、CMC移动能力）。ActorComponent列表中的组件大致可以分为两类：
		- 一类是继承自USceneComponent的组件，带有Transfrom变换信息，可以在关卡中进行拖拽，通常Actor的RootComponent都默认是USceneComponent类型，来支持Actor在关卡内的摆放，这一类位于component列表的上方，并且可以通过拖拽来指定Attach关系；
		- 一类是不带位置信息的，仅负责处理逻辑，如CMC，这一类位于component列表的下方，不具有层级关系。

	  2. 虚幻继承层级中第一个支持**网络复制**的类，UObject对象必须是作为子对象被Actor持有时才能被复制，并且还要开启复制选项Component Replicates才能被复制。


  	> UObject
  	>  └── UActorComponent                ← 所有组件的基类
  	>       ├── USceneComponent           ← 可拥有“空间位置”的组件
  	>       │    ├── UPrimitiveComponent  ← 可被渲染/碰撞的基础组件
  	>       │    │    ├── UMeshComponent
  	>       │    │    │    ├── UStaticMeshComponent
  	>       │    │    │    └── USkeletalMeshComponent
  	>       │    │    ├── UShapeComponent (如 UBoxComponent / USphereComponent)
  	>       │    │    └── UDecalComponent / UParticleSystemComponent / UNiagaraComponent
  	>       │    ├── USpringArmComponent
  	>       │    ├── UCameraComponent
  	>       │    ├── UArrowComponent
  	>       │    ├── USplineComponent
  	>       │    └── UWidgetComponent
  	>       ├── UMovementComponent        ← 控制 Actor 运动逻辑的组件
  	>       │    ├── UProjectileMovementComponent
  	>       │    └── UCharacterMovementComponent
  	>       ├── UAudioComponent           ← 播放声音
  	>       └── ULightComponent           ← 光源组件
  	
  	> UObject
  	>      └── AActor
  	>          ├── APawn
  	>          │    └── ACharacter
  	>          ├── AInfo
  	>          │    ├── AGameModeBase
  	>          │    ├── AGameStateBase
  	>          │    ├── APlayerState
  	>          │    └── AWorldSettings
  	> ​		   ├── AController
  	> ​        │    ├── AAIController
  	> ​        │    └── APlayerController
  	> ​        └── …（大量派生类）

5. **UGameInstance：**表示运行的游戏实例，是一个全局单例，引擎初始化时构造，引擎关闭时销毁。可以派生一个自己的UGameInstance在项目设置里指定，常常用它来管理跨越不同关卡的全局数据，如游戏的存档信息、游戏的整体通关进度、一些游戏性相关的设置（难度、玩家数量等）。多人游戏中GamInstance在每台机器上都存在，但是不参与网络复制。

6. **ULevel：**表示玩家实际体验游戏内容的场景，所有的Actor都被放置在Level中，持有关卡蓝图对象ALevelScriptActor和世界设置对象AWorldSettings：
	- **关卡蓝图**：可以方便的引用摆放在关卡中的所有对象
	- **世界设置**：提供了一个只对当前关卡生效的配置数据，可以方便的在其他类型中访问


7. **UWord：**负责协调不同ULevel之间的切换、加载和卸载，每个 UWorld 必须有 一个 持久性关卡Persistent Level作为基础关卡，这个 Persistent Level 可以加载多个流式加载的子关卡（Streaming Levels）作为补充场景。通常游戏运行时只存在一个world，而使用编辑器进行调试时，有两个world，一个是Editor World，就是我们摆放Actor的World，另一个是PIE World，点击播放后进入这个world。

8. **APlayerController：**是玩家的代理，控制Pawn。并持有增强输入子系统Enhanced Input Subsystem，负责接收玩家的键盘、鼠标、手柄等设备的输入，并通过输入映射上下文IMC（Input Mapping Context）将这些输入转化为游戏角色的输入行为IA（Input Action）。

	- 玩家从步行角色切换到驾驶载具：切换控制器所控制的 Pawn（Possess 新角色） + 切换输入映射上下文（IMC） + 切换摄像机逻辑

	- 多人游戏中，客户端和服务器都有PlayerController。当Client Controller通过Session加入Server时，GameMode会在服务器中为玩家创建按一个Server Controller。


9. **AAIController：**非玩家控制的角色控制器，挂载了AI所必须的组件。多人游戏下，只有服务器有AIController，Server跑AI决策，Client只用同步Pawn结果就行
- UBehaviorTree：决定AI如何执行决策的逻辑的
	
- UBlackboardComponent：存放AI做决策所需的“状态/信息”
	- UAIPerceptionComponent：用于AI感知环境（敌人、声音等）
- UNavigationSystemV1：内置了强大的自动寻路功能，通过 **NavMesh（导航网格）** 实现，使用时需保证场景中存在 **NavMeshBoundsVolume**，否则 AI 无法寻路。


10. **APlayerState：**派生自AInfo，每个玩家拥有唯一的PlayerState用来存储玩家的专属信息，被APlayerController持有，可以通过APlayerController来获得它的引用，我们常用它来存储玩家经验值、生命值等等这种只需要玩家自己掌握的信息。

	- 多人游戏中，在服务器和客户端都存在，当玩家掉线时，服务器会先存储玩家的PlayerState，当玩家重连时再恢复玩家的PlayerState

11. **APawn：**表示可以接收来自Controller的输入并作出反应的角色，是所有可以被控制的玩家和AI的基类。同时在服务器和客户端存在。

12. **ACharacter：**APawn的专业化子类，专门为Character Movement Component（CMC）设计，CMC提供了完整的角色移动解决方案，支持网络同步和预测。

13. **AGameStateBase：**派生自AInfo，用来记录所有玩家都应该知道的信息，如（游戏剩余时间、剩余人数、玩家得分等）。同时存在于客户端和服务器中，在多人游戏中会自动同步信息。

14. **AGameModeBase：**派生自AInfo，被设计用来定义特定关卡的游戏规则，管理游戏的整体流程。一个关卡可以指定一个要使用的GameMode，一个GameMode又可以指定要使用的PlayerController、PlayerState、HUD、DefaultPawn、GameStates等。多人环境下，GameMode只在服务器中存在，游戏规则逻辑只运行在服务器上，客户端被动的接收规则的执行结果。

15. **UUserWidget：**被用来创建游戏中的UI界面，是UMG系统的核心类型，可在蓝图中编辑、组合控件（可视化设计）
	- UObject
		└── UVisual
		      └── UWidget                     // 所有UI控件的基类
		           ├── UUserWidget            // 蓝图可编辑的UI界面（主入口）
		           ├── UButton
		           ├── UTextBlock
		           └── ...
	
16. **AHUD：**现在常用来管理UUserWidget的显示，决定什么时候显示哪些画面，虽然AHUD派生自AActor，但HUD不参与网络复制，只存在于客户端

## 2、Actor生命周期

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251104153124467.png" style="zoom:80%;" />

1. 放置在关卡中的Actor和Spawn动态生成的Actor的初始化流程差别：放置在关卡中的Actor相当于已经存在于.umap关卡文件中了，当关卡加载时，引擎会对.umap文件的数据进行反序列化，重新在关卡中创建Actor对象。

2. Actor生命周期：Construct → Initialize → BeginPlay → Tick → EndPlay （内存分配在Construct前）

	1. **Construct：**Actor 创建后立即执行，动态生成网格、子组件，并设置初始外观。C++中对应OnConstruct、蓝图中对应Construction Script。当编辑器修改属性时，每次属性变化都会重新触发 OnConstruction，根据属性更新视觉表现。

	2. **Initialize：**先进行组件的初始化，在同一帧里会先后调用PreInitializeComponents、InitializeComponents、PostInitializeComponents三个函数，其中PreInitializeComponents和PostInitializeComponents是可被子类覆盖的函数（虚函数），而InitializeComponents会遍历所有组件，先Activate激活再初始化。

	3. **BeginPlay：**所有组件初始化后，就会触发子类可覆盖的BeginPlay函数了（其中C++通常直接覆盖BeginPlay，而蓝图中则对应者ReceiveBeginPlay函数）。基类AActor中的BeginPlay会先执行一些必要的工作，包括注册组件、触发组件的BeginPlay，然后执行对应蓝图的ReceiveBeginPlay函数，所以当我们在C++中派生Actor的子类后，重写BeginPlay函数时，一般会在第一行写一个Super::BeginPlay()，然后在写一些自定义的逻辑，这就导致了C++派生的子类BeginPlay的自定义逻辑总是在其对应蓝图子类的BeginPlay之后执行。

	4. **下一帧进行Tick：**C++中覆盖Tick函数，而蓝图中则是覆盖ReceiveTick函数。和BeginPlay一样，其C++派生的子类的Tick中的自定义逻辑是在对应蓝图子类的Tick函数之后执行。

	5. **EndPlay：**当我们主动调用DestroyActor、编辑器停止Play、关闭游戏、关闭编辑器、切换地图时，都会触发Actor的EndPlay函数。当EndPlay触发后首先会通过UninitializeComponent函数对所有组件进行反初始化，随后Actor就会把自己和所有的组件都标记为垃圾（PendingKill），并把自己从ULevel的Actor列表中移除，最后在下一个垃圾回收循环中进行内存回收。

		![](https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251104184923411.png)

3. Construct → Initialize → BeginPlay 这三个阶段通常在同一帧之内完成。

4. 使用**SpawnActorDeferred**来构造Actor对象时，会先Construct Actor和其Component对象，直到使用对象调用FinishSpawning才进行Initialize和BeginPlay阶段。常用于初始化前，设置成员变量的值，如在网络游戏中，某些属性需要从服务器同步后再Spawn完成。

## 3、主要类型初始化顺序

1. 游戏引擎初始化阶段：由UGameEngine的Init函数触发；首先构造代表游戏实例的UGameInstance对象，UGameInstance在初始化的时候又会构造协调关卡的UWorld对象，而UWorld初始化时又会构造表示关卡本身的ULevel对象和表示这个关卡全局设置的AWorldSettings对象，然后在LocalPlayers列表中添加一个本地玩家。
2. 游戏引擎启动阶段：由UGameEngine的Start函数触发，并启动对游戏默认地图的加载逻辑；首先会构造掌管游戏规则的AGmaeModeBase对象，GameMode在初始化的过程中又会构造用来存储要在多个玩家之间同步的数据的AGameStateBase对象；游戏规则对象构建完成之后，就会构造接收玩家操作输入的APlayerController对象，

## 4、拆解示例

## 5、反射

我通俗地解释下, 程序代码的作用是执行特定的功能, 执行时的代码只知道"怎么做", 它不会天然知道"是什么". 就像你可以在源码里写让小明去炒个菜, 执行时小明就去炒菜了, 但他不知道自己叫"小明", 也不知道自己做的事叫"炒菜". 期间如果你想输入一段信息让他去"拖地", 他是不能理解什么是"拖地"的. 而反射就是把一些源码中包含的信息带入到运行时, 用一定手段记录下小明的类, 他有哪些变量, 会哪些函数等, 就可以让他在运行时根据变量名/函数名去访问/修改变量或执行函数了. 此外整个虚幻编辑器其实也是一段运行中的C++代码, 而蓝图想要访问C++代码里的变量/函数, 就需要用到反射的支持.

##  6、垃圾回收

## 7、序列化与反序列化

## 8、多人游戏

1. 客户端服务器模型（Client-Server）：UE中采用的方案，相比于点对点（peer to peer），服务器拥有权威Authority，且不会网络爆炸

	1. 监听服务器（Listen Server）：用一台玩家操控的设备来充当监听服务器，优点是便宜、简单，缺点是主机具有优势，适用于竞技性不高、人数较少的游戏
	2. 专用服务器（Dedicated Server）：用一台或多台专用的服务器设备，没有玩家进行操控，也不用进行游戏的渲染，适用于高竞技性、人数较多的游戏，是目前比较主流的方案。

2. 三种模式网络模式（NetMode）：无需打包就能进行跨设备联网测试

	1. Play Standalone：本地单人运行
	2. Play As Listen Server：有一个玩家会作为主机，其余的玩家作为客户端加入
	3. Play As Client：相当于在后台运行一个专用服务器，所有的玩家都作为客户端加入

3. 局域网LAN（Local Area Network）游戏：两台电脑需要在同一局域网下，一台主机Host开房，另一台通过主机的IP地址加入。

	1. 主机使用UGameplayStatics::OpenLevelBySoftObjectPtr()，其中Options填“listen”以作为监听服务器
	2. 客户端使用UGameplayStatics::OpenLevel()，其中LevelName把ip地址填进去
	3. 打包时，要设置游戏默认地图，并在MapLists中添加想要的地图

4. 使用Steam Online SubSystem（在线子系统）插件，主机创建Steam会话，并让会话托管监听服务器。

5. Actor的网络复制：

	1. bNetLoadOnClient：**关卡加载时**是否在客户端生成。若为false，则服务器存在Actor，而客户端不存在，导致客户端的角色会碰到空气墙，向Actor移动时会不停地被服务器拉回正确的位置，产生橡胶带现象。

	2. bReplicates：决定Actor是否从服务器复制到客户端。若Actor会移动且bReplicateMovement为false，也会产生橡胶带现象。

	3. bReplicateMovement：决定Actor的移动是否从服务器复制到客户端。私有成员变量，构造时无法初始化；SetReplicateMovement也不行，因为是虚函数，无法在构造函数中使用；可以使用SetReplicatingMovement函数。

	4. | 场景                                   | bNetLoadOnClient | bReplicates | bReplicateMovement | 说明                                 |
		| :------------------------------------- | ---------------- | ----------- | ------------------ | ------------------------------------ |
		| 世界中的静态装饰物（树、石头）         | ✅                | ❌           | ❌                  | 仅在地图加载时同步                   |
		| 服务端管理类（GameMode、AIController） | ❌                | ❌           | ❌                  | 仅服务器存在                         |
		| 玩家角色 Pawn、掉落物                  | ❌                | ✅           | ✅                  | 服务器生成 → 自动同步位移            |
		| 高速投射物Projectile（子弹）           | ❌                | ✅           | ❌                  | 服务器做命中判定，客户端只做视觉预测 |

	5. 对于高速投射物，不需要复制 Movement，因为

		1. 高速物体若复制Movement，复制频率会很高，会占用大量网络带宽，造成卡顿。
		2. 命中判定只需要在服务器进行，客户端无论是否复制movement都只是起到视觉效果的作用，所有关了也没影响，还会提高性能。

6. Authority和NetRole

	1. Authority：代表着谁拥有这个 Actor 的最终控制权，一般都在服务器上，单人游戏和监听服务器主机上在客户端。
	2. NetRole：每个Actor都有LocalRole和RemoteRole，分别代表该Actor在本地和远程机器上代表的NetRole。
		1. Authority权威
		2. Autonomous Proxy自主代理：客户端上由本地玩家控制的对象，如Pawn/Character
		3. Simulated Proxy模拟代理：除Autonomous Proxy自主代理外所有勾选复制Replicate的Actor（复制对象）
		4. None

7. Actor的Attachment可以复制，但要Actor开启复制；Actor Component的Attachment也可以复制，但要Actor Component开启复制。

8. 变量复制：

	1. 重写GetLifetimeReplicatedProps函数，使用DOREPLIFETIME宏来注册变量。
	2. 变量的UPROPERTY宏中标记Replicated。

9. RepNotify：若要在复制变量在同步到客户端时触发RepNotify回调函数，复制变量的UPROPERTY宏中可以使用ReplicatedUsing=RepNotify回调函数名，然后回调函数也要添加UPROPERTY宏，以便反射系统能将它和对应的变量进行绑定。

	1. C++中RepNotify只在客户端触发，蓝图中RepNotify在服务器和客户端都触发。
	2. RepNotify回调函数可以传入复制变量同类型的参数，用来接收复制变量的前值。

10. 复制条件ReplicationCondition：使用DOREPLIFETIME_CONDITION宏来注册变量，控制服务器仅将RepNotify发送给符合条件的客户端，减少网络带宽，并支持自定义条件。

	- 自定义条件：（注意：用来控制复制的复制条件变量只能在Server进行修改，从false变为true时会立即进行复制）
		1. COND传入COND_CUSTOM
		2. 设置一个bool变量作为复制条件变量
		3. 重写PreReplication函数，使用DOREPLIFETIME_ACTIVE_OVERRIDE宏来绑定复制变量和复制条件变量

## 9、网络同步和预测

GAS（Gameplay Ability System）的网络同步，本质上是把**“谁有权决定”**和**“哪些状态要复制”**拆开处理：

- **权威（Authority）**：最终结算（伤害/属性/标签/冷却等）通常以**服务器**为准。
- **预测（Prediction）**：为了手感，客户端可以先“演一下”，服务器回来再**确认/回滚**。
- **复制（Replication）**：把需要让别人看到的状态（属性、Tag、Cue、激活中的技能、AnimMontage 等）同步出去。

下面按你做 UE5 游戏客户端项目最常用的视角讲清楚：**ASC复制、能力激活、GameplayEffect、GameplayCue、动画与输入**。

------

## 1）ASC（AbilitySystemComponent）怎么被同步到网络

### 1.1 ASC 挂在谁身上（OwnerActor / AvatarActor）

GAS区分两层：

1. **OwnerActor**：拥有 ASC 的“所有者”（常见是 PlayerState 或 Character）
2. **AvatarActor**：实际在场景里执行技能/播放动画的“化身”（常见是 Character）

典型做法：

1. **PVP/多人+重生**：ASC 放 **PlayerState**（不随 Pawn 死亡销毁），Avatar 是 Character
2. **简单单局**：ASC 放 Character 也行（但重生/换Pawn会麻烦）

关键点：

- 服务器和客户端都要在合适时机 `InitAbilityActorInfo(Owner, Avatar)`，否则复制来的能力/效果可能“有但用不了”。

### 1.2 ASC 的复制模式（Replication Mode）

ASC 有复制策略（常见三种思路）：

1. **Full**：复制得最全（对所有人都同步较多数据）
2. **Mixed**：对自己同步得多，对别人同步得少（多人游戏最常用）
3. **Minimal**：最省（适合 AI / 小怪，或者你只关心 Cue/Tag 的场景）

经验：

- 玩家角色通常用 **Mixed**：自己需要完整信息（预测、GE堆叠、冷却等），别人只要看得见的结果（Cue/Tag/关键状态）。

------

## 2）能力激活（Ability Activate）在网络上怎么跑

### 2.1 “本地预测 + 服务器确认”的主流程

以**本地玩家按键放技能**为例（带预测的那种）：

1. **客户端本地**：尝试激活 `TryActivateAbility`
2. GAS 生成一个 **Prediction Key**（预测钥匙）
3. 客户端先执行一部分：比如播放前摇动画、生成本地瞄准指示、提前扣UI冷却（可选）
4. **RPC 发到服务器**：带上 PredictionKey 和必要参数
5. **服务器验证**：是否满足 Cost/Cooldown/Tag/距离/弹药等
6. **服务器权威执行**：真正应用 GameplayEffect（伤害、属性变化、Tag等）
7. 服务器把结果复制/广播：客户端收到后**确认预测**；如果服务器判失败则**回滚/取消**

你要记住的就是一句话：
**客户端可以先“演”，但服务器负责“判”和“结算”。**

### 2.2 预测失败会怎样

常见导致失败的原因：

- 客户端认为能放，但服务器判定 **Tag 不满足**、**资源不够**、**CD未好**
- 客户端的目标选择/位置和服务器不一致（例如命中校验）

失败时常见表现：

- 动画播了一点又被打断
- UI 冷却/资源被“纠正回来”
	所以你做技能时要把“预测段”和“权威段”分清：
- 预测段做“可回滚”的表现
- 权威段做“不可争议”的数值结算

------

## 3）GameplayEffect（GE）/ Attribute（属性）怎么同步

### 3.1 属性 AttributeSet 的复制

属性一般在 AttributeSet 里，用 **Replicated + OnRep** 同步到客户端。
流程：

- 服务器应用 GE 改变属性（Health/Mana/Armor…）
- 属性值通过复制到客户端
- 客户端 OnRep 更新 UI / 血条

重点：

- **真正的伤害结算在服务器**（尤其多人）
- 客户端看到的血量变化来自复制（或预测+纠正）

### 3.2 GE 的堆叠/持续/周期

持续类 GE（Buff/Debuff）需要考虑：

- 需要不需要别人知道“我身上有什么Buff”？
	- 只要表现：用 Cue 或 Tag 就够
	- 需要UI详情：对拥有者复制更多信息（Mixed 很合适）

##  10、行为树（Behavior Tree）

1. 定义：行为树是一种**树状决策结构**，用于让 AI 角色根据环境和状态自动选择并执行行为。一般通过黑板来存储状态和信息。
2. 主要节点类型：
	1. Composite（复合节点）：负责控制流程，如Selector / Sequence，决定执行哪个节点
	2. Decorator（装饰节点）：条件判断，如 “能否看到敌人”，决定是否允许执行下层节点
	3. Task（任务节点）：执行动作，如 “移动到目标”，实际执行某个行为（蓝图或C++）
	4. Service（服务节点）：定期运行、更新信息，如“每0.5s更新敌人位置”

## 11、状态树（State Tree）

12、状态树（State Tree）

## 13、UMG系统

1. UE 的 UI 系统主要由两层构成：
	1. **Slate 层（底层 C++ UI 框架）**：提供基础的渲染与事件系统；
	2. **UMG 层（蓝图 UI 系统）：**基于 Slate 封装，支持蓝图和可视化设计。

## 14、蓝图

# 项目

## 1、自我介绍

面试官您好！我是刘平安，目前就读于中国海洋大学计算机技术专业硕士，研究方向聚焦图形学，预计2026年6月毕业，渴望应聘游戏客户端开发岗位，可立刻到岗并实习3个月以上。本科阶段我就读于浙江理工大学建筑学专业，不仅以专业前20%的绩点斩获优秀学生二等奖学金，还凭借三维建模与空间审美积累，在多项省级以上建筑竞赛中获奖；硕士期间绩点稳居专业前5%，荣获校级学习优秀奖学金，科研上合作发表1篇中科院二区图形学论文，另有1篇一作论文在投。我始终热爱游戏开发，深耕UE5引擎、C++编程与图形学技术，希望能将专业所长与实践积累应用到岗位中，为团队贡献力量。

## 2、项目介绍

1. UE5俯视角RPG游戏（2025年4月到8月）

我用UE5做了个地牢风的RPG游戏原型，核心是靠GAS系统搭技能、属性和战斗这一套，还支持多人联机。
技术上主要用了UE5的C++、蓝图，还有行为树、MVC/MVVM这些架构。代码和演示视频都放GitHub和B站了，随时能看。
具体做的事挺多的：比如用增强输入实现鼠标点哪里角色走哪里、键盘放技能，动画上做了走路、受击、死亡溶解这些效果；多人同步这块，靠GAS和UE的复制机制，保证大家玩的时候技能、属性都是一致的；数值系统能支持不同职业成长，伤害计算也能算护甲、暴击这些；UI方面，不管是属性面板、技能栏还是存档功能，都做得比较灵活，技能还能拖拽装配；AI敌人会寻路、近战远程攻击，甚至能召唤小怪，关卡里敌人生成和掉装备的规则也能配置，性能也优化过。

2. 简易光栅渲染器（2025年3月到4月）

这个是我自己做的小项目，没依赖任何现成的渲染API，就是想搞懂3D图形怎么渲染出来的。
最后实现了不少基础功能：比如把3D模型通过矩阵变换转成屏幕上的图像，用算法画直线、填三角形，还加了深度测试避免物体互相穿透；画质上做了抗锯齿，光照用了Blinn-Phong模型，能模拟环境光、漫反射和高光，贴上图之后细节更清楚；还试着做了光线追踪，用BVH加速计算，甚至加了全局光照的模拟，最后用多线程把渲染速度提上去了。

## 3、你在UE5 RPG项目里用GAS做了技能和属性同步，多人联机时没遇到过延迟或者数据不一致的情况吗？怎么解决的？

回答：肯定遇到过，比如之前测试时，玩家放技能客户端显示放出去了，但服务端没判定，导致伤害没出来。后来我调整了GAS里AbilitySystemComponent的复制模式，不是全量复制而是选了“混合模式”，只传关键数据；另外给RPC调用加了优先级，技能释放这种关键操作优先传，还加了客户端预测——先在本地模拟技能效果，等服务端确认后再修正，这样玩家感觉不到延迟。最后还专门做了边界测试，比如两个玩家同时打一个怪，确保伤害计算不会叠加错，现在同步基本没出过问题。

## 4、简易光栅渲染器里你用了BVH加速光线求交，能说说为啥选BVH而不是其他加速结构吗？实际用下来效果明显不？

回答：选BVH主要是因为它对我这个小项目来说，实现起来不复杂还好用。之前也想过用网格划分，但我渲染的场景里物体大小差异大，网格划分容易有很多空格子浪费计算。BVH是把物体一层层包成 bounding box（包围盒），光线先跟大盒子碰，碰不到就不用查里面的小物体，省了很多步骤。实际测试下来，之前渲染一个有100多个三角面的场景要5秒，加了BVH后直接降到1.2秒左右，提速特别明显，尤其是物体越多，BVH的优势越明显。

## 5、你在RPG项目里用MVC和MVVM做UI，这俩架构具体怎么分工的？为啥不统一用一种，反而搞两种？

回答：主要是看UI功能需求来分的。像角色的HUD、属性面板这些，需要频繁跟数据联动，比如属性加点后要立刻更界面，用MVC就很方便——数据变了直接通过控制器通知视图更新，逻辑和界面拆得开，后面改属性计算规则时，不用动UI代码。而存档UI不一样，它要双向绑定，比如选个存档槽，地图信息、角色等级得立刻显示，改存档名称也得同步到数据里，MVVM的“视图模型”刚好能管这事，不用写一堆回调函数。要是统一用一种，比如存档UI用MVC，就得写很多判断来同步数据，反而麻烦，分开用更灵活。

## 6、你在渲染器里做了MSAA抗锯齿，那你知道MSAA和其他抗锯齿（比如FXAA）的区别不？当时为啥选MSAA？

回答：之前查资料时了解过，FXAA是在渲染完的图像上做后期处理，相当于“修图”，优点是快，但容易把细节磨糊；MSAA是在渲染过程中，对边缘的像素多采几次样，再平均计算颜色，细节保留得更好。我选MSAA是因为我的渲染器主要练手3D渲染流程，重点是把“光栅化到出图”的每一步搞懂，MSAA刚好能练到采样、卷积计算这些基础操作，而且我渲染的场景里有很多小物体的边缘（比如武器的棱角），用MSAA能让边缘不那么“锯齿状”，效果比FXAA更符合我这个项目的需求，虽然比FXAA稍微费点性能，但我的场景不大，完全能承受。

## 7、RPG项目里你实现了敌人的召唤技能，召唤出来的小怪怎么跟主怪配合？没出现过小怪乱跑、不跟着主怪打的情况吗？

回答：一开始还真出现过，小怪召唤出来后会自己跑去追远处的玩家，跟主怪的攻击节奏脱节。后来我在行为树里给小怪加了“跟随主怪”的优先级分支——主怪没攻击时，小怪就在主怪周围徘徊；主怪开始攻击，小怪才会跟着冲上去，还加了个“距离判定”，如果小怪离主怪太远，就强制拉回主怪附近。另外，主怪放召唤技能时，会通过GameplayTag给小怪发信号，小怪收到信号就知道“自己是哪个主怪召唤的”，不会跟其他主怪的小怪搞混。现在测试时，小怪能跟着主怪配合攻击，不会再乱跑了。

## 8、你在UE5 RPG项目里做了多职业属性成长，不同职业（比如近战和法师）的属性计算逻辑不一样，是怎么做到灵活适配的？后期要加新职业的话麻烦吗？

回答：我是在AttributeSet里留了“职业标识”的字段，然后用“配置表”存不同职业的成长系数——比如近战每级攻击力加5，法师只加3但法力值加8，不用改代码，改表格就行。另外伤害计算用了Execution Calculation，里面写了“职业专属加成”的分支，比如近战有“物理穿透”，法师有“法术暴击额外加成”，都是通过GameplayTag判断职业后触发的。后期加新职业的话，只要在配置表里填新职业的成长数据，给新职业加对应的Tag，再补一下专属加成的小逻辑，不用动现有代码，我之前试加个“刺客”职业，大概1小时就搞定了。

## 9、简易光栅渲染器里做了蒙特卡罗路径追踪模拟全局光照，这个功能实现时最头疼的是什么？最后怎么解决的？

回答：最头疼的是“噪点太多”，一开始渲染出来的画面全是小点点，尤其是暗部，根本看不清。查了资料才知道是采样次数不够，但加采样次数又会让渲染变慢。后来我搞了两个小优化：一是对暗部区域多采几次样，亮部少采，因为亮部噪点不明显，这样既减少了总采样数，又能压暗部噪点；二是加了“降噪滤镜”，用周围像素的颜色平均一下噪点区域。最后效果还不错，噪点基本看不到了，渲染时间只比之前多了0.3秒，能接受。

## 10、RPG项目里的技能栏支持拖拽装配，拖拽过程中没出现过技能拖错位置、或者拖完后技能状态不更新的情况吗？怎么处理的？

回答：一开始真有拖错的情况，比如把技能从槽位1拖到槽位2，结果两个槽位都显示同一个技能。后来我在拖拽逻辑里加了“临时锁定”——拖拽时原槽位的技能先标成“待移动”状态，不允许再被选，直到拖到新槽位确认后，再清空原槽位、更新新槽位；如果拖到槽位外取消拖拽，就把原槽位技能恢复回来。另外拖完后专门加了“状态同步检查”，比如技能拖过去后，立刻查技能的冷却时间、资源消耗，同步更到UI上，避免出现“槽位显示有技能，但实际不能用”的情况。现在拖拽基本没出过bug，很顺畅。

## 11、你在渲染器里用了Z-Buffer处理物体遮挡，那如果两个物体的Z值（深度）非常接近，会不会出现“闪烁”的情况？怎么避免的？

回答：会！之前渲染两个叠得特别近的方块时，画面会一直闪，一会儿这个方块在上面，一会儿那个在上面，这就是“Z-fighting”（深度冲突）。后来我加了两个小办法：一是给每个物体的Z值加了一点点“偏移”，比如后面的物体Z值稍微减一点，前面的稍微加一点，拉开差距；二是把Z-Buffer的精度调高一档，原来用的是16位，改成32位后，能区分的深度范围更大了。这两个办法一起用，之后不管物体多近，都没再出现过闪烁的情况，遮挡关系特别清楚。

## 12、RPG项目里实现了“击杀掉落战利品”，掉落概率是怎么设计的？比如想让稀有装备掉率低一点，普通装备高一点，是怎么控制的？后期要调整掉率的话方便吗？

回答：我是用“权重表”来控制掉率的，比如普通装备权重设100，稀有装备设10，传奇装备设1。每次怪物死亡后，系统会把所有可能掉落的物品权重加起来，生成一个随机数，看这个数落在哪个权重区间里，就掉对应的装备——比如总权重111，随机数1-100掉普通，101-110掉稀有，111掉传奇。掉率调整特别方便，不用改代码，直接在Excel里改权重值，导入项目就行。之前测试时觉得稀有装备掉太多，把权重从10改成5，立刻就少了，特别灵活。

## 13、主要用了三种，都是看项目里具体功能需求选的，没瞎用复杂的。

第一个是单例模式，比如管理全局技能配置、存档数据的那些类，我都做成了单例。因为这些数据全项目都要用到，要是每次用都new一个实例，很容易出现数据不一致——比如你在A地方改了技能冷却，B地方又读了旧数据。单例就保证全项目只有一个实例，不管在哪调，拿的都是同一个数据，省了不少同步的麻烦。

第二个是观察者模式，主要用在UI和数据联动上。比如角色属性变了（像升级加了攻击力），属性面板得立刻更过来吧？我就把属性数据做成“被观察者”，属性面板是“观察者”，面板一开始就盯着数据，数据一变就主动通知面板更新，不用我每次改完数据再手动调面板的刷新函数，代码少了很多重复的活儿。

第三个是工厂模式，用来生成敌人和技能。比如关卡里要生成不同类型的敌人（近战怪、远程怪、法师怪），要是每个怪都写个生成函数，后期加新怪就得改老代码，特别麻烦。我就做了个“敌人工厂”，把每种怪的生成逻辑封装好，要生成哪种怪，直接给工厂传个类型参数就行，加新怪也不用动工厂的核心代码，只需要加个新的生成逻辑，后期维护方便多了。



# 场景

## 1、多敌人同屏优化技术

1. 性能瓶颈来源：
	1. CPU：每个敌人不仅是静态模型的加载，还涉及到骨骼、动画、碰撞、AI等，每帧都要计算状态和行为。
	2. GPU：所有敌人动态阴影的渲染、不同敌人复杂材质贴图的加载可能会爆显存。

2. 静态资源的复用：
	1. 贴图的压缩与复用：把多张材质信息压入同一张图的不同通道，如ARM贴图中的RGB三通道。
	2. 骨骼的复用：不同敌人可以复用同一套骨骼，模型上可以更改不同的体型、贴图上可以通过更改颜色象征不同的属性。

3. 动态资源的复用：
	  - 对象池技术：一开始直接生成一定数量的对象放在对象池，当游戏中实际需要角色对象的时候，将对象池中的对象传送过去，当对象销毁时，再放回对象池，等待下次复用。避免了频繁的内存分配与释放。

4. 运行时分级优化：
	1. **LOD分级**（Level of Detail）：
	2. 距离远的敌人：低面数模型、低分辨率阴影、低帧率动画、低骨骼数量、MipMap等。像在极远敌人，如《刺客信条：大革命》将骨骼从300根降至11根，可能只保留静态Pose或几根关键骨骼。

  5. **AI分级**：
	1. 使用**中心AI**统一指挥整体移动路径，来减少重复决策和路径计算开销。
	2. 只有进入玩家攻击范围的敌人才激活复杂AI，比如真三国无双里的敌人，就十几个有复杂AI逻辑，其他的杂毛就原地缓慢移动。

6. 云游戏渲染卸载：移动端通过云端渲染降低本地性能压力，确保流畅体验。

## 2、对象池（Object Pool）

- 机制：设计模式上就叫做对象池模式，在初始化的时候就先分配一大片内存，在上面new一定数量的对象构成对象池，之后再需要对象时，就直接从对象池中拿取对象，不用的时候再放回来，最后统一的delele对象池。
- 目的：代替了频繁的new/delete操作，一方面在一开始就分配了一片连续的区域，避免了内存碎片；另一方面高效复用对象，避免了频繁的内存开辟与释放消耗大量性能。
- 典型用途：对于频繁new/delete的对象，如子弹、粒子、NPC等
- UE中使用：
	1. 创建了一个Actor作为对象池Actor的基类；又创建了一个ActorComponent作为对象池组件，来存放对象池Actor。
	2. 火球投射物继承对象池Actor的基类，并在角色身上挂载对象池组件，在 BeginPlay 中预生成一定数量的投射物并存入数组，并设置为未激活（关闭物理碰撞、关闭tick、不可见）
	3. 发射火球时从池中取一个未激活的实例进行复用，而不是 SpawnActor。
	4. 火球击中敌人后不销毁，而是调用 Deactivate 回到池子。
	5. 这种方式避免了频繁的 Spawn/Destroy 和 GC，大幅降低性能开销，是 UE 游戏中处理投射物、特效和可复用对象的常见优化手段。
- 链表实现的C++版本：

```cpp
#include<iostream>
#include<vector>

using namespace std;

template<typename T>
class ObjectPool {
private:
	struct Object {
		T obj;
		Object* next;
		Object(T o = T(), Object* n = nullptr) :obj(o), next(n) {}
	};
	Object* poolHead;
	vector<Object> pool;
public:
	ObjectPool(int size) {
		poolHead = new Object();
		pool.resize(size);
		for (int i = 0; i < size - 1; ++i) {
			pool[i].next = &pool[i + 1];
		}
		pool.back().next = nullptr;
		poolHead->next = &pool[0];
	}
	~ObjectPool() {
		//for (auto o : pool) delete o;由vector析构
		delete poolHead;
	}
	Object* allocate() {
		Object* cur = poolHead->next;
		if (!cur) throw "pool is empty!";
		poolHead->next = cur->next;
		cur->next = nullptr;
		return cur;
	}
	void deallocate(Object* cur) {
		cur->next = poolHead->next;
		poolHead->next = cur;
	}
};
struct Bullet {
	int x,y,vx,vy;
	Bullet(int x=0,int y=0,int vx=0,int vy=0)
		:x(x),y(y),vx(vx),vy(vy){}
};
int main() {
	ObjectPool<Bullet> BulletPool(50);
	for (int i = 0; i < 10000; i++) {
		auto* slot = BulletPool.allocate();
		cout << ++slot->obj.x << endl;
		BulletPool.deallocate(slot);
	}
}

```

## 跨地图自动寻路

1. 将不同的地图抽象成不同的顶点，传送阵抽象成边（如果两地图可以互相传送就是双向边，否则是单向边），边的权重可以自定义，将地图内的寻路时间、地图的加载时间都算上一个权值，然后对这张图求起点到终点的最短路径（如 Dijkstra、BFS）。
2. 最短路径算法的选择：
	1. BFS：如果只考虑地图的加载时间，我们就可以假设边权值都为1，使用BFS实现简单，也很快
	2. Dijkstra：如果权值不为1，可以使用Dijkstra，支持带权重的最短路径
3. 路径的回溯：
	1. 单路径恢复：使用一张表记录每个节点和对应的前驱，然后从终点从前往直接查表恢复
	2. 多路径恢复：也是使用一张表记录每个节点和对应的前驱，前驱可以使用一个hashset来存储，最后使用DFS来恢复所有路径
4. 地图内寻路：使用A*，将地图抽象成一张每个节点都与上下左右相连的图，对于普通的格子权值可以设为1，对于水坑可以设得更高，一些无法通过的障碍可以设为正无穷。
5. 地图预加载：如果一开始就计算好了从起始地图到终点地图的路径，那么刚进入一张地图后可以多线程的预加载下一张地图的资源。

## 3、设计弹刀

## 4、设计AI敌人被发现，就攻击

## 5、GAS优势、网络底层实现

# 图形学

## 1、讲一下渲染管线。

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251101192052505.png" alt="50" style="zoom: 33%;" /><img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251101192022998.png" style="zoom: 35%;" /><img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251101201331982.png" style="zoom: 23%;" />
<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/image.png" style="zoom: 33%;" />



1. 从原理上看,目前主流的有两种渲染管线,一种是**光栅化渲染管线**(Rasterization Pipeline)，一种是**光线追踪渲染管线**(RayTracing Pipeline)。第一种是光栅化渲染管线是遍历整个场景的三角形,然后投影到屏幕上,查询覆盖了哪些像素,然后对覆盖的像素进行着色；第二种光追渲染管线能则是从屏幕像素出发射出一条光线,通过加速结构进行遍历,然后查询光线命中的哪个三角形,拿到三角形的信息,对这个像素进行着色,它们最终的目的都是对像素进行着色,然后呈现游戏画面到屏幕上。
2. 从实现上来看，光栅化的渲染管线可以简单分为5步,它从应用程序输入到Vertex Shader,然后再到光栅化，再到Pixel Shader,最后到Frame Buffer进行输出。光追渲染管线,以UE5使用的DXR为例,它首先通过RayGenerationShader 执行的发射光线,经过加速结构（物体与三角形两个层级）的遍历,然后每次遍历都会进行intersection shader跟any-hit shader的计算,如果接受这次命中的话,这就进入到closeset-hit shader,那如果一直都没有命中的话,就进入到miss shader，其中any-hit shader主要用来处理透明物体和alpha计算和阴影的计算，而closeset-hit shader主要用来负责真正的着色计算。
3. 从工程实践上来看，当今市面上大多数游戏都是将这两个管线混合着用的,叫做hybrid rendering pipeline。其中场景几何、GBuffer、基础光照 由**光栅化**完成，反射、全局光照、阴影、AO 等由**光线追踪**计算。流程上，它会先进行geometry pass,然G-buffer和深度信息在物体表面去发射光线,去做ray tracing计算,然后得到反射、全局光照（GI）、阴影、AO等等信息,然后最后给lighting Pass去做光照计算。

## 2、前向渲染、延迟渲染、G-Buffer

1. 相同点：前向渲染和延迟渲染是传统光栅化渲染管线的两种实现方式。
2. 前向渲染每绘制一个物体时，都要立刻计算它受到的所有光源影响，当光源很多时，性能会暴跌
3. 延迟渲染将流程拆成两步：几何阶段（Geometry Pass）和光照阶段（Lighting Pass）。
	- Geometry Pass：把每个像素的重要信息（位置、法线、材质等）存进几个缓冲区中（G-Buffer）
	- Lighting Pass：对于每一个像素，都读取G-Buffer中对应像素的数据，最后无论多少光源，都统一进行光照计算。
4. 优缺点：
	- 延迟渲染：支持多光源、画面质量高；不支持透明物体和MSAA
	- 前向渲染：支持透明物体和MSAA、适用于显存较小的移动端设备

## 3、判断点在三角形内

1. 假设三角形顶点为ABC，点为P的话，可以判断点P是否在AB、BC、CA这三个向量的同一侧，如果是点P就在三角形内。
2. 判断点P在AB向量的哪一侧可以使用叉积来算，AB x AP >0则P在AB右侧。

```cpp
struct Point {
    double x, y;
    Point(double _x = 0, double _y = 0) : x(_x), y(_y) {}
};

double cross(Point a, Point b, Point c) {
    // 计算向量 AB × AC 的叉积
    return (b.x - a.x) * (c.y - a.y) - (b.y - a.y) * (c.x - a.x);
}

// 判断点 P 是否在三角形 ABC 内（含边界）
bool inTriangle(Point A, Point B, Point C, Point P) {
    double c1 = cross(A, B, P);
    double c2 = cross(B, C, P);
    double c3 = cross(C, A, P);

    // 全同号或为 0（点在边上）
    return (c1 >= 0 && c2 >= 0 && c3 >= 0) ||
           (c1 <= 0 && c2 <= 0 && c3 <= 0);
}
```

## 4、判断线段是否相交

1. 判断两条线段的包围盒是否相交，如果未相交，那线段也不可能相交。
2. 假设两条线段分别是AB和CD，判断C、D两点是否在线段AB两侧，再判断A、B两点是否在线段CD两侧，如果都满足，则它们相交。
3. 判断点在线段哪侧使用叉积。

## 5、判断射线与平面求交

1. 已知射线原点O，方向d，平面法线n，平面上点P
2. 假设交点为X，有X=O+td；
3. X也在平面上，有（X-P）·n=0；
4. （O+td-P）·n=0 解得 t=（（P-O）·n）/（d·n）
5. 当d·n=0时射线与平面平行，无解
6. t<0时，射线与平面无交点

## 6、MVP矩阵/坐标变换全流程

- 模型空间 → 世界空间 → 观察空间 → 裁剪空间 → 归一化设备空间（NDC）→ 屏幕空间。

1. Model矩阵，进行模型变换，将模型从局部坐标转换为全局坐标，应用模型的旋转、缩放与平移。
2. View矩阵，进行视图变换，连同所有模型一起，将相机移动到坐标原点，并朝向-Z轴，通过相机正向变换的矩阵求逆得到。
3. Perspective矩阵，进行透视变换，进行透视裁剪，然后将透视的裁剪的视锥体变换为一个长方体。
4. 进行正交变换，将之前的长方体归一化到一个【-1，1】的立方体，即NDC坐标，矩阵上的话先进行平移，在进行缩放
5. 最后进行视口变换，缩放平移到屏幕空间。

## 7、抗锯齿

1. 产生原因：采样频率过低，而在频域上产生混叠，画面上表现为锯齿。
2. 解决思想：
	1. 提高采样频率。
	2. 低通滤波，减少高频信息，减少混叠现象。
3. 常用抗锯齿：（效果）FXAA<TAA<MSAA<SSAA
	1. SSAA：先进行超采样，原来一个像素点分为4个进行渲染，然后再降采样。它的效果是最好的，但性能开销也是最大的，渲染工作量直接*4。
	2. MSAA：它和SSAA有点像，也是一个像素点分为4个或更多，但它不是渲染四遍，而是看边缘囊括了几个采样点，从而对该像素点的着色进行加权平均，缓解了尖锐的边缘锯齿。
	3. FXAA：它相当于图像的后处理，先检测出图像的边缘，在进行模糊，性能开销比较小。
	4. TAA：它是时域上进行超采样，结合上一帧的信息进行超采样，可以缓解闪烁问题。
	5. DLSS：利用深度学习的方法进行超采样。

## 9、光照模型

1. Phong模型：某点的着色=漫反射+高光+环境光。
	1. 漫反射与光照方向和法线夹角呈正相关，夹角越小，光强越大，与观察方向无关，与距离光源距离呈平方反比。k1*(I/r^2^) *(N·L)
	2. 高光与观察方向有关，只有当观察方向和反射方向比较相近的时候能观察到，反射方向比较难算，后面的Blinn-Phong模型将观察方向和反射方向的点积换成了法线与半程向量的点积，半程向量比较好算。k2*(I/r^2^) *(N·H)
	3. 环境光相当于间接光照，该模型把它直接简化成了常量。
	4. 缺点：不考虑能量守恒、不考虑全局光照、缺乏物理依据
2. PBR模型：**微表面理论** 和 **双向反射分布函数（BRDF）**
	1. 微表面理论：任何一个表面微观都是一个个镜面反射组成的，粗糙度决定了微表面的分布方式。
	2. BRDF：是光线从入射方向射到某点后反射出去，出射光与入射光的反射率比值，符合能量守恒定律。
	3. 实践上：为了更好的表现某个模型，我们会给它有多张贴图，有漫反射贴图、金属度贴图、粗糙度贴图、法线贴图，其中金属度贴图、粗糙度贴图存储的就是PBR计算所需的物理参数，都是0~1的，可以和AO贴图一起存储到一张彩色贴图的三个通道之中。

## 10、Z-Buffer/深度测试/模板测试/混合

- Z-Buffer定义：Z-Buffer是一个大小和Frame Buffer一样的二维内存缓冲区，储存片元的深度信息。
- **深度测试**：
	1. 一开始Z-Buffer里的值会全初始化为最大值。
	2. 遍历所有片元，如果深度大于Z-Buffer对应像素的值，则深度测试不通过，片元被丢弃；如果深度小于Z-Buffer对应像素的值，则深度测试通过，更新Z-Buffer中对应像素的值，并将该片元的颜色写入帧缓冲的对应位置。
- **深度写入**：渲染透明物体时，通常开启深度测试但关闭深度写入，深度测试通过时，不会更新 Z-Buffer，避免透明物体遮挡后续物体。
- **模板测试（Stencil Test）**：**模板缓冲**是一块与帧缓冲、深度缓冲分辨率相同的内存区域，每个像素存储一个整数，通常为1B（0~255），用于标记像素的 “渲染权限”，数字不同的模板可以执行不同的操作，比如描边效果，对敌人显示红色、友方显示绿色、中立显示白色，就可以通过设置不同的模板值，分别设置不同的描边颜色。
- **混合（Blending）**：透明物体需在深度测试后执行混合，将片元颜色与帧缓冲已有颜色叠加，实现**透明、半透明、颜色叠加**效果。
	1. 必须先绘制**不透明物体**（开启深度测试 + 深度写入）。
	2. 再按**从后到前**的顺序绘制**透明物体**（开启深度测试，关闭深度写入，避免透明物体遮挡后续物体

## 11、MipMap/各项异性过滤



## 12、HDR/环境光贴图

## 13、软阴影的实现、阴影映射、shadowmap是啥

## 14、LOD

## 15、剔除算法

## 16、AABB包围盒

## 17、后处理

## 18、光线追踪

## 19、BVH

# 计算机网络

## 1、TCP和UDP区别

1. 相同点：都是传输层的协议，为应用层提供服务。
2. 是否面向连接：
	- TCP：面向连接，需要三次握手、四次挥手，只能一对一通信
	- UDP：无连接的，不需要三次握手、四次挥手，支持一对一、一对多、多对一、多对多
3. 数据传输方式：
	- TCP：基于字节流的，当用户数据包超过MSS时，会被分为多个TCP报文段。默认MSS=1460B；
	- UDP：基于报文的，不会进行分段
4. 是否可靠传输：
	- TCP：可靠传输，使用流量控制和拥塞控制
	- UDP：不可靠，不使用流量控制和拥塞控制
5. 传输效率：
	- TCP：慢，有可靠传输、流量控制和拥塞控制等机制，首部20B
	- UDP：快，无可靠传输、流量控制和拥塞控制等机制，首部只有8B，数据密度高
6. 应用场景：
	- TCP：适用于需要可靠传输的场景
	- UDP：适用于对实时性要求比较高的场景

## 2、三次握手

- 三次握手的核心是**确认双方收发能力正常**，步骤如下：
	1. **客户端→服务端**：发送 SYN 报文，请求建立连接，并随机一个初始序列号。
	2. **服务端→客户端**：发送 SYN+ACK 报文，确认客户端的序列号，同时携带自己的初始序列号。
	3. **客户端→服务端**：发送 ACK 报文，确认服务端的 SYN 报文，连接正式建立。
- 为啥要三次：为了防止已失效的连接请求又传到了服务器，导致服务器一直处于连接已建立状态。

​	<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20250919205037991.png" style="zoom: 33%;" />

## 3、四次挥手

- 四次挥手的核心是**双方独立关闭各自的发送通道**，步骤如下：
	1. **客户端→服务端**：发送 FIN 报文，告知 “我的数据发送完了”。
	2. **服务端→客户端**：发送 ACK 报文，确认收到 FIN，此时服务器继续发送数据。
	3. **服务端→客户端**：当服务器数据发完后，发送 FIN 报文，告知 “我的数据也发送完了”。
	4. **客户端→服务端**：发送 ACK 报文，确认收到 FIN，等待 2MSL 后释放连接，被动方收到 ACK 后直接释放。

- **为啥要四次**：保证双方数据都传输完毕，避免数据丢失。
- **为啥要等2MSL**：假如最后一次确认报文丢失后，服务器会继续发FIN报文，但此时客户端已经关闭了，无法进行确认，导致服务器一直关闭不了。

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20250920091738516.png" style="zoom: 25%;" />
<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20250920092619048.png" style="zoom: 25%;" />

## 4、可靠传输

- 可靠传输要保证数据**无差错、按序到达、不丢失**
	1. 无差错：是通过校验和来实现的，传输方根据整个TCP报文段计算校验和，接收方判断校验和是否准确，如果错误就丢弃。
	2. 按序到达：是通过序列号来实现的，每个报文段带唯一序列号，接收方缓存乱序到达的报文段，按序列号重组后再递交给应用层。
	3. 不丢失：通过确认应答（ACK）和超时重传机制实现的，对于每一个接收到的报文段，接收方都会发送 ACK 确认，并且seq+1，未收到 ACK 则发送方计时器结束重传。
	4. **滑动窗口**：既实现流量控制（匹配收发双方处理能力），又通过窗口内报文批量发送 / 确认提升传输效率。
	5. **流量控制与拥塞控制**：前者避免接收方缓冲区溢出，后者避免网络拥塞导致的数据丢失，间接保障可靠性。

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20250919204153556.png" style="zoom:33%;" />	

## 5、流量控制

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20250919200636734.png" style="zoom: 50%;" />	

## 6、拥塞控制

1. **慢启动**：cwnd 初始为 1–2 个 MSS，每 RTT 翻倍，达到慢启动阈值（ssthresh）后进入拥塞避免。
2. **拥塞避免**：cwnd 每 RTT 线性 +1（而非翻倍），平稳接近带宽上限。
3. **快速重传**：收到 3 个重复 ACK 时，立即重传丢失报文段，无需等待超时。
4. **快速恢复**：触发快速重传后，ssthresh 设为 cwnd/2，cwnd 设为 ssthresh 并进入拥塞避免；超时丢包则重置 ssthresh = cwnd/2，cwnd = 1，重新慢启动。

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20250919202245245.png" style="zoom:33%;" />	

# 操作系统

## 1、寄存器、多级Cache、内存、磁盘

1. 速度：寄存器>多级Cache>内存>磁盘
2. 容量：寄存器<多级Cache<内存<磁盘
3. 利用寄存器：主要是由编译器来管理和优化
	1. 编译器会优先将一些局部变量放入寄存器，如循环计数器，避免反复从内存中读取。
	2. 栈帧的开辟与释放就是通过ebp和esp的移动实现的，相比于堆上内存的管理要快几个数量级。
4. 利用Cache：Cache 是 CPU 和内存之间的 “缓冲层”，利用 **时间局部性**（刚访问的数据大概率会被再次访问）和 **空间局部性**（访问某数据时，其相邻数据大概率会被访问），可以提升 Cache 的命中率。
	1. 多维数组遍历的时候优先遍历行，因为同行的元素都是顺序存储的
	2. 存储方式上可以优先考虑顺序存储，局部性好；相对来说链表是链式存储，节点散乱分布在内存中，Cache命中率低
	3. 减少跳转语句，也就是call指令，可以通过inline来使得调用方函数直接在调用点展开，也就不用跳转语句了，而且还能优化掉函数调用开销，但inline也只是建议，适合一些小而高频的函数，太大的函数编译器也不会进行内联。



## 3、进程与线程之间的切换

## 4、进程和线程的区别。

## 5、进程/线程间通信的方式。

## 6、那么每种方式有什么好处呢。

## 7、进程是资源分配的基本单位，这个资源指什么。

## 8、解释一下内部碎片和外部碎片？

## 9、虚拟内存的原理？

## 10、页表、页面置换算法

## 11、具体说一下时间片轮转算法。

## 12、多线程中，为什么要加锁？锁有哪些种类？

## 13、死锁、如何解决

# C++

## 1、进程的虚拟地址空间

#### 1）虚拟地址空间的定义

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251113130138579.png" style="zoom: 67%;" />	

1. 定义: 进程虚拟地址空间是Linux系统为当前进程分配的一块大小为2^32（即4GB）的虚拟内存空间（在x86 32位Linux环境下）
	- 用户空间（user space）: 占用3GB，用于存放用户态的代码和数据。低地址：0x00000000~0xC0000000
	- 内核空间（kernel space）: 占用1GB，用于存放内核态的代码和数据。高地址：0xC0000000~0xFFFFFFFF
2. 虚拟性: 它不是直接在物理内存上，而是一个由内核创建的数据结构。进程看到的是连续的虚拟地址空间，而实际物理内存可能是不连续的。
3. 主要作用：
	1. **内存扩展：**当物理内存（RAM）不够时，系统会把暂时不用的数据页放到磁盘上（即“换出”），需要时再换回（即“换入”）。这样可以使用较小的物理内存营造出很大的虚拟地址空间。
	2. **内存隔离：**每个进程都有自己独立的虚拟地址空间，防止程序相互篡改，提升系统的稳定性与安全性。
	3. **内存管理简化：**每个进程的虚拟地址空间都有相似空间划分，并且程序员不许用关心物理内存的实际布局，这些都自动又操作系统和硬件MMU完成。
	4. **支持进程内存共享和进程间通信：**不同进程可以通过映射相同的物理页实现共享内存（例如动态库共享）；同时不同进程他们的内核空间是共享的，所以可以通过内核空间来实现进程间的通信，这叫做匿名管道。

#### 2）虚拟地址空间的划分

- 用户空间的细分:
	- 0x00000000~0x08048000：不能访问
		- 0x00000000 ~ 0x00010000：空指针陷阱区，用于捕获空指针解引用。
		- 0x00010000 ~ 0x08048000：保留区域
	- .text段（代码段）: 存放程序的指令代码，只能读
	- .rodata段（只读数据段）: 存放只读数据，如常量字符串。const char* p="hello"; "hello"就存放在其中
	- .data段（数据段）: 存放已初始化的且值不为零的数据。
	- .bss段（也叫数据段）: 存放未初始化或值为零的数据，由操作系统在程序启动时置零。
	- 堆(heap): 存放通过new或malloc动态分配内存
	- 共享库映射区: 存放动态链接库（如.dll或.so文件）。向高地址增长
	- 栈(stack): 用于函数调用时的局部变量和函数参数，向低地址增长。
		- 每个进程都由独立的栈，用于存储局部变量和函数调用链，对其他进程不可见
		- 每个线程都有独立的栈，由线程库pthread在进程堆中动态分配，所有线程共享同一进程的地址空间，如代码段、数据段、堆等
	- 命令行参数和环境变量: 存放程序启动时的参数和环境变量。
		- 命令行参数：包含了用户输入的参数
		- 环境变量：包含了程序搜索头文件和库文件的默认路径
- 内核空间的细分
	- DMA区: 用于直接内存访问，大小约为16MB。
	- Normal区: 主要存放内核代码、数据以及进程控制块（PCB）等，大小约为800多MB。
	- High Memory区: 用于高端内存映射，处理高地址的物理内存。

#### 3）堆和栈的区别

1. 栈是连续的、结构简单。在栈上进行内存分配和释放，只需要改变ebp、esp这类寄存器的值，几乎不耗时。
2. 堆是离散的、结构复杂。堆由操作系统管理，它需要维护一张“空闲内存表”（或链表/红黑树）来跟踪哪些区域可用。在堆上分配内存时，首先要查找大小合适的空闲内存，内配内存后还有更新表，内存回收后可能还要进行内存碎片的合并；此外，在多线程环境下还要加锁。所以速度会比比栈慢几个数量级。

## 2、函数的调用栈过程

###### 1）问题一：main函数调用sum，sum执行完后，怎么知道回到哪个函数中？

- 栈帧建立过程:
	1. 保存调用方ebp（push ebp）: 进入sum函数后，首先将调用方函数的ebp值压入栈中，以保存调用方的栈帧信息。
	2. 更新ebp指针（mov ebp）: 然后将esp的值赋给ebp，使ebp指向当前函数的栈帧底部。
	3. 开辟栈空间（sub esp ，4Ch）: 为sum函数开辟栈空间，用于存储局部变量等。
	4. 初始化栈帧空间（rep stos for）：在WINDOWS下的VS系列编译器中，将栈帧空间内全初始化成0xCCCCCCCC
- 栈帧回退: 
	1. 函数调用结束后，通过mov esp, ebp指令将栈指针esp恢复到栈帧基指针ebp的位置，函数占用的栈空间被释放，交还给系统。
	2. pop ebp指令: 该指令将栈顶元素弹出，并将其值赋给ebp寄存器。在函数调用结束时，此操作使ebp指针重新指向调用方函数的栈底，从而恢复调用方的栈帧。
	3. RET指令: 执行RET指令时，将栈顶元素（即返回地址）弹出，把出栈内容放入CPU的PC寄存器中，此返回地址是调用函数时由call指令压入栈中的下一行指令的地址。

###### 2）问题二：sum函数执行完，回到main以后，怎么知道从哪一行指令继续运行？

- call指令功能: 
	1. 保存返回地址: 将call指令下一行指令的地址压入栈中，以便函数执行完毕后能够返回到正确的位置继续执行。
	2. 跳转至函数地址: 跳转到被调用函数（如sum函数）的起始地址，开始执行函数代码。


## 3、程序的编译链接原理

###### 1）例子

- ![img](https://thumbnail0.baidupcs.com/thumbnail/05103a99fg75df07bffcd4b820a7b592?chkbd=0&chkv=0&dp-callid=0&dp-logid=2365732750705543543&expires=8h&fid=1102791590972-250528-898370546587835&ft=image&quality=100&rt=pr&sign=FDTAER-DCb740ccc5511e5e8fedcff06b081203-LaBpfpK5SgTuzJghXfT4K1Z%2Fovs%3D&size=c1080_u1080&time=1762848000&vuk=1102791590972)
- 符号引用示例: 在main.cpp中，通过extern int gdata声明了全局变量gdata，并通过int sum(int, int)声明了函数sum。这两个声明均引用了外部文件sum.cpp中定义的内容。
- ![img](https://thumbnail0.baidupcs.com/thumbnail/fcf20b3d5g65c0fe4df4ec44e94bc705?chkbd=0&chkv=0&dp-callid=0&dp-logid=2365732881306049973&expires=8h&fid=1102791590972-250528-929653231384397&ft=image&quality=100&rt=pr&sign=FDTAER-DCb740ccc5511e5e8fedcff06b081203-1ONxXEBmw0niBLOmipiVWogJnCE%3D&size=c1080_u1080&time=1762848000&vuk=1102791590972)

###### 2）编译过程

1. 预编译: 处理以\#开头的指令，如#include、\#define等，但\#pragma指令除外，因其功能多样，部分功能需在链接阶段处理。

	1. \#include、#define都是直接进行文本替换
	2. \#include的循环依赖问题解决：
		1. 前向声明
		2. \#pragma once
	
2. 编译: 进行语法分析、语义分析、词法分析以及代码优化，生成特定平台的汇编代码。

3. 汇编: 将汇编代码转换为特定平台的机器码，生成二进制可重定位的目标文件（.o或.obj文件）。

###### 3）链接过程

- 包含所有的二进制可重定位目标文件和静态库文件
- 手动链接	![img](https://thumbnail0.baidupcs.com/thumbnail/f72042eeeo94a3df915e6521b7ff43ac?chkbd=0&chkv=0&dp-callid=0&dp-logid=2365732953860196830&expires=8h&fid=1102791590972-250528-282907393337644&ft=image&quality=100&rt=pr&sign=FDTAER-DCb740ccc5511e5e8fedcff06b081203-fsCI7XdkQ56ic2AnHKAQyIlknN0%3D&size=c1080_u1080&time=1762848000&vuk=1102791590972)	

1. 段的合并: 链接器合并所有目标文件的段（包括符号表段）

	1. 符号表段合并时，要进行符号解析：对所有的符号引用都要找到其定义的地方！（报错：符号未定义、符号重定义）

2. 符号重定位: 链接的核心步骤，符号解析完成后，为所有的符号分配虚拟地址，修改目标文件中需要修改的位置，如调用外部函数或引用全局变量的指令。

###### 4）可执行文件生成

- 文件格式: 在Windows下生成.exe文件，在Linux下默认生成a.out文件，可通过-o选项指定可执行文件的名称。
- 和.o文件一样也是一段段的

##### 

#### 1）符号表 （.symtab）

- objdump查看.o文件的符号表 objdump -t main.o
- 符号表作用: 存放程序中定义和引用的函数、全局变量的信息，用于链接时的符号解析。

![img](https://thumbnail0.baidupcs.com/thumbnail/9d15b3a4fhe231725083183371af46e3?chkbd=0&chkv=0&dp-callid=0&dp-logid=2365733228856504747&expires=8h&fid=1102791590972-250528-992769458836106&ft=image&quality=100&rt=pr&sign=FDTAER-DCb740ccc5511e5e8fedcff06b081203-QWCc%2B6UQU1DcixhOW2kvdy%2BRExk%3D&size=c1080_u1080&time=1762848000&vuk=1102791590972)

- 符号引用（UND）: 如果当前文件引用了外部文件的函数或全局变量，也会生成相应的符号，但这些符号是未定义的（UND），表示在当前文件中只是引用，未定义其具体内容。

- 局部符号与全局符号 

	- 局部符号（l）

		: 只能在当前文件中可见，通常用static关键字修饰。

		- 静态全局变量与静态函数 : 都只能在定义它的文件中可见，其他文件不可见

	- 全局符号（g）: 在其他文件中也可见，未用static修饰的符号默认为全局符号。

- 函数符号的生成规则 : 函数符号通常由函数名加上形参列表组成，如Z3sumii

- 注意

	：

	- 编译过程中符号不产生虚拟地址，链接时分配
	- 指令在编译阶段就产生好了，指令中的地址先默认填0，符号解析后分配虚拟地址（符号的重定向）；

- ![img](https://thumbnail0.baidupcs.com/thumbnail/1746b6993p85676a4eecbcaab7e86579?chkbd=0&chkv=0&dp-callid=0&dp-logid=2365733294120626714&expires=8h&fid=1102791590972-250528-43120057479455&ft=image&quality=100&rt=pr&sign=FDTAER-DCb740ccc5511e5e8fedcff06b081203-4e9MtiSCTQL1JHkKYMxQEh1u2UE%3D&size=c1395_u1125&time=1762848000&vuk=1102791590972)

#### 2）可重定位目标文件（.o文件） ﻿28:21﻿

- ELF可重定位目标文件的格式（Windows中为PE头）
- objdump -s main.o --->打印常见段
- readelf -S main.o  --->打印elf段表
- readelf -h main.o --->打印文件头信息
- ![img](https://thumbnail0.baidupcs.com/thumbnail/f426ec44fl3f45eed08f9d80c29de264?chkbd=0&chkv=0&dp-callid=0&dp-logid=2365733371710262147&expires=8h&fid=1102791590972-250528-1082971525451286&ft=image&quality=100&rt=pr&sign=FDTAER-DCb740ccc5511e5e8fedcff06b081203-bVynwTFbyidzrLnVizRGXOvbU0k%3D&size=c1080_u1080&time=1762848000&vuk=1102791590972)
- ![img](https://thumbnail0.baidupcs.com/thumbnail/34bfbbe38m5128926f92bf69fedc339a?chkbd=0&chkv=0&dp-callid=0&dp-logid=2365733023761326448&expires=8h&fid=1102791590972-250528-360788817438404&ft=image&quality=100&rt=pr&sign=FDTAER-DCb740ccc5511e5e8fedcff06b081203-6TgFb0WM0W9SAixayoFjn6S18e8%3D&size=c1080_u1080&time=1762848000&vuk=1102791590972)
- 典型的ELF可重定位目标文件包含的节
	- .text: 已编译程序的机器代码。
	- .rodata: 只读数据，如printf语句中的格式串和开关语句的跳转表。
	- .data: 已初始化的全局C变量。局部C变量在运行时保存在栈中，不出现在.data或.bss节中。
	- .bss: 未初始化的全局C变量。在目标文件中不占据实际空间，仅作为占位符。
	- .symtab: 符号表，存放程序中定义和引用的函数和全局变量的信息。每个可重定位目标文件都有一张符号表，但不包含局部变量的条目。
	- .rel.text: .text节中需要修改的位置的列表，用于链接时修改调用外部函数或引用全局变量的指令。
- 注意事项
	- 节的概念: 节是ELF文件中的基本组成单元，每个节都有特定的功能和内容。
	- 符号表的作用: 符号表（.symtab）中存储了程序中定义和引用的函数和全局变量的信息，对于调试和链接过程非常重要。
	- 重定位信息: .rel.text等重定位节包含了需要修改的位置信息，以便在链接时调整地址。可执行目标文件中不需要重定位信息。



#### 3）可执行文件（.exe文件）

![img](https://thumbnail0.baidupcs.com/thumbnail/b970a3f96i20da90e3d8c14f29447247?chkbd=0&chkv=0&dp-callid=0&dp-logid=2365733094614217443&expires=8h&fid=1102791590972-250528-58811493095970&ft=image&quality=100&rt=pr&sign=FDTAER-DCb740ccc5511e5e8fedcff06b081203-Mu13drhE76CWzO6HqN4MPnK3hts%3D&size=c1104_u660&time=1762848000&vuk=1102791590972)

- program headers: 可执行文件特有的段，称为program headers，它告诉系统在运行程序时应该加载哪些内容到内存中。program headers通常包含两个load段，分别对应代码段（.text）和数据段，这两个段在运行时会被加载到内存中。

##### 3. 可执行文件加载过程

1. 加载内容: 系统通过查看可执行文件的program headers，确定需要加载代码段和数据段到内存中。
2. 入口地址: 可执行文件的文件头（ELF header）中包含程序的入口地址，即程序开始执行的第一条指令的地址。
3. 执行过程: 加载完成后，系统将该入口地址写入CPU的PC寄存器，CPU从该地址开始执行程序的第一条指令。
4. 地址映射: 程序运行时，访问的指令或数据地址需经过地址映射，若该地址尚未映射，会产生页面异常，由异常处理程序分配物理内存并进行地址映射。

![img](https://thumbnail0.baidupcs.com/thumbnail/b384b5c31rfb4619607ea6fcb571ed8d?chkbd=0&chkv=0&dp-callid=0&dp-logid=2365733162528334189&expires=8h&fid=1102791590972-250528-860366367277376&ft=image&quality=100&rt=pr&sign=FDTAER-DCb740ccc5511e5e8fedcff06b081203-jp4ovfKKRa%2FLiN5VaqkvtkNbd%2F0%3D&size=c1080_u1080&time=1762848000&vuk=1102791590972)

######

静态链接库和动态链接库有什么区别？
·链接方式：静态链接库在编译链接时会被完整地复制到可执行文件中，成为可执行文件的一部分；而动态链接库在编译链接时只会在可执行文件中包含对库的引用，实际的库文件在运行时由操作系统动态加载。
·文件大小：静态链接库会使得可执行文件的大小增加，因为库的代码被完整地复制到可执行文件中；而动态链接库不会增加可执行文件的大小，因为库的代码在运行时才会被加载。
·内存占用：静态链接库在运行时会被完整地加载到内存中，占用固定的内存空间；而动态链接库在运行时才会被加载，可以在多个进程之间共享，减少内存占用。
·可扩展性：动态链接库的可扩展性更好，可以在不修改可执行文件的情况下替换或添加新的库文件，而静态链接库需要重新编译链接。

## 4、内联函数和普通函数的区别

1. 调用开销: 普通函数有标准的函数调用开销，包括参数压栈、函数栈帧的开辟和回退等步骤，这些步骤会带来额外的开销，而内联函数没有调用开销，其代码在调用点直接展开，适用于简单且频繁调用的函数。
2. 符号生成: 内联函数成功内联后，不会在符号表中生成相应的函数符号。可以通过objdump命令查看符号表，确认函数是否内联。
3. 编译器决定: inline只是建议编译器将函数处理成内联，最终是否内联由编译器决定
	- 递归函数: 递归函数由于调用次数在编译时无法确定，因此不会被处理成内联函数。
	- 代码量过多: 函数内部代码过多时，即使标记为inline，编译器也可能不会将其处理成内联函数，以避免代码膨胀和潜在错误。
	- Debug版本中inline不起作用，release版本下才进行优化

## 5、 函数重载

#### 1）函数重载的定义

- 基本概念：一组函数名相同但参数列表的个数或类型不同的函数，称为函数重载。是实现静态多态的一种方式。
- 调用机制：编译器根据调用函数时传入的实参类型自动选择匹配的重载版本。

#### 2）C++支持函数重载 ，而C语言不支持

1. 编译差异：C语言中仅由函数名决定符号，同名函数会产生链接错误，而C++中由函数名+参数列表类型组成符号（如compare_int_int），能正确区分不同参数类型的重载函数。

2. C++与C代码相互调用（C++与C生成符号的规则是不同的

	- extern "C"的作用：告诉C++编译器按C规则生成函数符号
		- C调用C++：将C++降级，需将C++源码用extern "C"包裹
		- C++调用C：将C升级，需将C函数声明用extern "C"包裹

	- \#ifdef __cplusplus宏的作用 
		- 工程意义：__cplusplus是C++编译器内置宏，如图，C编译器会忽略extern语法，C++编译器会处理，使头文件能同时被C和C++项目包含，保证符号生成规则一致

#### 3）函数重载需要注意些什么

- 返回值无关性: 函数重载与返回值类型无关，仅取决于函数名和形参列表的差异
- 同一作用域原则：
	- 一组函数要构成重载，必须首先处于同一个作用域中
	- 不同作用域的同名函数不会形成重载关系，因为局部作用域的声明会隐藏全局作用域的同名函数。（当局部作用域存在函数声明时，编译器会优先匹配局部作用域的函数声明，而不会去全局作用域寻找其他重载版本。）

## 6、 const和（一级、二级）指针

- const修饰的变量称为常量，与普通变量有三点区别：
	- 编译方式不同，有编译期替换机制：所有出现const常量名的地方都用初始值替换，所以即使内存值能被修改，打印时const常量都会被编译器替换为初始值
	- 不能作为左值（不能直接修改常量的值）
	- 不能把常量的地址泄露给普通指针或普通引用变量（不能间接修改常量的值）

- 规则:
	- const修饰的是离它最近的类型（const int *p 和 int const *p:等价）
	- 当const右边没有*时，const不参与类型判定

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251113132350363.png" style="zoom: 33%;" />

- const与二级指针结合：
	- const int **q：const修饰最终指向的整型变量
	- int *const *q：const修饰中间的一级指针
	- int **const q：const修饰二级指针变量本身
- const与一级指针与引用&的结合：
	- 写一句代码，在内存0x0018ff44处写一个4字节的10：int *&&p=（int *）0x0018ff44；或 int *const&p=（int *）0x0018ff44；
	- & 与 *的转化：int &b=a ;<=>int *b=&a;

## 7、引用

#### 1）引用和指针的区别

1. 引用是一种更安全的指针：引用必须要初始化，并且初始化值必须能取地址（左值），保证了指向有效内存，避免野指针问题；而指针可以不用初始化，需要开发者自行保证有效性

2. 汇编层面：

	1. 定义一个引用变量，和定义一个指针变量，都是存储内存地址，其汇编指令是一模一样的，
	2. 通过引用变量修改所引用内存的值，和通过指针解引用修改指针指向的内存的值，其底层指令也是一模一样的

3. 引用传递优势：相比于值传递，不需要构造临时对象，效率高；相比于指针传递，写法上更简洁、不容易出错。

	<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251113131155997.png" style="zoom: 67%;" />	

4. 指针可以有一级指针，也可以有多级指针；引用只有一级引用，没有多级引用，&&代表着右值引用。

#### 2）右值引用

- 左值特征：有内存空间，有变量名，值可修改（如变量a）
- 右值特征：无内存空间，无变量名，不能作为赋值左操作数，如：字面常量、临时对象

1. C++11新增特性（int &&c = 20），专门用来引用右值类型
2. 底层指令实现：(和 const int & c=20 相同)
	1. 与const引用相同：先自动生成临时量存储右值，再将临时量地址存入引用变量
	2. 与const引用区别：右值引用值可修改，const引用值不可修改
3. 重要特性：
	1. 右值引用变量本身是左值（有内存、有名字）；只能用左值引用来引用右值引用变量（如int &e = c）
	2. 不能用右值引用变量引用左值（如int &&f = a会报错）
	3. 右值引用变量生命周期应短于临时量
4. 核心要点：
	1. 设计目的：高效处理临时对象（移动语义基础）
	2. 典型应用场景：实现移动构造函数、完美转发



## 8、OOP（面向对象）

- OOP编程的核心是类的设计与使用
- OOP四大特征：抽象、封装/隐藏、继承、多态。
- OOP编程方法：通过分析问题场景中的实体，抽象其属性与行为，分别转化为类的成员变量与成员方法。其中，实体属性对应类的成员变量，实体行为对应成员方法。每个对象有独立成员变量，所有对象共享同一套成员方法。
- 类、实体与对象的关系：类代表实体的抽象数据类型（ADT），对象是类的实例化结果，占用内存空间。
- 内存对齐：对象大小仅与成员变量相关，与成员方法无关，按最长成员类型对齐计算。例：类对象大小为40字节（char[20]补4字节，double8字节，int补4字节）
- 命名规范：类名采用驼峰式（如CGoods），成员变量前缀m_ 或 _。

## 9、构造函数和析构函数

- 构造函数：对象创建时自动调用构造函数，无需手动管理初始化操作，函数名与类名相同，无返回值类型
	- 可重载：支持多个构造函数，通过参数列表差异区分（如带默认值的整型参数构造函数）。
- 析构函数：对象销毁时自动调用析构函数，无需手动管理释放操作，类名前加波浪号，无返回值类型
	- 唯一性：每个类仅能有一个析构函数，且不带参数。
	- 调用时机：对象离开作用域时自动调用，也可以通过手动调用提前终止对象生命周期（但可能导致内存非法访问）。
- 对象生命周期：同一作用域下，先构造的对象后析构，后构造的对象先析构，此过程类似入栈出栈操作。
	- 栈对象(.stack)：定义时构造，作用域结束时析构。
	- 全局对象(.data)：程序启动时构造，程序终止时析构。
	- 堆对象(.heap)：需手动通过delete释放，delete操作包含析构函数调用及内存释放两步。

## 10、成员初始化列表

1. 定义：初始化列表是在构造函数中直接初始化成员变量的一种方式，它比在函数体内赋值更高效，而且对于 const、引用、以及没有默认构造函数的成员对象是唯一合法的初始化方式，因为它们必须初始化。
2. 高效率：初始化列表可以直接完成成员变量初始化，而构造函数体内的操作需要先调用默认构造函数，再赋值，会多一次赋值操作
3. 书写顺序：成员变量的初始化和它们定义的顺序有关，和它们在构造函数成员初始化列表中的顺序无关。

## 11、深拷贝和浅拷贝

- 浅拷贝：拷贝对象中的值（包括指针地址），多个对象共享同一份资源。
	- 浅拷贝问题：
		- 未声明拷贝构造和拷贝赋值函数时，生成的默认拷贝构造和默认拷贝赋值函数都是浅拷贝。
		- 浅拷贝导致多个对象共享堆内存，析构时产生双重释放错误。
- 深拷贝：不仅拷贝对象的值，还重新分配内存并复制内容，多个对象资源各自独立。
	- 在一个类中使用了指针或动态资源，你应当同时重写以下三个函数：
		1. 拷贝构造函数： 为新对象分配独立的内存空间，并复制源对象的数据。
		2. 拷贝赋值函数：
			- 自赋值检查：if(this == &src) return *this，可避免野指针访问
			- 资源释放：清除当前对象持有的堆内存
			- 深拷贝执行：同拷贝构造函数逻辑
		3. 析构函数： 默认析构函数仅释放对象本身内存，实现深拷贝的析构函数需要使用delete显式的释放堆内存。
- 选择依据 ：在于对象是否占用外部资源，涉及到指针和动态内存的尽量使用深拷贝。
- 现代 C++ 推荐：使用智能指针，让类的构造/析构函数自动管理资源（RAII），从根本上杜绝浅拷贝、内存泄漏、重复释放等问题。
	- RAII：Resource Acquisition Is Initialization（资源获取即初始化） ：资源（如内存、文件、锁等）在对象创建时自动获取，在对象销毁时自动释放。

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251113132551316.png" style="zoom:50%;" />

## 12、this指针和三种成员方法

#### 1) this指针

- this指针机制：一个类可以有很多对象，这些对象都共享一套成员方法，如何区别是那个对象调用了成员方法是通过this指针来实现的。类的成员方法一经编译，编译器会自动为所有成员方法参数列表中隐式添加this指针，接收调用该成员方法的对象的地址。而当对象调用成员方法时，会自动将对象的地址作为实参传入。

#### 2) 三种成员方法

1) 普通成员方法

	- this指针：编译时，编译器会自动为普通成员方法添加this指针作为形参变量，用于接收调用对象的地址，同一类的不同对象共享同一套成员方法，通过this指针区分不同对象，所以调用普通成员方法必须通过对象调用。

	- 访问权限：普通成员方法可访问任意成员变量

2) 静态成员方法 （static）

	- this指针：编译时，普通方法编译时添加this形参，而静态方法不生成this指针，所以静态成员方法不依赖具体对象，一般通过类作用域调用。

	- 访问权限：静态方法只能访问静态成员变量和静态成员方法，无法访问普通成员变量，因为他没有this指针
		- 静态成员变量：需在类外定义并初始化，属于类级别而非对象级别，存储在数据段（.bss/.data）而非栈内存(.stack)/堆。

	- 作用：用于在不依赖具体对象的情况下，访问类级别的功能或数据。比如说统计这个类的对象有多少个。

3) 常成员方法 （const）

	- this指针：编译器会生成const修饰的this指针作为形参变量，所以常成员方法和普通成员方法构成重载关系，二者虽然函数名相同但形参类型不同（const T* 与 T* ）。所以当我们想要一个不能修改成员变量的常对象时，只能调用常成员方法，不能调用普通成员方法，因常对象传入的实参地址为const类型，无法匹配普通方法的非const形参。﻿

	- 访问权限：可访问私有成员，但权限仅限于读取。

	- 作用：对于仅执行读操作的成员方法，应统一实现为常成员方法，确保普通对象和常对象均可调用，并保证对象的成员变量不会被修改。

#### 3) 指向成员的指针

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251113132704509.png" style="zoom:40%;" /><img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251113132750835.png" style="zoom: 67%;" />

## 13、运算符重载

##### 1) 定义

- C++运算符重载的意义在于使对象的运算表现与编译器内置类型一致。运算符重载的本质是调用对象的成员方法，具体表现为在运算符前添加operator关键字。

##### 2) **全局**运算符重载函数

- 优先匹配**成员**运算符重载函数，再匹配**全局**运算符重载函数。（比如>>运算符）
	- 友元函数必要性：一般全局运算符重载函数需声明为类的友元，以访问类的私有成员。

##### 3）自增运算符重载函数

- 通过形参列表差异实现重载：
	- 前置++：直接对变量执行加一操作，然后返回 引用/值。声明为complex& operator++()，返回当前对象引用。
	- 后置++：需要保存旧值副本，再执行加一，然后返回副本。声明为complex operator++(int)，形参仅用于区分重载，返回旧值副本。
- 后置 ++ 比前置 ++ 多了“拷贝开销”，前置++性能更优，优先选择

##### 4）输出输入运算符重载函数

- 对内置类型可直接打印，但自定义类型需支持标准输出流操作，解决方案需为自定义类实现输出运算符重载
- 基本结构：ostream& operator<<(ostream& out, const Complex& src)
	- 必须实现为全局函数，因运算符左操作数为流对象而非自定义类实例
	- 必须返回ostream引用/istream引用以实现链式调用（如cout << a << b）
	- 流对象特性：不能添加const修饰，因流状态在输出过程中持续变化

##### 5）下标运算符重载

- 容器的下标运算符[]通常只在底层采用数组或支持随机访问的数据结构时才有实际意义，此时访问元素的时间复杂度为 O(1)。
- 对于不支持随机访问的容器（如链表），重载下标运算符不仅没有效率优势，反而可能导致误导使用者，因此一般不会实现。

## 14、自定义string

##### 1) C语言字符串操作的不足

- 固定内存分配：C语言使用字符数组存储字符串时需预先固定大小，缺乏灵活性。
- 操作复杂性：字符串比较、连接等操作需调用特定库函数（如strcmp、strcat），且需手动处理内存分配问题。

##### 2) C++ string类型的引入

- 专用字符串类型：C++引入string类封装字符串操作，简化内存管理及基本操作流程。
- 构造逻辑：
	- 非空参数：通过new char[strlen(p)+1]分配内存并拷贝数据
	- 空参数：统一初始化为含结束符的空字符串，避免后续操作中的空指针判断
- 重写三函数：析构函数、拷贝构造和赋值运算符重载，因为动态开辟了堆内存（所以要实现深拷贝）。

##### 3) 加法运算符重载与内存管理问题

- 缺陷与优化：
	- 首先，要使用new动态开辟一段大小为合并后字符串长度（含终止符）的内存，再在该内存上进行字符串的连接。
	- 如果直接构造匿名对象进行返回，那么new出来的那个内存就没有释放，会导致内存泄漏。
	- 如果构造临时的String对象进行返回，再delete掉new出来的空间，相当于重复开辟内存拷贝数据，导致效率低下。
	- 解决方法是使用移动语义优化资源转移。

```cpp
//深拷贝+运算符重载+迭代器+移动语义
class String {
public:
	String(const char* str=nullptr) {
		if (str != nullptr) {
			_p = new char[strlen(str) + 1];
			strcpy(_p, str);
		}
		else {
			_p = new char[1];
			_p[0] = '\0';
		}
	}
	~String() {
		delete[] _p;
		_p = nullptr;
	}
	String(const String& other) {
		_p = new char[strlen(other._p) + 1];
		strcpy(_p, other._p);
	}
	String& operator=(const String& other) {
		if (this == &other) return *this;
		delete[] _p;
		_p = new char[strlen(other._p) + 1];
		strcpy(_p, other._p);
		return *this;
	}
	int size() const { return strlen(_p); }
	bool operator<(const String& other) const { return strcmp(_p, other._p) < 0; }
	bool operator==(const String& other) const { return strcmp(_p, other._p) == 0; }
	bool operator>(const String& other) const { return strcmp(_p, other._p) > 0; }
	bool operator!=(const String& other) const { return strcmp(_p, other._p) != 0; }
	bool operator<=(const String& other) const { return strcmp(_p, other._p) <= 0; }
	bool operator>=(const String& other) const { return strcmp(_p, other._p) >= 0; }

	char operator[](int i)const { return _p[i]; }
	
	class iterator {
	public:
		iterator(char* it=nullptr):_it(it){}
		bool operator!=(const iterator& other) { return _it != other._it; }
		char& operator*() { return *_it; }
		void operator++() {  ++_it; }
	private:
		char* _it;
	};
	iterator begin(){ return iterator(_p); }
	iterator end() { return iterator(_p + strlen(_p)); }
private:
	char* _p;

	friend String operator+(const String& s1, const String& s2);
	friend ostream& operator<<(ostream& out, const String& s);
};

String operator+(const String& s1, const String& s2) {
	String temp;
	temp._p = new char[strlen(s1._p) + strlen(s2._p) + 1];
	strcpy(temp._p, s1._p);
	strcat(temp._p, s2._p);
	return temp;
}
ostream& operator<<(ostream& out, const String& s) {
	out << s._p;
	return out;
}
```

## 15、new与delete

#### 1）new与malloc区别

| 对比维度   | C语言(malloc/free)    | C++(new/delete)                      |
| ---------- | --------------------- | ------------------------------------ |
| 1.性质     | 库函数                | 运算符                               |
| 2.构造对象 | 不构造                | 先分配内存，后构造对象               |
| 3.失败处理 | 返回空指针            | 抛出bad_alloc异常，需要try catch捕获 |
| 4.返回类型 | void*，需手动类型强转 | 自动匹配类型                         |
| 5.数组释放 | 统一使用free          | 使用delete[]                         |

#### 2) new和delete底层

- 编译器会将new操作处理成两步：
	1. 调用operator new运算符重载分配内存；编译器强制设置为static函数，因为不存在对象!
		- operator new底层通过malloc实现，失败时抛出bad_alloc异常；成功返回内存指针。
	2. 调用定位new初始化对象（若为对象）。

![](https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251114202716749.png)	

- delete操作分两步：
	1. 调用对象析构函数（若为对象指针）；
	2. 调用operator delete运算符重载释放内存。编译器强制设置为static函数，因为不存在对象!
		- operator delete底层通过free实现，仅释放内存，无额外逻辑。

![](https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251114202734081.png)	

- 可以通过重写operator new 和operator delete来实现对内存的自定义管理，比如实现内存池

#### 3) delete与delete[]混用问题

- 内存泄漏：不管是内置类型还是自定义类型，都必须严格配对使用new/delete或new[]/delete[]，否则会导致内存泄漏或未定义行为。
	- new[]时：编译器还会隐藏的在当前内存之上使用一个额外的整数来记录数组长度，但返回的地址是对象起始地址。
	- delete[]时：会根据这个记录的额外数组长度，逐个调用对象的析构函数，最后释放掉包括额外数组长度在内的所有内存。

​	<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251114204325980.png" style="zoom: 67%;" />

#### 4) 四种new

1. 标准new：内存失败时抛出bad_alloc异常。
2. nothrow new：通过new(nothrow)声明，失败返回空指针（类似malloc）。
3. const对象new：需用const int*接收（如const int* p = new const int(40)）。
4. 定位new：在指定地址划分内存（如new (address) int(50)），不分配新内存，仅初始化。

#### 5）检查内存泄漏

1. 看new后有没有delete，或者malloc后有没有free。
2. 使用IDE的内存调试器，或Valgrind等工具。
3. 重写全局new和delete运算符重载函数，记录内存开辟和释放的位置，并统计，最后查看哪里的内存未释放。

## 16、模板

#### 1）模板

- 模板（template）是一种编译期的代码生成机制，它允许你编写通用、可复用的代码，通过参数化类型或值，让同一份代码适用于多种类型或配置。
- 模板本身不是函数/类，不可编译，仅在实例化后才会被编译。
- 语法：（模板名称+模板参数列表=函数/类名称）类模板的构造函数和析构函数可以省略模板类型参数<T>，其他地方都要加上。

#### 2) 模板的实例化

- 模板的实例化发生在调用点，依据**用户指定的类型**或通过**实参推演**出的类型生成针对该特定类型的实际代码。
- 模板类的选择性实例化：类模板实例化时，仅生成代码中实际调用的成员方法，未调用的方法不会出现在最终生成的模板类中，以减少编译工作量。

#### 3）模板的实参推演：

- 编译器根据调用实参自动推导模板类型，类模板的实参推演C++17引入。

#### 4）模板的特例化

- 对于某些特殊的类型，通用的模板函数并不能满足需求，模板的特例化允许开发者自定义类型处理逻辑，比如STL（标准模板库）广泛使用特例化来处理字符串类型（const char*），解决字符串操作与其他类型的逻辑差异问题
- 必须基于原模板声明
- 显式指定类型参数

#### 5）模板的偏特化

- 定义：类模板支持偏特化，函数模板不支持；全特化是 “所有参数都固定”，偏特化是 “部分参数固定 ”/缩小了参数的范围，比如将任意类型限定为指针、引用或数组类型或者像std::function的实现中，限定为返回值+可变参数模板类型的函数签名。

- 优先级：完全特例化 > 部分特例化（偏特化） > 原模板实例化。
- 优势：保留部分模板参数的通用性，为特定参数组合 / 类型范围提供定制实现

#### 6）非模板函数/模板函数/特例化模板函数 间的重载关系

- 调用优先级：编译器优先匹配普通函数，其次查找特例化版本，最后调用通用的模板函数

	<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251113153727862.png" style="zoom: 50%;" />	

## 17、容器空间配置器allocator

#### 1）解决的问题

- 需求：STL容器在初始化分配内存时，需要的只是“裸内存”；只有在插入元素时才会调用元素构造函数，在释放或擦除元素时调用元素析构函数。
- 问题：如果 STL 容器使用new/delete来管理元素，将导致：
	- 容器使用new进行容器的构造时，预期的行为是仅预分配内存，但是new不仅分配内存，还强制构造元素，极大的占用性能。
		- 还会导致在添加元素时，预期的行为是拷贝构造，但是因为元素已经构造过了，所以进行的是拷贝赋值了。
	- 容器使用delete进行容器的析构时，预期的行为仅对容器范围内有效的元素进行析构，并不释放内存，但是delete会将元素的内存也释放掉，但这部分内存是属于容器的。
- 解决：使用allocator将内存的（开辟/释放）与对象的（构造/析构）解耦。

#### 2）作用

- allocator是 STL 容器使用的 默认空间配置器，它是一个 模板类，默认定义在< memory >中，负责容器元素的：
	- 内存开辟（allocate）：通过malloc按类型大小与数量分配原始内存。
	- 内存释放（deallocate）：通过free释放内存块。
	- 对象构造（construct）：使用定位new在指定内存地址调用拷贝构造函数。
	- 对象析构（destroy）：显式调用对象析构函数（如~T()）但不释放内存。

## 18、容器的迭代器

#### 1)迭代器

- 泛型算法：不同容器的底层数据结构不同，可能为数组、链表、哈希表、红黑树或图等。如果想设计一套泛型算法适用于所有容器，就需要一种统一遍历所有容器元素的方式，迭代器就是为了实现这一需求的，泛型算法接收的参数都是迭代器。
- 迭代器：本质是对容器底层数据结构的一种封装，通过迭代器可以统一透明的访问所有容器内部的元素，无需关心底层数据结构。
	- 三种遍历形式：迭代器、下标、for each（底层原理也是通过迭代器实现的，要实现begin和end）
	- 每种容器都有特定的迭代器类型，不同容器的迭代器是不能进行比较运算的
- 实现：
	- 通常设计为容器的嵌套类。嵌套类如果希望主类访问自己的私有成员，就应该把主类声明为友元。
	- 迭代器类要重写三个运算符重载函数：前置++，不等于!=，解引用*
	- 容器类要实现begin和end成员方法。容器的begin方法返回容器底层首元素的迭代器，end方法返回容器底层末尾元素后继位置的迭代器。

#### 2)迭代器失效

| 容器              | 插入是否导致失效                       | 删除是否导致失效   |
| ----------------- | -------------------------------------- | ------------------ |
| **vector**        | 扩容 → 所有失效；不扩容 → 位置之后失效 | 删除位置到末尾失效 |
| **deque**         | 两端插入可能使所有迭代器失效           | 删除位置到末尾失效 |
| **list**          | ❌ 不失效（双向链表）                   | 仅删除该节点失效   |
| **map / set**     | ❌ 不失效（红黑树）                     | 仅删除该节点失效   |
| **unordered_map** | 可能全部失效（rehash）                 | 仅删除该节点失效   |

- 迭代器失效：容器内部元素位置或内存发生改变后，旧的迭代器指向的地址就无效了，继续使用会导致未定义行为。

- vector在erase和insert操作后迭代器失效解决：

	- 删除元素时，需用erase返回值更新当前迭代器，且后续元素会前移，因此无需执行迭代器自增操作。

	- 插入元素时，需用insert返回值更新当前迭代器，并且还需额外执行一次迭代器自增以避免重复处理被后移的元素。

## 19、自定义vector

1. 模板类+空间配置器+迭代器
	- 模板类：支持泛型
	- 空间配置器：外部类，将容器的内存管理和对象管理进行分离，保证高效、内存安全
	- 迭代器：内部类，提供能统一遍历容器内所有元素的方式
2. 成员变量：三指针
	- first指向底层动态数组的首地址
	- last指向最后一个有效元素的后继位置
	- end指向数组空间的末尾边界
3. 扩容机制：通过expand函数实现2倍容量增长。

```cpp
//深拷贝+运算符重载+迭代器+移动语义+模板+空间配置器
template<typename T>
struct Allocator {
	T* allocate(size_t size) {return (T*)malloc(sizeof(T) * size);}
	void deallocate(T* p) { free(p); }
	void construct(T* p, const T& val) { new (p)T(val); }
	void destroy(T* p) { p->~T(); }
};

template<typename T,typename Alloc=Allocator<T>>
class Vector {
public:
	Vector(int size=10){
		_first=_alloc.allocate(size);
		_last = _first;
		_end = _first + size;
	}
	~Vector() {
		//delete[] _first;
		for (T* p = _first; p != _last; ++p) _alloc.destroy(p);
		_alloc.deallocate(_first);

		_first = _last = _end = nullptr;
	}
	Vector(const Vector<T>& other) {
		int size = other._end - other._first;
		int len = other._last - other._first;
		//_first = new T[size];
		_first = _alloc.allocate(size);
		for (int i = 0; i < len; ++i) {
			//_first[i] = other._first[i];
			_alloc.construct(_first + i, other._first[i]);
		}
		_last = len + _first;
		_end = size + _first;
	}
	Vector<T>& operator=(const Vector<T>& other) {
		if (this == &other) return *this;

		for (T* p = _first; p != _last; ++p) _alloc.destroy(p);
		_alloc.deallocate(_first);

		int size = other._end - other._first;
		int len = other._last - other._first;
		//_first = new T[size];
		_first = _alloc.allocate(size);
		for (int i = 0; i < len; ++i) {
			//_first[i] = other._first[i];
			_alloc.construct(_first + i, other._first[i]);
		}
		_last = len + _first;
		_end = size + _first;

		return *this;
	}
	T& operator[](int i) { return _first[i]; }
	void push_back(const T& val) {
		if (full()) enlarge();
		//*_last++ = val;
		_alloc.construct(_last++, val);
	}
	void pop() {
		if (empty()) throw "vector is empty!!!";
		//--_last;
		//_last->~T();
		_alloc.destroy(--_last);
	}
	bool empty()const { return _last == _first; }
	bool full() const { return _last == _end; }
	int size()const { return  _last - _first; }
	T top()const { return *(_last - 1); }

	class iterator {
	public:
		iterator(T* p=nullptr):_p(p){}
		bool operator!=(const iterator& other) const{ return _p != other._p; }
		void operator++() { ++_p; }
		T& operator*() { return *_p; }
		const T& operator*()const { return *_p; }

	private:
		T* _p;
	};
	iterator begin() { return iterator(_first); }
	iterator end() { return iterator(_last); }
private:
	T* _first;
	T* _last;
	T* _end;
	Alloc _alloc;
	void enlarge() {
		int size = _end - _first;
		int len = _last - _first; // 当前已有元素数量
		//T* temp = new T[size * 2];
		T* temp = _alloc.allocate(size * 2);

		for (int i = 0; i < len; ++i) {
			//temp[i] = _first[i];
			_alloc.construct(temp + i, _first[i]);
		}
		//delete[] _first;
		for (T* p = _first; p != _last; ++p) _alloc.destroy(p);
		_alloc.deallocate(_first);

		_first = temp;
		_last = _first + len;
		_end = _first + size * 2;
	}
};
```



## 20、STL

### 0.概览

C++ STL是标准模板库，作为C++标准库的一部分，随编译器安装至本地操作系统。通过包含相应头文件即可调用其功能。该库采用模板技术实现，包含类库和算法库，支持通过类型实例化模板实现代码通用性。

### 1.顺序容器

#### 1）vector：向量容器

- 底层数据结构：为动态开辟的一维数组，扩容机制为每次以原空间大小的2倍进行扩容。(0,1,2,4,8,16...)
- 迭代器失效问题：连续调用insert或erase会导致迭代器失效，需更新迭代器以避免未定义行为。
- reverse和resize区别：
	- reverse：如果已知vector大致所需的大小，可以使用reverse来预留空间，减少vector的扩容操作。只负责开辟特定大小的内存，不会添加新元素。
	- resize：不仅会开辟内存，还会添加新元素，构造默认的对象。

#### 2）deque：双端队列容器

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251116160712623.png" style="zoom: 33%;" />	

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251116160825460.png" style="zoom: 33%;" />	

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251116160909101.png" style="zoom: 33%;" />	

- 底层数据结构：动态开辟的类二维数组，第一维是连续的指针数组，第二维是一个个彼此内存不连续的数组，第一维的数组从2开始，每次以2倍的方式进行扩容，第二维是固定长度的数组。每次扩容后，原来第二维的数组，从新的第一维数组的下标[oldsize/2]开始存放，上下都预留相同的空行，方便支持deque的首尾元素添加

#### 3）list：链表容器

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251116161201907.png" style="zoom: 33%;" />	

- 底层数据结构：双向循环链表  （pre  data  next）
- 操作与deque基本一致
	- 区别：单就insert/erase操作，时间复杂度仅为O(1)，但每次insert/erase基本都要查询，查询时间复杂度为O(n)

#### 4）vector、deque、list对比

- vector与deque的区别
	1. 底层数据结构：一维数组 VS 二维数组
	2. 增删查的时间复杂度：
		- 头部增删，deque更优，O(1)，vector为O(n)
		- 中部增删，都是O(n)，硬要说的话，vector更优，因为deque内存非完全连续，增删要涉及非连续内存间的移动
	3. 内存的使用效率：
		- vector必须完全连续，需要大块空间，内存使用率较低
		- deque内存非完全连续：仅第二维数组内部连续，但彼此之间非连续，所以内存使用效率上相对较高一点
- vector与list的区别
	1. 底层数据结构：一维数组 VS 链表
	2. 增删查的时间复杂度：
		- 数组支持随机访问，但增删慢O(n)，适用于查询多的场景
		- 链表增删快O(1)，但不支持随机访问，适用于增删多的场景
	3. 内存的使用效率：vector必须完全连续，需要大块空间，内存使用率较低

### 2.容器适配器

#### 0）特点

- 底层无独立数据结构，完全依赖所封装的容器实现功能
- 模板定义包含两个参数：
	- 类型参数T：指定容器适配器存储的数据类型
	- 容器参数Container：指定底层实现容器
- 方法均为代理实现，例如栈的push/pop直接调用底层容器的push_back/pop_back
- 不提供迭代器接口，无法遍历适配器内的元素

#### 1) stack：栈

- 容器参数Container：默认deque
- 后进先出(LIFO)结构

#### 2) queue：队列

- 容器参数Container：默认deque
- 先进先出(FIFO)结构，元素从队尾插入，队头删除

#### 3) priority_queue：优先级队列

- \#include<queue>
- 三参数
	- 类型参数T
	- 容器参数Container：默认vector
	- 比较器Comparator:  默认比较器std::less<T>,大根堆,首元素为堆顶,元素按优先级排序，优先级高的元素先出队(首元素)，支持自定义比较器调整优先级规则
- 弹出首元素后:
	- Step 1：交换堆顶元素和最后一个元素
	- Step 2：移除最后一个元素（原堆顶）
	- Step 3：从根节点开始向下调整（heapify-down）

#### 4）容器适配器底层依赖差异分析：

- stack与queue默认依赖deque的原因：

| 对比维度         | deque优势                    | vector劣势                       |
| ---------------- | ---------------------------- | -------------------------------- |
| 1.初始内存效率   | 预分配固定容量（如1024单元） | 需从0开始动态扩容（1→2→4→ - ..） |
| 2.头部删除复杂度 | O(1)                         | O(n)需移动元素                   |
| 3.内存碎片适应性 | 支持分段存储，利用率高       | 需连续内存块，易分配失败         |

- priority_queue依赖vector的原因：
	- 堆结构需求：默认将数据组织为大根堆结构，其本质为完全二叉树，通过数组下标关系维护节点层级（父节点i，左子节点2i+1，右子节点2i+2）
	- deque局限性：其非连续内存特性无法满足堆结构的下标计算要求

### 3.关联容器

#### 0）无序关联容器 ＶＳ有序关联容器

- 无序关联容器：unordered_map /unordered_set （链式哈希表）
	- 哈希桶（bucket）是一个数组：每个元素是一个链表/桶链（C++20 后可能是更高效的结构，如链表+指针压缩等）。每个元素根据哈希函数被映射到某个桶中，放到链表末尾。操作时间复杂度趋近于O(1)
- 有序关联容器： map/set （红黑树）
	- 所有节点满足二叉搜索树规则（左 < 根 < 右），迭代器遍历是通过中序遍历（in-order traversal）操作时间复杂度为O(log₂n)

#### 1) unordered_set：哈希集合

- 增删查：
	- 插入元素：insert(val)（无需指定位置，由哈希函数/红黑树性质决定）
	- 元素计数：count(k)（单重集合返回0/1，多重集合返回实际重复次数）
	- 删除元素：
		- 按值删除：erase(k)
		- 按迭代器删除：erase(iterator)（需注意迭代器失效问题）
	- 查找元素：find(k)返回迭代器（未找到时返回end()迭代器）
- 典型应用场景：去重

#### 2) unordered_map：哈希映射

- 核心特性：
	- 存储pair类型键值对（first为键，second为值）
	- 元素插入方式：
		- insert(make_pair(k,v))
		- C++11大括号初始化{k,v}
	- 中括号运算符特性：

| 操作类型 | 行为表现           | 注意事项                             |
| -------- | ------------------ | ------------------------------------ |
| 查询操作 | 返回键对应值的引用 | 键不存在时会自动插入（值为默认构造） |
| 修改操作 | 通过引用直接修改值 | 需确保键已存在避免误插入             |

- 典型应用场景为海量数据查重与去重，利用哈希表高效处理重复数据。

#### ３) set：集合

- 默认比较器为std::less<Key>
- 存入的数为自定义类型时需重载小于运算符，例如按学生ID排序．

#### ４) map：映射表

- 默认比较器为std::less<Key>
- 键为自定义类型时需重载小于运算符，与集合处理逻辑一致。
- []查询不存在的键时会自动插入默认构造的值，值类型需确保值类型有默认构造函数．

### ４.迭代器

#### １）常量迭代器 

| 迭代器类型     | 访问权限 | 继承关系             |
| -------------- | -------- | -------------------- |
| const_iterator | 只读     | 基类                 |
| iterator       | 可读写   | 派生自const_iterator |

- 关键限制：const_iterator解引用返回常量引用，禁止修改元素值
- 类型兼容性：基类类型可接收派生类对象（如const_iterator it = vec.begin()）

#### ２）反向迭代器

- 反向迭代器定义：通过reverse_iterator或const_reverse_iterator实现
- 核心方法：
	- rbegin()：返回指向末元素的反向迭代器
	- rend()：返回首元素前驱位置的反向迭代器

## 21、继承的本质

1. 代码的复用
2. 在基类中给所有的派生类提供统一的虚函数接口，让派生类进行重写，然后就可以使用多态了

## 22、类的访问控制（类级别的，不是对象级别的）

1. 外部只能访问对象public的成员，protected和private的成员无法直接访问。
2. 在继承结构中，派生类从基类可以继承过来private的成员，但是派生类却无法直接访问。
3. 基类的成员的访问限定，在派生类里是不可能超过继承方式的。
4. protected和private的区别？在基类中定义的成员，想被派生类访问，但是不想被外部访问那么在基类中，把相关成员定义成protected保护的；如果派生类和外部都不打算访问，那么在基类中，就把相关成员定义成private私有的。

![](https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251129152552486.png)

## 23、派生类的构造过程

- 基类和派生类各自的构造函数和析构函数，分别负责各自的成员的初始化和资源释放。
	1. 调用**基类**的构造函数，初始化从基类继承来的成员
	2. 调用**派生类**自己的构造函数，初始化派生类自己特有的成员
	3. 调用**派生类**的析构函数，释放派生类成员可能占用的外部资源（堆内存，文件）
	4. 调用**基类**的析构函数，释放派生类内存中，从基类继承来的成员可能占用的外部资源

## 24、重载、隐藏、覆盖

1. 重载关系：一组函数要重载，必须处在同一个作用域当中；而且函数名字相同，参数列表不同
2. 隐藏：在继承结构当中，派生类的同名成员，把基类的同名成员给隐藏调用了，只能通过基类作用域调用。
3. 覆盖、重写：在继承结构中，若基类是虚函数，那么派生类中有返回值、函数名、参数列表都相同的函数，那么它会被处理成虚函数，并且覆盖掉虚函数表中基类同名函数的地址。

## 25、虚函数（一个类添加了虚函数，对这个类有什么影响？）

- vftable虚函数表：在编译阶段，编译器给这个类类型产生一个唯一的vftable虚函数表，虚函数表中主要存储的内容就是RTTI（Runtime Type Information）指针、vfptr偏移量和虚函数的地址。当程序运行时，每一张虚函数表都会加载到内存的.rodata区。
- vfptr虚函数指针：在运行阶段时，该类创建的对象的内存中开始部分，多存储一个vfptr虚函数指针，指向相应类型的虚函数表vftable。
- vftable&vfptr：vftable 是静态常量表，而 vfptr 是对象里的成员变量（隐藏的），**在运行时被构造函数初始化**。一个类型定义的n个对象，它们的vfptr指向的都是同一张虚函数表；一个类里面虚函数的个数，不影响对象内存大小（vfptr），影响的是虚函数表的大小.
- 如果使用基类指针指向派生类对象：
	- 如果基类没有虚函数，基类指针指向的对象*pBase的类型，就是编译时期的类型，基类类型Base。
	- 如果基类有虚函数，基类指针指向的对象*pBase的类型，就是运行时期的类型，RTTI类型。因为基类指针指向的对象里的vfptr虚函数指针没有变，指向的虚函数表的RTTI类型还是派生类。
- 覆盖：派生类会自动继承基类的虚函数，并创建自己的虚函数表。如果派生类中的方法，和基类继承来的某个方法，返回值、函数名、参数列表都相同，而且基类的方法是virtual虚函数，那么派生类的这个方法，自动处理成虚函数，并且会在派生类的虚函数表中，使用派生类的虚函数地址覆盖掉基类的虚函数地址（先删除基类的虚函数地址，再在最后添加派生类的虚函数地址）。

​	<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251118203120291.png" style="zoom: 45%;" /><img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251118203147469.png" style="zoom: 40%;" />	

## 26、静态绑定 VS 动态绑定

- 静态绑定：在编译阶段，如果发现一个对象指针（引用）调用函数是普通函数，或者用对象本身调用函数的话，就进行静态绑定，在编译阶段就绑定函数的函数调用地址，这个地址在编译期就已经确定，所以叫静态绑定（汇编上直接call函数地址 Base::show()）。
- 动态绑定：在编译阶段，如果发现一个对象指针（引用）调用函数是虚函数，就进行动态绑定，汇编指令上call的是ecx寄存器，运行时，先在对象的内存中找到虚函数指针，再通过虚函数指针找到对应的虚函数地址，存放到ecx寄存器中，，这时才能确定要调用的虚函数，所以是动态绑定。

## 26、是否虚函数的调用都是动态绑定？

- 不是！只有通过指针或者引用变量调用，才发生动态绑定。
- 不是所有的虚函数调用都是动态绑定，如
	- 在构造函数中调用虚函数
	- 用对象本身调用虚函数，是静态绑定，因为它的类型在编译期已经确定，不可能是某种子类。
	- 调用Base：：show（）![](https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251118213507623.png)

## 27、哪些函数不能实现成虚函数？

- 虚函数依赖：对象必须存在（vfptr ->vftable->虚函数地址）
	1. 构造函数：对象还没有产生，构造函数中调用的任何函数都是静态绑定的，包括虚函数
	2. static静态成员方法：不依赖对象，没有this指针

## 28、析构函数可以是虚函数吗？是么时候必须实现成虚析构函数？

1. 虚析构函数：析构函数调用时，对象存在！所以析构函数可以是虚函数。
	- 基类的析构函数是virtual虚函数，那么派生类的析构函数自动成为虚函数
2. 黄金法则：只要一个类有虚函数，那么它的析构函数必须是 virtual
	- 因为当基类指针指向一个堆上new出来的派生类对象时，在delete基类指针执行析构函数的时候，因为没有基类的析构函数实现成virtual，就会进行静态绑定，那么只会调用基类的析构函数，此时如果派生类中使用了外部资源（堆内存、文件），则外部资源不会被释放，造成内存泄漏。
	- 如果基类实现成虚析构函数，那么将会进行动态绑定，那么首先会调用派生类的析构函数，在调用基类的析构函数，内存安全。

## 29、多态

- 静态（编译时期）的多态：在编译阶段就确定好调用的函数版本。
	- 函数重载、模板 (函数模板和类模板)
- 动态（运行时期）的多态：在运行阶段才确定好要调用的函数版本
	- 在继承结构中，基类指针（引用）指向派生类对象，通过该指针（引用）调用同名覆盖方法，基类指针指向哪个派生类对象，就会调用哪个派生类对象的同名覆盖方法，称为多态
	- 多态底层是通过动态绑定来实现的，基类指针指向哪个派生类，就会访问该派生类的虚函数指针，通过该指针访问该派生类的虚函数表，最后调用该派生类的同名覆盖方法。

## 30、抽象类、纯虚函数

- 纯虚函数：抽象类必须包含至少一个纯虚函数（如virtual void func() = 0）
	- 不可实例化对象，但可定义指针或引用变量
	- 强制派生类实现特定函数
- 作用：
	- 代码复用：派生类可直接继承基类属性（如string name）
	- 多态支持：为派生类保留统一重写接口（纯虚函数），遵循开闭原则（对扩展开放，对修改关闭），做到高内聚低耦合
- 设计场景：基类通常设计为抽象类，因其核心功能是提供派生类扩展框架而非具体实现

## 31、例题实战

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251204153815659.png" style="zoom: 45%;" /><img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251204160232750.png" style="zoom:55%;" />



#### 1）例题:交换虚函数指针

- 虚函数指针可以修改、交换，以指向不同类的虚函数表。

#### 2）例题:继承结构中的形参默认值 

- 在继承结构中，派生类虚函数的形参默认值若与基类不同，实际调用时仍沿用基类默认值，因为参数压栈在编译期确定，编译器根据基类类型生成压栈指令（push 10），动态绑定仅影响运行时调用的函数。

#### 3）例题:继承结构中的访问权限

- 基类public时，动态绑定调用派生类私有方法仍可执行，因为：
	- 访问权限检查发生在编译阶段，编译器仅检查基类指针调用的基类方法是否为public
	- 实际调用目标在运行阶段确定，通过虚函数表动态绑定到派生类方法
- 基类private时，动态绑定调用派生类的公有方法连编译都无法通过，因为不能访问基类的私有方法

#### 4）例题:继承结构中的虚函数指针初始化顺序

- 基类构造函数中的clear()：将对象内存清空！虚函数指针失效！
- 虚函数表指针写入时机：
	- 先执行基类的构造函数，再执行派生类的构造函数
	- 虚函数表指针在构造函数栈帧开辟后立即写入对象中

- 运行结果：
	- Base *pb1 = new Base()；
		- 运行崩溃，因为对象为基类，在构造函数结尾调用clear()将将内存清零了，导致其虚函数指针失效，动态绑定失败。
	- Base *pb2 = new Derive();
		- 运行正常，因为对象为派生类，先执行基类的构造函数，再执行派生类的构造函数，虽然虚函数指针在基类的构造函数中被清零了，但在派生类的构造函数中被正常赋予，能正确指向派生类的虚函数表。

## 32、多重继承

- 多重继承的本质：派生类同时继承多个基类实现代码复用，例如类C同时继承类A和类B。
- 多重继承的优点：
	- 功能复用强
	- 结构灵活,有利于解耦设计
- 多重继承的缺点：
	- 可维护性差,内存布局复杂,构造与析构顺序复杂,不同基类中同名函数冲突
	- 菱形继承问题/半圆形继承（二义性）
- 行业实践:
	- C++标准库和主流开源项目普遍避免多重继承，Java等语言直接移除该特性。建议通过组合或其他设计模式替代多重继承。
	- 如必须使用，应尽量保持每个基类职责单一，并配合虚继承解决歧义。

## 33、菱形继承的问题

- 菱形继承结构：基类A被B和C单继承，派生类D同时继承B和C，形成菱形结构。D会包含两份间接基类A的数据成员（如MA），导致数据冗余。
- 构造顺序：A→B→A→C→D，基类A被重复初始化。

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251120132912208.png" style="zoom: 33%;" /><img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251120132936260.png" style="zoom:50%;" /><img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251120133553284.png" style="zoom:50%;" />



- 解决方案：B和C都虚继承A，使派生类D仅保留一份A的成员MA，虚基类数据置于内存末尾。通过虚基表指针（VBPTR）定位唯一基类数据位置，消除数据二义性。
- 初始化责任：虚基类数据必须由最底层派生类（D）直接初始化，所以需在派生类d中显式调用虚基类a的构造函数，或确保虚基类a提供默认构造函数,否则会报错

## 34、虚继承

#### 1)虚继承&虚基类

- 虚继承：虚继承导致派生类对象内存中的基类数据偏移到末尾，并在内存开头新增虚基类指针（vbptr），指向虚基类表（vbtable在编译期生成），表中存储偏移量以定位虚基类数据。
- 虚基类指针（vbptr）：基类数据偏移至派生类末尾，原位置由虚基类指针替代。
- 虚基类表（vbtable）：
	- 虚基类指针偏移量,一般为0
	- 虚基类数据的偏移量（如第二行值为8，指向mA的存储位置）。
- 虚函数表（vftable）对比：包含RTTI指针 和 虚函数指针偏移 和 虚函数地址
	- 虚函数表指针共享机制：当基类已存在虚函数时，派生类复用基类vfptr而非新建指针

#### 2)基类指针内存问题

- 堆内存释放：基类指针始终指向派生类中基类数据的起始地址，而如果使用虚继承会导致基类指针偏移，导致内存释放起始地址错误，无法使用基类指针直接释放堆上派生类对象的内存
- 内存分配验证：通过重载operator new显示实际分配地址（0xD0）与基类指针持有地址（0xD8）存在8字节偏移

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251120133918459.png" style="zoom:50%;" />	

- 解决方案：基类的析构函数设置为虚析构函数，就会先调用派生类的析构函数，再调用基类的析构函数。

#### 3)虚基类指针和虚函数指针内存占用

- 空类在C++中占用内存为1字节，类A和类B的sizeof结果均为1
- 基类添加虚函数后，派生类B的sizeof变为4字节，因包含虚函数表指针（vfptr）
- 虚继承情况下，派生类B的sizeof扩展为8字节，新增虚基类表指针（vbptr）

## 36、四种类型转换方式

1. static_cast: 编译时进行安全的类型转换，不安全报错。
2. dynamic_cast: 运行时类型检查，转换失败时返回nullptr，常用来检测基类指针指向的对象是否是某一特定派生类。
	- RTTI机制（Runtime Type Information）：运行时获得对象的类型信息，通过typeid和dynamic_cast实现的。
3. const_cast: 去除对象const属性，以便可以调用普通函数。
4. reinterpret_cast: 类似C风格的强制类型转换，不安全。

## 37、临时对象的优化

- 返回临时对象机制：返回值时通过拷贝构造在调用函数的栈帧上创建临时对象，将局部对象的值带出函数作用域，临时对象在赋值完成后析构。
- 临时对象拷贝构造同类型新对象：会被编译器优化为直接使用构造函数，而不构造临时对象（即使是调用函数返回的临时对象，返回值优化（RVO））
	- **RVO/NRVO 的目标（c++17）：减少构造次数 → 尽可能避免 copy / move 的开销。**因此编译器会“跳过移动构造函数”，不是因为移动构造不好，而是因为「连移动都不需要」。

- 三大法则：
	1. 函数参数传递过程中，对象优先按引用传递，不要按值传递
	2. 函数返回对象的时候，应该优先返回一个临时对象，而不要返回一个定义过的对象
	3. 接收返回值是对象的函数调用的时候，优先按初始化的方式接收，不要按赋值的方式接收

## 38、右值引用

- 左值定义：具有内存和名称的变量（如变量a）。
- 右值定义：无名称或无内存的临时量（如常量20、临时对象）。
- 绑定规则：
	- 左值引用（&）只能绑定左值，右值引用（&&）只能绑定右值
	- 右值引用变量本身是左值（有内存和名称），需用左值引用绑定（如int &f = d）。

## 39、 移动语义

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251123143222426.png" style="zoom: 67%;" />

- 解决的问题：临时对象构造时冗余内存操作
	- 当我们调用函数时，返回值是通过在调用放栈帧构造一个临时对象来带出返回值的，所以很多时候临时对象的构造是无法避免的。并且当一个类若拥有外部资源（如堆内存、文件句柄等），那么它在构建一个临时对象的时候存在冗余的内存操作。临时对象在执行完开辟内存并拷贝数据的操作后，又被立即析构，多此一举。
- 优化方向：通过移动语义直接将临时对象资源转移至目标对象，可避免临时对象的资源重复分配与拷贝。
- 实现：对于临时对象提供带右值引用的重载版本。
	- 带左值引用参数的拷贝构造和赋值重载：处理普通对象（有内存和名称），执行深拷贝（开辟独立的内存，并拷贝数据）。
	- 移动构造函数（String(String&& other)）和移动赋值运算符（String& operator=(String&& other)）：处理临时对象（无名称），直接转移资源并置空原指针（这样析构临时对象时就是delete nullptr，安全无害），防止双重释放。

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251123142154822.png" style="zoom:50%;" />

- 问题一：想要确保使用移动语义，就需要确保对象构造过程中调用的函数传入的参数都是右值，而右值本身是个左值，调用右值本身进行传参，会使用左值引用，无法触发移动语义。
	- 解决：可以传参时使用std::move将对象显式转换为右值引用。

## 40、完美转发

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251123143526357.png" style="zoom: 50%;" />

- 问题二：对于拷贝构造和赋值重载函数，如果想实现传入左值时使用左值引用，传入右值时使用右值引用，就需要分别写两个重载版本，很麻烦。
	- 解决：可以使用函数模板+std::forward+万能引用，通过模板将两个重载整合到一起，并通过std::forward自动的识别传入的参数是左值引用还是右值引用并保留。这就是完美转发。

## 41、裸指针问题

- 定义：裸指针只存储一个地址，不会自动释放内存，也不携带引用计数或资源管理逻辑。
- 不安全：没有任何生命周期信息，容易出现悬空指针、野指针、重复释放、内存泄漏。

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251124154721549.png" style="zoom: 80%;" />

## 42、智能指针有哪些？

- 定义：智能指针是C++中用于管理内存资源的工具，底层封装裸指针，采用RAII思想，利用栈对象出作用域时自动析构的特性，实现自动的释放资源的功能，达到防止内存泄漏的目的。
- 分类：
	- 不带引用计数的智能指针：包含auto_ptr、scoped_ptr、unique_ptr
	- 带引用计数的智能指针：包含shared_ptr和weak_ptr，重点解决交叉引用和多线程共享对象问题

## 43、unique_ptr实现

1. 禁止拷贝构造和赋值。
2. 提供了移动构造和移动赋值运算符重载函数（带有右值引用参数的拷贝构造函数和赋值运算符重载函数），支持显式调用std::move实现资源所有权转移（右值引用机制），资源所有权转移后，原unique_ptr（如p1）不再持有资源，访问其内容将导致未定义行为
3. 局限：无法实现多个智能指针管理同一个资源

```cpp
template<typename T>
class Unique_ptr {
private:
	T* _ptr;
public:
	Unique_ptr(T* ptr=nullptr):_ptr(ptr){}
	~Unique_ptr() { delete _ptr; }
	Unique_ptr(const Unique_ptr&) = delete;
	Unique_ptr& operator=(const Unique_ptr&) = delete;
	Unique_ptr(Unique_ptr&& other) {
		_ptr = other._ptr;
		other._ptr = nullptr;
	}
	Unique_ptr& operator=(Unique_ptr&& other) {
		if (this == &other) return *this;
		delete _ptr;
		_ptr = other._ptr;
		other._ptr = nullptr;
		return *this;
	}
	T& operator*() { return *_ptr; }
	T* operator->() { return _ptr; }
};
```





## 44、shared_ptr底层/引用计数如何实现？

1. shared_ptr底层有两个指针，一个是指向实际对象的指针，一个是指向控制块的指针。
2. 控制块：
	1. 引用计数（use_count)：统计有多少个shared_ptr指向该对象。
	2. 弱引用计数（weak_count）：统计有多少个weak_ptr指向该对象，它不影响对象的生命周期，仅仅只是观察。
	3. 对象指针
	4. 删除器（deleter）：指定对象的析构方法，默认是普通的delete（可以在模板参数列表中传入相应的删除器类型，删除器是一个函数对象，其()重载函数，对于文件操作要修改为fclose，对于数组要修改成delete[]）
	5. 空间配置器（allocator）：用于分配与释放控制块的内存空间。
3. 引用计数控制对象生命周期：当使用shared_ptr拷贝时，不会新创建对象，而是指向同一个对象，并使得引用计数加一；只有当引用计数归零时，对象才会析构。
4. 弱引用计数控制控制块的生命周期：当引用计数归零时，控制块的内存还不会释放，只有当弱引用计数归零时，才释放控制块的内存。因为弱指针底层持有控制块指针，若控制块内存被回收了，此时弱指针调用lock方法就会访问野指针，照成未定义行为。

## 45、weak_ptr底层/如何解决shared_ptr循环引用问题？

1. 循环引用问题：两个对象通过shared_ptr互相持有，导致引用计数永远无法归零，最终导致内存泄漏。
2. 使用weak_ptr打破循环引用：将原有的一个或多个shared_ptr改为weak_ptr，weak_ptr指向对象并不会增加引用计数，以此来打破循环。
3. weak_ptr底层只有有一个指向控制块的指针，没有指向对象的指针；当需要访问对象时，会先通过lock方法提升为临时的shared_ptr，访问结束后自动释放，既安全又不影响引用计数。

## 46、为什么尽量使用make_shared？

- 因为绝大部分情况使用make_shared是利大于弊的
- 优点：
	1. 内存管理开销小：使用裸指针构造的话，会先后构造对象和控制块两块内存，释放的时候也要释放两次。而使用make_shared后，会为对象和控制块一次性分配一整块内存，释放的时候也只需要释放一次。
	2. 内存碎片少，缓存命中率高
	3. 更安全：直接使用裸指针构造的话，对象和控制块可能一个内存分配成功一个失败，因此可能造成内存泄漏。
- 缺点：
	1. 不支持deleter：像数组、文件可能无法传入deleter，C++17 已支持 make_shared创建数组。
	2. 会延迟释放对象内存：理论上引用计数为0时，对象就该释放内存，但使用make_shared后要对象内存和控制块要一起释放，要等弱引用计数也为0时才全部一起释放。

## 47、如何从类的内部返回this的shared_ptr,原理是啥？

1. 从类的内部返回this的shared_ptr是不安全的，它会新建一个shared_ptr，导致引用计数出错。
2. 解决：让类继承enable_shared_from_this，然后返回shared_from_this，底层通多weak_ptr实现。

## 48、shared_ptr是否线程安全？

1. 是线程安全的，shared_ptr底层的引用计数采用了C++11提供的atomic，通过原子操作保证引用计数的++和--是线程安全的，不会导致内存泄漏。
2. 虽然shared_ptr的引用计数的线程安全的，但是对shared_ptr指向的对象的操作不是线程安全的，需要自己加锁。



## 49、强弱智能指针的选择问题

1. 循环引用时，必须使用weak_ptr来防止内存泄漏。

2. 业务逻辑上：

	1. 如果两个类的生命周期要各自独立，我销毁了，不影响你是否销毁，

		（玩家死亡时，装备掉地上），那么使用weak_ptr;

		1. 比如我正在攻击一个目标，我要记录这个目标，就可以使用weak_ptr，因我和目标的生命周期互不影响，我只是作为一个观测者，他的生命周期由他管理，不由我管理。

	2. 如果一个类需要管理另一个类的生命周期，我销毁了，你也要销毁，（玩家死亡时，装备和玩家一起消失），那么使用shared_ptr

## 50、function

- 本质：是一个类模板，作为一个函数包装器，具有类型擦除的功能，屏蔽不同可调用对象（函数对象、成员函数、普通函数）之间的类型差异，返回一个仅关注“返回值+参数列表”的抽象类型。
- 场景：
	1. 实现回调函数（最核心场景），这类需要将函数 作为参数传递的情况（如事件响应、异步任务、框架扩展），统一回调接口，屏蔽不同回调实现（Lambda / 成员函数 /bind 结果）的类型差异。
	2. 存储可调用对象，比如lambda表达式，还可以把它们放到容器中。
- 底层：靠**类型擦除 + 多态**实现+模板偏特化，用抽象基类定义通用调用接口，派生类封装具体可调用对象，主体持有基类指针实现统一调用，最终达成 “屏蔽类型差异、统一封装任意可调用对象” 的核心目标。

## 51、bind

- 本质：是一个函数模板，一个函数适配器，可以将可调用对象的部分或全部参数进行绑定，并返回一个函数对象，一般配合placeholders使用。

## 52、lambda底层实现

1. 本质：lambda表达式本质上是一个匿名的函数对象，编译器底层会生成一个匿名类，重载()运算符，调用lambda表达式就是调用该匿名类对象的()运算符重载函数。
2. 优势：lambda表达式最大的优点是它很简单轻量、可读性也好，它不用我们很麻烦的去重新定义一个类。
3. 语法：`[捕获列表](参数列表)mutable->返回值{函数体}`
	1. 捕获列表：
		- 主要分为值捕获=、引用捕获&、this指针，也可以组合。
			- 捕获的本质是捕获外部变量，通过匿名类的构造函数初始化匿名类里的私有的成员变量，如果值捕获就会复制一份外部变量到类内，如果是引用捕获就是引用绑定，如果捕获的是this就可以访问该类的的所有成员变量（前提是lambda表达式在类内）。
		- 若未捕获任何外部变量，lambda表达式可以转化为函数指针，编译器会额外生成一个静态成员函数，因为它可以不依赖对象，可以不用this指针调用。
	2. mutable：匿名类默认生成的()运算符重载是const修饰的，不允许修改成员变量的值。而加了mutable就会去掉const，就允许修改成员变量的值了。
	3. 返回值：编译器会自行推导，可省略。

## 53、bind 和 lambda区别

1. 都会生成函数对象
2. 适用场景不同：
	1. bind适用于调整参数顺序、适配成员函数（只需绑定函数指针和对象，lambda还需要写参数列表，比较麻烦）
	2. 其他场景lambda比较合适，轻便

## 54、lambda引用捕获风险

1. 如果引用捕获了局部变量，而lambda的生命周期又大于该局部变量的话，就会出现悬空引用的现象，访问时触发未定义行为
2. 多线程环境下，引用捕获的变量若被多线程同时修改 / 访问，且无同步机制，会引发**数据竞争**（一种未定义行为）

## 55、lambda表达式可以拷贝吗？

- 可以拷贝，但主要取决于捕获的对象是否可以拷贝。像unique_ptr、mutex这种禁止了拷贝构造和赋值运算符重载的对象就不能进行拷贝。



## 56、什么是线程？它与进程有什么区别？

- 线程（Thread）：进程内的一个执行流，拥有自己的栈、寄存器上下文，但与同属进程的其他线程共享地址空间、文件句柄等资源。


- 进程（Process）：操作系统分配资源的最小单位，拥有独立的虚拟内存空间、句柄表和系统资源。


- 区别要点：
	- 资源隔离：进程间相互独立，线程间共享数据更高效但需同步。
	- 创建开销：线程要比进程轻量，创建/切换开销更小。
	- 通信方式：线程间直接读写共享内存；进程间需 IPC（管道、消息队列、共享内存段等）。

## 57、进程间通讯、线程间通讯

- 进程间通讯：
	1. 管道
	2. 消息队列
	3. 共享内存
	4. 信号
	5. 信号量
	6. socket
- 线程间通讯：
	1. 互斥锁
	2. 读写锁
	3. 条件变量
	4. 自旋锁
	5. 信号量

## 58、什么是数据竞争（data race）？如何避免？

- 定义：两个或以上线程并发访问同一内存位置，至少有一个写操作，且没有使用同步原语，会导致未定义行为。


- 避免方法：
	- 互斥锁：std::mutex + std::lock_guard/std::unique_lock
	- 原子类型：std::atomic< T>，适合简单计数／读写。
	- 其他同步工具：std::condition_variable、读写锁（std::shared_mutex）。

## 59、std::mutex、std::lock_guard、std::unique_lock 有何区别？

- std::mutex：底层互斥量，只提供 lock()/unlock()。

- std::lock_guard< std::mute>：
	1. 简单 RAII，不用手动解锁。
	2. 底层还是mutex，构造时mutex.lock()，析构时mutex.unlock()，利用出作用域就自动析构的性质。
	3. 左值引用的拷贝构造和赋值运算符重载函数被禁用，不可以用在函数参数传递或者返回过程中，只能用在简单的临界代码段的互斥操作中。
- std::unique_lock< std::mutex >：
	1. 更灵活，支持延迟上锁、手动 unlock()/lock()、可转移所有权。
	2. 与 std::condition_variable 配合，完成线程间的同步通信。
	3. 有右值引用的拷贝构造和赋值运算符重载函数，可以用在函数参数传递或者返回过程中。

## 60、什么时候需要使用 std::atomic< T >？它和加锁相比有什么优缺点？

- 适用场景：对一个基本类型（整型、指针）做简单的原子读-改-写操作，如计数器、自旋锁等。


- 特点：无锁（lock‑free），延迟低；只适合简单操作，复杂逻辑仍需锁。

- volatile：多线程中，某一全局变量的更改并不一定会立马反应到其他线程，因为其他变量会对全局变量会进行缓存。使用volatile就是告诉编译器不要进行优化，直接到内存上读值，虽然会慢一些，但会保证运行的正确性。

## 61、如何使用 std::condition_variable 实现生产者-消费者模型？

```cpp
#include<iostream>
#include<queue>
#include<mutex>
#include<condition_variable>
#include<thread>

int ItemSize = 1000;
std::mutex mtx;
std::queue<int> q;
std::condition_variable cv;
bool finished = false;
void producer() {
	for (int i = 0; i < ItemSize; ++i) {
		{
			std::unique_lock<std::mutex> lck(mtx);
			q.push(i);
			std::cout << "producer:" << i << std::endl;
		}
		cv.notify_one();
	}
	{
		std::unique_lock<std::mutex> lck(mtx);
		finished = true;
	}
	cv.notify_all();
}
void consumer() {
	while (true) {
		std::unique_lock<std::mutex> lck(mtx);
		cv.wait(lck, []() {return !q.empty() || finished; });
		if (!q.empty()) {
			int item = q.front();
			q.pop();
			std::cout << "consumer:" << item << std::endl;
		}
		else if (finished && q.empty()) {
			break;
		}
	}
}
int main() {
	std::thread t1(producer);
	std::thread t2(consumer);
	std::thread t3(consumer);
	t1.join();
	t2.join();
	t3.join();
	return 0;
}
```

- cv.wait(uni_lck)；
	1. 先使线程进入等待状态，知道cv.notify()之后进入阻塞状态，获得锁后进入运行态。
	2. 释放锁。防止死锁。
- 使用 wait(lock, predicate) 避免虚假唤醒。
- notify_one 唤醒一个消费者，notify_all 在结束时唤醒所有。

## 62、解释死锁的四个必要条件，并举例说明如何预防。

- 四条件：互斥（mutex）、持有并等待、不可剥夺、循环等待。


- 预防策略：

	1. 锁排序：所有线程按全局统一固定顺序加锁。**资源有序分配法，来破坏循环等待条件**
	2. 限时锁：持有锁超过一定时间就释放锁。

## 63、什么是读写锁？何时用 std::shared_mutex？

- 读写锁：允许多个读者（shared lock）并发访问，写者（exclusive lock）独占访问。

- 使用场景：读操作远多于写操作时，可大幅提高并发性能。
- C++17 提供 std::shared_mutex，与 std::shared_lock / std::unique_lock 配合使用。



# 设计模式

## 1、单例模式

- 定义：单例模式是一种创建型设计模式，保证某个类在程序运行期间只有一个实例，并提供一个全局访问点来获取这个实例。

	1. 构造函数放在private里，防止外部进行对象的构造，同时删去拷贝构造和赋值重载。
	2. 写一个getInstance方法返回全局实例。

- 分类：

	1. 饿汉单例：

		- main执行之前就已经初始化了，不管有没有用到，性能较差。

		- 使用静态成员 `Singleton::instance` ，在 **程序启动阶段**（main执行之前）就已经初始化了
		- 线程安全（因为初始化阶段没有并发）

	2. 懒汉单例：

		- 使用单例对象时，才进行初始化，性能较好。
		- 一般使用静态局部变量来实现线程安全的懒汉单例，放在数据段，第一次访问时初始化，之后始终返回该实例。
		- 线程安全由编译器自动完成，它会为我们自动上锁、解锁。

```cpp
#include<iostream>
#include<mutex>

class Singleton {
public:
	static Singleton* getSingleton() {return &instance;}
private:
	static Singleton instance;
	Singleton() { std::cout<<"Singleton()" << std::endl; }
	Singleton(const Singleton&) = delete;
	Singleton& operator=(const Singleton&) = delete;
};
Singleton Singleton::instance ;

//使用静态局部变量的懒汉单例：放在数据段，第一次访问时初始化，之后始终返回该实例。线程安全由编译器自动完成。
class LazySingleton {
public:
	static LazySingleton* getInstance() {
		static LazySingleton instance;
		return &instance;
	}
private:
	LazySingleton(){ std::cout << "LazySingleton()" << std::endl; }
	LazySingleton(const LazySingleton&) = delete;
	LazySingleton& operator=(const LazySingleton&) = delete;
};

int main() {
	Singleton* p1 = Singleton::getSingleton();
	Singleton* p2 = Singleton::getSingleton();
	Singleton* p3 = Singleton::getSingleton();
	std::cout << p1 << "," << p2 << "," << p3 << std::endl;
	LazySingleton* l1 = LazySingleton::getInstance();
	LazySingleton* l2 = LazySingleton::getInstance();
	LazySingleton* l3 = LazySingleton::getInstance();
	std::cout << l1 << "," << l2 << "," << l3 << std::endl;
	return 0;
}
```

## 2、简单工厂、工厂模式、抽象工厂

| 模式         | 工厂数量                                | 优点                                                     | 缺点                                   |
| ------------ | --------------------------------------- | :------------------------------------------------------- | -------------------------------------- |
| **简单工厂** | 1 个工厂封装所有产品的new，简单switch   | 简单、隐藏new                                            | 新增产品时要修改工厂代码，违反开闭原则 |
| **工厂方法** | 1个抽象的工厂类，每种产品一个工厂       | 新增产品时只需要新增新工厂，不用修改原代码，符合开闭原则 | 一个产品一个工厂，类爆炸               |
| **抽象工厂** | 1个抽象的工厂类，每种**产品簇**一个工厂 | 一个工厂生产整套产品族，有一定分类结构                   | 抽象基类的虚函数都要实现               |

## 3、代理模式

- 思想：代理模式是一种 **结构型模式**，在不修改原有委托类的情况下，通过将原有委托类对象封装代理类中，实现对委托类的访问权限的管理。
- 实现：
	1. 代理类和委托类都继承自抽象类。
	2. 采用组合，代理类中有委托类的对象或对象指针。
	3. 调用时传入特定的代理类。

## 4、装饰器模式

- 思想：装饰器模式是一种 **结构型模式**，在不修改原有类的情况下，通过将原有类对象和新功能封装到一起，实现原有类功能的拓展。
	- 相比于通过继承添加子类实现新功能的增加：装饰器模式组合更灵活，不会派生出过多的子类。

![](https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251121165932318.png)	<img src="../../AppData/Roaming/Typora/typora-user-images/image-20251121170044195.png" alt="image-20251121170044195" style="zoom: 60%;" />

## 5、适配器模式

- 思想：把一个接口转换成客户期望的接口，让原本不兼容的类可以协同工作。
- 实现：最常用的就是对象适配器，用“组合”这个思路，让适配器里面保存一个 old 接口的对象，同时自己继承新接口；这样外部调用的是新接口的方法，适配器内部再把这个调用“转发”成 old 对象的老方法，相当于给旧代码加了一个“转换头”。
- 场景：当使用外部库接口不一致时，又不想重构代码，可以使用适配器模式。

## 6、观察者模式

- 思想：观察者模式是一种 **行为型模式**，采用一种“订阅—通知”机制，观察者首先订阅被观察者的特定行为或状态，当一个被观察者对象的状态发生改变时，它会通知所有的观察者对象，观察者对象执行自己的回调函数，这使得观察者与被观察者不必强耦合。
- 实现：被观察者底层维护一张观察者表，状态改变时，遍历所有订阅者并通知。
- 场景：游戏开发实例（事件系统、UI 更新、属性变动）

## 7、MVC模式

## 8、MVVM模式



# 数据结构

## 1、哈希表

1. 哈希表：对于给定的key和val，通过一个哈希函数将key映射到一个下标，在对应位置存储value，从而实现平均时间复杂度都为O（1）的查找。缺点是占用内存空间较大。
2. 哈希函数：需要满足以下特性：运算快、分布均匀（冲突少）
	1. 直接定址法：f(key)=a*key+b,简单的线性映射，若key不重复，则不会产生冲突，适用于key相对连续的情况
	2. 除留余数法：f (key) = key mod p，不仅可以对关键字直接取模，也可以平方取中法等运算之后取模。p的取值很重要，一般取小于等于表长的最大素数，否则冲突的概率会比较大
	3. md5、sha等加密算法
3. 哈希冲突：
	1. 线性探测：每次产生冲突时就往后面寻找空位存放，容易产生堆积现象，ASL平均查找长度较大。
	2. 二次探测：对线性探测的改进，左右都可以探测空位，并且距离呈平方增加，缓解堆积现象。
	3. 链地址法：使用链表存取产生冲突的所有值。
4. 扩容（Rehash）
	- 当负载因子 load factor > 阈值（一般 0.75）时，容量增大，所有元素重新哈希
	- 代价高，但发生频率低，所以平均仍是 O(1)

## 2、红黑树 VS AVL

1. 红黑树：是一颗自平衡的弱平衡搜索二叉树。
	1. 弱平衡：是指对于每一颗子树的左右子树，高的那颗不超过低的那颗的两倍。
	2. 搜索二叉树：是指对于每一个子树，它的根节点的值会大于所有左子树节点的值，并且小于所有右子树节点的值，所有中序遍历这棵树得到的是递增序列。
	3. 自平衡：指的是当树的平衡规则被打破，树会根据一定的规则，通过节点的旋转，重新满足平衡并且依旧满足红黑树定义规则。
	4. 红黑树定义规则：
		1. 根节点和叶子节点都为黑色，且叶子节点为空节点。
		2. 从根节点到每一个叶子节点的路径中，所以经过的黑节点个数相同。
		3. 若一个节点为红色，其子节点必须为黑色。
2. 红黑树 VS AVL
	1. 概念：红黑树是一颗弱平衡二叉树，它要求每一颗子树的左右子树，高的那颗不超过低的那颗的两倍；而AVL树是一颗严格的平衡二叉树，他要求每一颗子树的左子树和右子树的高度差不超过1。
	2. 查询效率：由于红黑树和AVL树对平衡的严格程度不同，相对来说在节点数相同的情况下，红黑树的树高是要比AVL树高一些的，查询速度和树高高度相关，所以红黑树的查询速度要比AVL树稍慢一些，但不会差很多。
	3. 插删效率：因为插入和删除都可能破坏红黑树和AVL树的平衡，再通过旋转节点达到平衡，因为红黑树是弱平衡，平衡要求没那么高，所以平衡开销也会相对较小，所以插入和删除的效率高于AVL树。
	4. 使用场景：对于频繁插入、删除的场景使用红黑树，对于查询更平凡的场景使用AVL树。

## 3、B树 VS B+树

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20251117151244539.png" style="zoom: 25%;" />

1. B树是一颗自平衡的M叉搜索二叉树，并且叶子节点都在同一层，所有节点既有key也有value（记录）。
	1. m阶B树有m条分叉，最多m-1个元素，每个节点最少有[m/2]-1个元素
	2. 插入元素：先将根节点从磁盘加载到内存，再在内存中使用二分查找到该插入的位置。插入后若节点数超出m-1个，则取中间节点加入父节点，并分裂原节点，若父节点也超出最大节点个数，则继续分裂。
2. 对超大规模数据进行查找时，一般这些数据都存储在硬盘上，要用时在从磁盘加载到内存中，其中磁盘IO的时间要远远大于内存读取的时间，所以要加快查找速度，就要减少IO次数，而IO次数又和树的高度呈正相关。所以B树相比于AVL树是一颗多叉树，在相同节点个数的情况下，树高较小，IO次数也较少，查找效率更高。
3. B+树 VS B树：
	1. B+树的非叶子节点只存关键字，相当于只是索引，所有的记录都存在叶子节点上，适用于充当数据库的索引结构；B树每个节点即存关键字也存记录。
	2. 同样是M阶，B+树每个节点最多有m个关键字，m个分支，关键字代表该分支内最大的数；而B树每个节点最多有m-1个关键字，m个分支，分支内的关键字的值位于左关键字和右关键字之间。
	3. B+树还将底层叶子节点串成一个链表，天然支持顺序查找和随机查找，而B树只支持随机查找，遍历所有节点的话要中序遍历。

## 4、海量数据查重/去重

1. 哈希表unorder_map（无内存限制）
2. 哈希表思想+分治（有内存限制）：分多个文件，文件数=所需内存/限制内存，根据这个文件数选择一个最相近的素数作为文件数，来做除留余数法，data%文件数=放入的文件下标，同一个数一定会映射到同一个文件，最后分文件统计。
3. 布隆过滤器
	1. 一个 **位数组（bit array）**+多个 **哈希函数**
	2. 所有数分批次通过多个哈希函数映射到维数组中，映射到的位置置1；查询时，通过多个哈希函数映射后如果某位为0，则一定不在；若通过了，则可能在。
	3. 可以允许一定的误判、内存很小的情况下，判断一个元素是否存在。
4. 字典树Trie Tree（对于字符串）

## 5、海量数据Top K问题

1. 堆（N*logK）：先把前K个数组成一个大根堆（小根堆），堆顶就是这K个数中最大的，再遍历所有数，比他小就加入并弹出堆顶，最后堆中剩下的就是前K小的数了。
2. 快排分割（N）：每次随机选择一个数作为枢纽来划分数组，比它小的放左边，比它大的放右边，最后枢纽值会在它排完序后应在的位子，可以算出它是第几大/小的值，根据它和K的比较关系，再去左边或右边继续递归。
3. 大文件+内存限制——>分治思想：分多个文件，文件数=所需内存/限制内存，根据这个文件数选择一个最相近的素数作为文件数，来做除留余数法，data%文件数=放入的文件下标，同一个数一定会映射到同一个文件，最后分文件统计。

## 6、最小生成树

- 代价最小的联通图，n个节点，一定有n-1条边
- Kruskal算法：选边法，适合稀疏图，贪心思想，并查集
	1. 将所有边加入小根堆排序
	2. 每次弹出堆顶最小的边，使用**并查集**判断这条边的两个端点是否是不同的集合
	3. 若属于不同的集合，则加入边，并合并集合；否则什么都不做
	4. 直到选了n-1条边
- Prim算法：选点法，适合稠密图，贪心思想，加强堆（下标反向索引表）
	1. 可以从任一点出发，该点加入集合
	2. 该点出发的所有边加入小根堆，每次弹出堆顶的最小边，看它指向的点是否再点集中
	3. 如果指向的节点在点集中，丢弃；不在点集中，则加入集合，并把该点出发的边都加入小根堆
	4. 一直循环，直到堆空，若点集个数为n，则全连通

# HR

## 1、自我定位类

### 自我介绍、学习经历、为什么学 Unity/UE、学习路径、项目来源

1. 自我介绍：面试官您好！我是刘平安，目前就读于中国海洋大学计算机技术专业硕士，研究方向聚焦图形学，预计2026年6月毕业，渴望应聘游戏客户端开发岗位，可立刻到岗并实习3个月以上。本科阶段我就读于浙江理工大学建筑学专业，不仅以专业前20%的绩点斩获优秀学生二等奖学金，还凭借三维建模与空间审美积累，在多项省级以上建筑竞赛中获奖；硕士期间绩点稳居专业前5%，荣获校级学习优秀奖学金，科研上合作发表1篇中科院二区图形学论文，另有1篇一作论文在投。我始终热爱游戏开发，具备UE5引擎的使用经验、C++编程与图形学技术，希望能将专业所长与实践积累应用到岗位中，为团队贡献力量。
2. 项目介绍：
	- 我用UE5做了个地牢风的RPG游戏原型，核心是靠GAS系统搭技能、属性和战斗这一套，还支持多人联机。
		技术上主要用了UE5的C++、蓝图，还有行为树、MVC/MVVM这些架构。代码和演示视频都放GitHub和B站了，随时能看。
		具体做的事挺多的：比如用增强输入实现鼠标点哪里角色走哪里、键盘放技能，动画上做了走路、受击、死亡溶解这些效果；多人同步这块，靠GAS和UE的复制机制，保证大家玩的时候技能、属性都是一致的；数值系统能支持不同职业成长，伤害计算也能算护甲、暴击这些；UI方面，不管是属性面板、技能栏还是存档功能，都做得比较灵活，技能还能拖拽装配；AI敌人会寻路、近战远程攻击，甚至能召唤小怪，关卡里敌人生成和掉装备的规则也能配置，性能也优化过。
	- 简易光栅渲染器（2025年3月到4月）这个是我自己做的小项目，没依赖任何现成的渲染API，就是想搞懂3D图形怎么渲染出来的。最后实现了不少基础功能：比如把3D模型通过矩阵变换转成屏幕上的图像，用算法画直线、填三角形，还加了深度测试避免物体互相穿透；画质上做了抗锯齿，光照用了Blinn-Phong模型，能模拟环境光、漫反射和高光，贴上图之后细节更清楚；还试着做了光线追踪，用BVH加速计算，甚至加了全局光照的模拟，最后用多线程把渲染速度提上去了。
3. 为啥转行：我本科是浙江理工大学建筑学的，主要的学习内容是空间设计和美术，还有一些3D建模和渲染的工具，比如PS、3dMax等，我转行的契机是因为我觉得建筑这个行业随着房地产的衰弱，未来的前景我不是很看好，并且我在大三的时候在一家建筑设计公司实习，结合网上的信息和实习的直观感受，我就决定转行。

## 2、职业动机类

### 为什么选择游戏行业/客户端、找工作关注什么、是否了解公司产品

1. 为啥选择游戏行业：因为我从小比较喜欢打游戏，从幼儿园就开始去街机厅，从小到大主要的娱乐方式主要就是打游戏，所以当我考虑转行的时候，就希望往游戏行业发展，所以我就打算跨考计算机，最后选择的导师的方向也是与游戏客户端相关度比较高的图形学。
2. 选择游戏客户端的原因：第一点还是喜爱，因为游戏客户端它是制定的是游戏运行的规则，游戏好不好玩很大程度上与玩法高度相关，所以我觉得这个岗位很酷。第二点是岗位匹配度，因为我本科学的是建筑，有一定的美术基础，游戏客户端也算是前端，算是美术和技术的结合，所以我觉得我在游戏客户端这个岗位上有一定的美学优势，能将我本科的学习和研究生的学习内容结合起来。
3. 职业规划：职业规划的话，我是想一直在游戏客户端这个方向深耕，我短期1~2年的目标是先熟悉完整的客户端开发流程，UI、玩法、网络、渲染等；中期3 ~5年，我会选择一个感兴趣的子方向进行深耕。争取这个阶段能做到主程，再往下的规划可能要走一步看一步了。
4. 是否了解公司产品：我了解贵公司旗下的几款产品，大多数都是MMORPG类型的，例如《神火大陆》和《九灵神域》，像《神火大陆》在海外的表现还是非常不错的，登顶韩国Google Play畅销榜TOP3。然后我也在B站看过《神火大陆》的游玩视频，游戏画面还是非常不错的，玩法也很成熟，游戏世界观庞大，角色技能体系丰富，对客户端架构、表现力和性能优化要求都很高，这个游戏类型我也是比较感兴趣的。

## 3、项目经验类

### 项目独立/合作、最有成就感的项目、课程项目 vs 自主项目、遇到困难/收获

1. 项目的心得体会和收获：
	- 在这个 UE5 地牢风 RPG 项目里，我做的是一整套基于 GAS 的技能、属性和战斗系统，也支持多人联机。一开始我主要想搞清楚 UE 的客户端到底怎么和服务器协作，比如技能释放、数值同步这些。过程中最大的挑战是网络同步，因为不同玩家的动作、技能效果经常不同步。后来我深入研究了 UE 的 Replication 和 Prediction 机制，把伤害判定放到服务端，通过客户端预测减少延迟感，最终体验流畅了很多。
		- 另一个收获是对架构设计的理解。以前做项目只想着功能能跑，但这次我用了 MVC/MVVM 去组织 UI 和数据交互，让系统更清晰、扩展更方便。
		- 最后整个项目下来，我不仅熟悉了 UE5 的 C++ 框架，也学会了怎么在多人联机环境下保证逻辑一致性。这让我更理解“游戏客户端”不只是写逻辑，更是**系统设计和玩家体验的结合**，不能仅仅满足于功能的实现，还要关注持续运营项目的可扩展性。
	- 另一个项目是我自己从零写的简易光栅渲染器。做这个的初衷其实很简单——我想搞清楚 3D 图形到底是怎么从数学变成画面的。 所以我没用任何现成 API，从坐标变换、扫描线填充、深度测试，到光照、抗锯齿、光线追踪都自己实现了一遍。
		- 最大的收获是：以前只会调用引擎接口，现在真正理解了**渲染管线的原理**，也更清楚哪些计算最耗性能、该怎么优化。它让我在看 UE 的渲染模块时更有底层直觉。
2. 实习的心得体会和收获：我研究生期间没有实习经历，但我本科阶段有一段建筑设计的实习。
3. 学习方式：对于完全没接触过的技能，一般我会先在Youtub或者B站找那种相对系统的课程，先跟着学习一遍，然后做个小Demo巩固知识，当我遇到困难时会去询问AI，ChatGPT或者Claude等等，当我想深入某一方面的技术时，我会去CSDN和知乎找一些大神写的文章，然后结合着源码去理解，总结并做笔记，方便以后进行复习。

## 4、工作匹配类

### 能否接受加班、了解工作流程、进入公司后可能负责什么、会遇到什么困难、不足与改进

1. 我平时也会从网络上收集游戏行业的相关信息，我了解到游戏行业总体的加班状况还是比较严重，特别是临近游戏上线前，甚至后熬夜加班；我对此抱有一定的预期，并且我也接受合理的加班，比方说平时八九点下班，上线前偶尔熬夜。因为我认为在我的职业生涯前期也是我精力和身体比较好的阶段，更加适合奋斗和拼搏，相应的游戏行业的薪资相对于其他行业也是偏高的，所以说适当的加班也是合理的。
2. 我认为在刚入职的时候，遇到困难是很正常的事情，刚进入一个新环境，无论公司的工作流程还是使用的工具和代码的编码规范都是需要不断学习的；我认为比较好的处理方法是对于简单的问题，可以先自己主动思考，查阅相关的资料和文档、包括现在AI发展的也很好，很多基础的问题都能得到解答；对于一些难处理的问题，在我们有了一定的认知，可以向前辈主动请教，特别是当我们有了一定的认知后，提问也会更有针对性，也不会耽误前辈特别多时间，沟通的效率也是比较高的；其次做一些关键的决策前，即使可能比较简单，我们也可以向前辈进行确认，确保工作不会出大失误。

## 5、性格与团队类

### 团队沟通、喜欢什么样的人、不能接受什么性格、他人评价、MBTI性格

1. 对自己的性格评价：我觉得我是一个理性、情绪稳定、抗压能力强，理性和情绪稳定是别人对我的评价，我自己也这么认为，我平时很少会生气或者悲伤到流泪这种，即使和人有争执，也会保持冷静的尽力沟通。抗压能力强是因为我原来学建筑的时候，期中和期末的时候都要赶图，经常会熬夜画图，这个能力也算是练出来了，包括实习的时候，有几次也是干到凌晨，感觉都还好，另一方面是我平时会坚持体育锻炼，身体上也比较能抗。
2. 团队合作的项目：

## 6、意向与待遇类

### 是否有 offer、期望薪资、是否了解公司项目、能否实习、城市地点OK吗

## 7、反问

1. 想问一下贵公司是否有mentor，还有新人的培养机制是怎么样的
2. 面试的结果什么时候出？
3. 能不能提前来实习？
4. 新人一般做什么业务？



## C#回收机制

![](https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20260130194853209.png)



# 奥德赛

## 0、状态机

- 缺点：扩展状态时比较繁琐，在每个其他状态中都要检查一下是否需要更改，难免会有疏漏。

## 1、泛型状态机框架

- **泛型 CRTP（奇异递归模板模式）** 设计：
	1. 核心目的：是在基类中就能精准拿到子类的强类型实例，避免重复的类型转换，as/is这些，并在编译期就能提醒一些类型转换的错误。
	2. 写法上就是使用泛型约束，把模板参数限定为 “继承泛型基类的子类本身”。
	3. 与使用virtual的动态多态相比，自由度没那么高，无法使用一个通用的容器装不同的子类对象。
- 这个项目最核心的就是实体层和状态层的设计。

### 1.实体层

1. `EntityBase`（抽象）：最顶层是一个EntityBase的抽象基类，继承自MonoBehaviour，是所有实体的公共基类，存放所有实体共有的变量和属性，比如水平速度、垂直速度、是否在地面上这些，还有一些不依赖具体类型的函数，比如一些工具性质的函数。
2. `Entity<T>`（抽象，泛型）：第二层采用泛型的抽象基类Entity<T>，使用CRTP模式，限制泛型T继承自Entity<T> 自己。为每个具体实体提供强类型的通用逻辑，包括各个生命周期函数都写在里面。
3. `Player` / `Enemy`（具体实体）：第三层就是具体的实体，Player和Enemy，继承自泛型基类Entity<T>，实现player和enemy自身的逻辑。

### 2.状态层

1. `EntityState<T>`（抽象，泛型）：最顶层是一个采用泛型的抽象基类，他会限制泛型为Entity<T>，也就是Player或者Enmey。它里面最重要的是四个抽象方法，OnEnter、OnStep、OnExit、OnContact，分别代表进入状态、在状态中，离开状态和产生碰撞要做的事，所有的子类都要去实现。然后这四个函数上面还会再封装一层，用来记录一下状态进入的时间，并且分发一下事件，由状态管理器去调用。
2. `PlayerState` / `EnemyState`（抽象）：第二层就是PlayerState和EnemyState，这一层没干什么。
3. `IdlePlayerState` 等（具体状态）：在下面一层就是具体的状态了，Idle、Walk之类，需要去实现四个抽象函数。



### 3.状态管理层

- 有了状态层，当然需要一个东西去管理它，存储所有的状态，并管理状态的进入、离开、切换这些。

1. 状态管理层和实体层很类似，也是一个三层的结构，最顶层是抽象的基类，第二层是抽象的泛型基类，第三层是具体的状态管理器层，也就是PlayerStateManager和EnemyStateManager。
2. 最重要的是PlayerStateManager，它是如何管理玩家的状态的呢：
	- 它里面会储存一个public的字符串数组，我们可以在编辑器里输入状态的名字，并按一定的顺序排列。
	- 然后初始化的时候，会根据字符串列表通过反射机制，GetType传入字符串来得到对应状态的type，再通过System.Ativator.CreateInstance来实例化，并把所有的状态并存储在一个状态列表中，以便之后对状态进行管理、切换等等的操作，然后还可以加一个字典来加速对状态进行查找，还会存储当前状态为current，默认是状态列表的首位Idle待机状态。
	- 如何执行某一个状态的逻辑：泛型的实体类会持有这个相同泛型的状态管理器，实体类是继承自Mono的，所以在它的Update生命周期函数里会去调当前状态current的Step轮询函数，这样角色就可以一直处于某一个状态。每一帧都会去执行当前状态该执行的逻辑。
	- 如何切换状态：当前状态的Step轮询函数会在Update中每帧执行，step函数中每帧都会检测是否可以切换到其他状态，比如当玩家按下键盘的移动键时，PlayerInputManager中就可以得到玩家输入方向，这时候如果检测这个玩家输入方向的值大于0的话，Player就会让PlayerStateManager切换到Walk状态。这时就会执行老状态的Exit退出函数和新状态的Enter进入函数，然后分发一下状态退出、进入、改变的事件给外部。

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20260203113932923.png" style="zoom:50%;" />

#### 1. 核心角色与职责

- `EntityStateManager<T>`（状态管理器）
	- 作为状态机的 “大脑”，负责管理所有状态的注册、切换和执行。
	- 维护状态列表、当前 / 上一个状态的引用，并提供 `Change()` 系列方法用于状态切换。
	- 通过 `Step()` 驱动当前状态的逻辑更新，通过 `OnContact()` 转发碰撞事件。
- `EntityState<T>`（状态基类）
	- 作为所有具体状态（如 “Idle”“Run”“Attack”）的父类，定义状态的统一接口。
	- 包含 `OnEnter`/`OnExit`/`OnStep`/`OnContact` 四个核心生命周期方法，由具体状态子类实现。
	- 提供 `onEnter`/`onExit` 事件，允许外部代码监听状态的进入与退出。

#### 2. 架构设计意图

- **解耦状态逻辑（低耦合）**：
	1. 状态与管理器分离的设计，让新增状态无需修改状态机核心代码，符合 “开放封闭原则”。
	2. 每个状态的行为被封装在独立的 `EntityState<T>` 子类中，避免状态逻辑混杂在实体类里，符合 “单一职责原则”。
- **复用性（高内聚）**：
	1. `virtual` 提供通用默认逻辑，允许子类按需扩展，提升代码复用性。
	2. 泛型 `<T>` 让框架支持任意类型的实体（如 `Player`/`Enemy`），无需为不同实体重复编写状态机。
- **事件驱动扩展**：`onEnter`/`onExit` 事件和 `EntityStateManagerEvents` 让外部系统（如 UI、音效）可以监听状态变化，无需修改状态内部逻辑。
- **统一状态生命周期**：`abstract`强制所有状态实现标准的生命周期方法，保证状态切换时的逻辑一致性（如进入状态时初始化、退出时清理）。

------

### 二、`virtual` 与 `abstract` 的区别与选用逻辑

|   特性   |                       `virtual`                        |                  `abstract`                  |
| :------: | :----------------------------------------------------: | :------------------------------------------: |
| 实现要求 |                    必须提供默认实现                    |         不能提供实现，仅定义方法签名         |
| 子类行为 | 子类可以选择重写（`override`），也可以直接继承默认实现 | 子类必须强制重写（`override`），否则编译报错 |
| 适用场景 |        有通用默认逻辑，且允许子类按需扩展的方法        | 无通用默认逻辑，必须由子类实现核心行为的方法 |
| 类的限制 |                  可用于普通类或抽象类                  |            仅能用于抽象类或接口中            |

## 2、反射代替

### 一、反射的问题：

1. **性能开销大**：`Type.GetType()` 和 `Activator.CreateInstance()` 是运行时动态查找、创建对象，比直接 `new` 对象慢 10~100 倍。
2. **不支持代码优化**：编译器无法对反射代码做静态优化，且容易出现 “类型名拼写错误” 等运行时异常。
3. **调试困难**：反射创建的对象无法在编译时做类型检查，问题只能在运行时暴露。

如果状反射频率高，反射的性能开销会被放大，因此需要用更高效的方式替代。

------

### 二、常用的 “代替反射” 方案

#### 1. 工厂模式（最常用）

- **核心逻辑**：预先注册 “状态类型名 → 创建状态实例的委托”，后续通过委托直接创建实例，避免反射。

#### 2. 字典缓存（兼容原有反射逻辑）

- **核心逻辑**：第一次用反射创建实例后，将实例缓存到字典中，后续直接复用缓存，避免重复反射。

- **优势**：兼容原有 “通过类型名创建” 的逻辑，同时避免重复反射的性能开销。

------

#### 3. 枚举映射（最安全）

- **核心逻辑**：用枚举代替字符串类型名，编译时就能检查类型正确性，彻底避免反射和运行时错误。
- **优势**：编译时类型检查，无反射、无字符串硬编码，性能最高、最安全。

------

### 三、选用建议

|   方案   |          性能          | 安全性 | 兼容性 |              适用场景              |
| :------: | :--------------------: | :----: | :----: | :--------------------------------: |
| 工厂模式 |           高           |   中   |   高   | 需要兼容 “字符串类型名” 创建的场景 |
| 字典缓存 | 中（首次反射，后续高） |   中   |  最高  |  原有代码依赖反射，需最小改动优化  |
| 枚举映射 |          最高          |  最高  |   低   |    状态固定、无需动态扩展的场景    |

## 3、PlayerInputManager（游戏输入）

1. 新输入系统InputSystem相比于老输入系统：
2. 首先用一个文件InputActionAssert来记录“动作”和不同设备输入按键的映射，PlayerInputManager获取到这个类对象。InputActionAssert有点像字典，可以通过“动作名称”来获取到相应动作的InputAction，最后再ReadValue就可以得到输入的值。（有些时候可能会限制一下死区，然后做一下重映射。）

## 3、数据Stats

- 数据层面采用的是把数据和管理逻辑分开的策略，Stats作为单纯的数据，用一个StatsManager去管理它。Player和Enemy是走两套配置的。
	1. Stats数据层面有两层，最顶层是抽象的泛型基类EntityStats，继承ScriptableObject，并限制泛型也为ScriptableObject，然后具体的PlayerStats去继承EntityStats，所有的涉及到玩家手感、能力的数据都存在这，比如重力大小、摩檫力大小等等。一开始继承`ScriptableObject` 是一种**不依赖场景、独立存在的数据容器**，不用挂载到GameObject上就能再多个场景中共享数据。像策划可以在直接在Inspector面板上统一的去挑整各个参数，而不用修改代码。使用的时候要加一个CreateAssertMenu的特性，然后才能在编辑器里进行资产的创建，然后可以配置多分资产。
	2. StatsManager逻辑层面也有两层，最顶层也是抽象+泛型的基类EntityStatsManager，他继承自Mono，限制泛型为EntityStats，最底层PayerStatsManager继承它，就只能管理Player的Stats。因为它继承自Mono，所以最后会挂载到角色对象身上，并被Player所持有，然后用来管理PlayerStats。比如我们可以配置不同难度下的PlayerStats资产，根据玩家选择难度的不同，使用不同的配置。

## 3、角色控制器CharacterController

`CharacterController` 是 Unity 提供的**专门用于控制角色移动的组件**，核心定位是 “替代 Rigidbody 做人形角色（如玩家）的移动控制”，有了它就不需要刚体了。

1. 它不会受物理引擎的重力，要我们自己写，移动是通过Move方法来控制的。
2. 它自带胶囊体碰撞器，能检测与场景中 Collider 的碰撞，并自动阻止角色穿模；
3. 他还有一些专为角色设计参数，比如设置 `stepOffset` 让角色能上台阶，`slopeLimit` 限制角色无法爬上过陡的斜坡；



## 3、相机

### 第三人称视角相机

相机系统我采用的方案是Unity官方提供的Cinemachine插件来实现的，在选择方案的初期，我确定的需求就是要实现市面上常见的第三人称相机的功能，一是要以玩家为中心，玩家移动相机也要跟着移动，二是通过鼠标输入，围绕着玩家观察四周的环境。相比于自己实现一套摄像机逻辑，Cinemachine可以说是开箱即用，只用在初始化的时候设置Follow的对象和LookAt的对象就能实现摄像机跟随玩家并看向玩家的功能。最后在每帧的LateUpdate去更新相机目标位置就行了。

- 为啥相机更新在LateUpdate：所有游戏对象的 `Update` 会在同一帧内无序执行，比如玩家移动的 `Update` 和相机的 `Update` 谁先谁后不确定，而`LateUpdate`会**等所有对象的 `Update` 执行完之后才会执行**，能拿到本帧所有逻辑更新后的最终状态，避免画面和玩家状态不同步导致的抖动。

初始化时LookAt设置为玩家是没问题的，然后Follow设置为player的transform就不太行，因为玩家来回蹲起的化就会造成摄像机的抖动，死区的话也不太好设置，有点太大了，于是我就在初始化的时候在player的位置new了一个对象作为Follow，就能很好的解决上述问题。

具体实现上，主要分为三个函数实现：

1. 第一个函数实现当玩家有输入时，比如鼠标移动和摇杆转动视角时，会改变相机的偏航角Yaw和俯仰角Pitch，需要注意的是鼠标输入时即时的，而手柄是按帧累积的，往一个方向推着摇杆就会一直往那个方向转，所以分开处理，手柄要乘上一个deltatime。Pitch计算的时候要乘上-1，才会符合玩家直觉。
2. 第二个函数是处理相机跟随的旋转的，如果相机只是简单跟随玩家的话，玩家向左就向左，玩家向右就向右，可能会有一点僵硬。这个函数就是计算玩家的水平速度，再转换到相机的本地坐标系中，来改变相机的偏航角Yaw。这样就能实现玩家水平移动时，相机跟随着玩家平滑转动。
3. 第三个函数是处理相机的高度偏移的，通过设置死区来保证相机的稳定，防止抖动。空中和地面的死区和相机的移动速度可以分别设置，达到不同的效果，在空中保持一定的延迟感，让画面更加自然。

### 相机的障碍物遮挡问题

还有一个问题是相机跟随玩家时，中间有障碍物的话会遮挡玩家的问题：

1. Cinemachine本身有一个Mask Layer来判断相机的碰撞，把游戏场景中可能会遮挡的物体设置为指定的Mask Layer，就可以确保不会对相机进行遮挡。
2. 而我在UE5的RPG项目里，采用的是2.5D的视角，采用的相机遮挡的解决方案有点不一样。player的蓝图里有一个相机和一个springArm弹簧臂来和玩家间保持一定的距离。相机离玩家的距离是固定的，不想让相机再产生遮挡时移动到遮挡物前，产生视角突变。我是通过在玩家和相机之间创建了一个碰撞体来进行碰撞检测。对于场景中的物体来说，比如一些高塔和城墙，我创建了一个可以（隐藏/溶解）显示的材质，和一个可被隐藏的物体的蓝图基类。这个溶解材质我使用的是UE里提供的一个抖动TAA的蓝图节点，可以某种程度上设置材质的透明度，当然也可以用一些菲涅尔效果来实现溶解材质。当继承该基类的物体和摄像机碰撞体产生碰撞时，物体会根据当前的材质创建一个动态材质，并根据一个可以自行设置透明度的参数来设置该动态材质，并赋予给物体。当离开碰撞体时，再把材质还原回去。
3. 另一种思路：[unity urp 实现遮挡显示角色轮廓](https://blog.csdn.net/qq_30100043/article/details/126234436)思路就是获取深度，使用模版测试，获取到被遮挡的区域再重新绘制

## 3、动画

- 动画主要是通过Unity里Animator动画控制器来实现的，每个动画都相当于一个状态机，彼此之间会有切换的条件，是通过设置Parameter的值来进行控制的，当满足一定条件后，Animator会自动帮我们切换动画，所以在代码中我们是通过在LateUpdate中每帧去改变Parameter的值来播放不同的动画。Parameter的名称都是一个string字符串，我们可以在代码中通过这个string字符串来设置Parameter的值，我们这里最重要的一个Parameter就是PlayerState，他代表PlayerState的ID，和在PlayerStateManager里设置的顺序一样，大部分动画的切换只要改变它就行。这里的动画不单单由PlayerState决定，还由其他一些参数共同决定，比如跳跃动画和下落动画都是处于FallPlayerState状态时进行播放的，他还会根据速度是否大于0，来决定播放跳跃还是下落动画。像还有些参数可以用来控制动画的播放速度，比如有个参数就对应玩家的水平速度，会根据这个参数来控制行走动画的播放速度，速度越快，走路动画播放的越快。
- 小优化：可以把Parameter名称的string字符串先转换成hash，然后把它存成成员变量，再使用这个hash去和Animator进行参数的同步，这样查找更快。因为底层本来就会先把字符串转成hash再同步，一开始就记录下来的话就不用每次参数同步都计算一遍各个参数的hash了。

并且还会做动画的混合。

- 多Layer：Layer分成两层，一个Base，一个上半身，大部分动画都在Base里，当我们拿起物体的时候，上下半身的动画是分离的，上半身就不怎么动，下半身可能走也可能跳，通过不同的Layer做一个融合。

## 4、行走/疾走（Walk/Run）

- 玩家最核心的参数就是它的速度，整个项目物理的核心同时通过改变玩家的速度来实现的。这个速度又可以被才分为水平速度和垂直速度，这两个属性在get的时候都是去拿玩家原始速度的分量，set的时候也是去设置原始速度。
- 当检测到水平速度不为0的时候，就会从Idle待机状态切换到行走状态。
- Walk的OnStep轮询函数中，会先通过PlayerInputManager来获取到玩家的移动方向，再通过计算相机的水平旋转得到一个四元数，去旋转玩家的移动方向，使得玩家前进的方向为相机看向的方向，更符合人体直觉。
- 状态转换：移动方向有了之后，会与当前玩家得水平速度做一个点积，如果点积大于0，就说明玩家当前帧的前进方向与前一帧相近，就会做加速和平滑转向；如果点积小于0的话，就说明玩家当前帧的前进方向与前一帧相反，就要进入刹车状态。
- 疾走：有一个变量会去存储玩家是否在Run，玩家按下疾走键的时候会变成true，然后会改变传入加速函数中的加速度、转向阻力、最大速度，所以操作手感会不一样，动画也会因为速度超过了阈值而变为奔跑动画。

## 4、刹车（brake）

- **减速**：刹车状态下，简单的使用MoveTowards把速度降为0，刹车系数在PlayerStats里去设置就行。
- 当速度降为0时，切换成Idle。

## 5、加速、转向、重力（可二次封装，不同状态下设置不同的参数以调整手感）

- **加速**：采用的是基于物理公式的做法，一开是我们会有两个速度，一个是玩家输入想要去移动的方向，一个是角色当前点水平速度，这两个速度一般来说方向是不一样的，我们想要实现的就是向着我们期望的方向进行移动，但又不想这一开始往一个方向移动，突然又往另一个方向移动，希望有点过渡。所以采用的做法是将角色的水平速度拆分成两个向量，一个平行于移动方向的向量，一个垂直于移动方向的向量。这个垂直的向量，我们肯定希望它越来越小，所有使用moveToward使它慢慢变为0向量。这个平行向量相当于水平速度在希望移动方向上的速度分量，我们可以用速度与加速度的公式来计算，ΔV=at来计算，相当每过一帧，速度+=加速度*deltaTime，最后在限制一下最大速度。最后再把平行和垂直的两个速度合起来，就是玩家最终的速度。然后在Update的时候通过最终速度计算玩家的位移，x=Vt（这里因为玩家操控用的是CharacterController，所以直接Move）
- **平滑转向**：除了处理加速，我们还要处理角色的平滑转向，就是想让角色现有朝向变为我们输入的目标方向。做法就是使用RotateTowards来在两个旋转上做个线性插值。

- **重力**：当玩家不在地面上时，会在OnStep函数中每帧给玩家施加重力，重力的公式是Δv=gt，根据公式来计算玩家的垂直速度，这里面加速度g可以自行设置，在上升和下降的过程中可以设置为不同的值，来达到更好的手感。

## 5、跳跃优化

- 跳跃的实现：在所有允许跳跃的状态中的step函数中写一个函数Jump，每帧检测是否跳跃，jump函数中先判断是否可以跳跃，比如角色是否在地面isGrounded，是否还有跳跃次数，是否持有物品等，如果可以跳跃，再去检测跳跃键时候按下，如果按下了就以最大高度进行跳跃。然后切换状态到Fall掉落状态，最后在触发跳跃事件，通知监听该事件的监听者们。
- 跳跃的小优化：
	1. 支持多段跳，可以在PlayerStas这个类中来配置最大跳跃的次数，player记录当前的跳跃次数，与最大次数做对比来判断是否可以跳跃。
	2. 支持土狼跳，当player离开地面的一小段时间内，都允许玩家继续跳跃，因为玩家从看到屏幕到按下按键以及反应都需要时间，允许土狼跳会有更好的跳跃手感，符合玩家直觉。做法就用一个变量来记录上一次的在地时间，与当前时间做差，只要小于自己配置的土狼时间就允许跳跃。
	3. 支持跳跃缓冲，它相当于可以记录玩家的跳跃操作，然后延迟执行，和土狼跳有点类似，都是为了提升玩家手感，符合玩家直觉。具体做法是，使用一个变量来记录上一次跳跃的时间，只要我们摁着空格键，他就会在update里更新lastJumpTime，进行跳跃检测时，如果当前时间减去lastJumpTime小于跳跃缓冲阈值就还可以跳跃。
	4. 长按长跳短按短跳，根据玩家释放跳跃键的时机和当前速度进行判断，当玩家释放跳跃键的那一帧去检测玩家此时的速度，如果大于某个值就强制将速度变小到一个固定值，因为当我们按下跳跃键时我们会给player施加一个最大速度，它会随着重力的影响慢慢变小，所以说如果玩家速度很大话证明它才刚起跳，这时松开跳跃键将它的速度限定到一个较小的值就可以实现长按长跳短按短跳。

## 5、冲刺Dash / 俯冲AirDive / 下砸Stomp / 旋转攻击Spin

- 实现的功能是玩家按下冲刺键，角色就会向前冲刺，进入到冲刺状态
- 具体实现：
	1. 封装一个冲刺的接口，在每一个可以切换到冲刺状态的状态的OnStep轮询函数中添加这个接口，每一帧都去检测是否冲刺。
	2. 这个接口进入后，会先根据时候在地面、允许的冲刺次数等等来判断是否允许冲刺，允许冲刺并按下冲刺键的话，玩家就会进入冲刺状态。

- 状态内部：
	1. **冲刺状态**内部，刚进入的时候OnEnter函数里会给玩家实施加一个瞬时的水平速度，并把玩家的垂直速度限制为0，然后分发“进入冲刺状态”的事件给外部，用来处理一些特效和音效等等；OnStep中回去检测玩家进入该状态的时间，达到设置的值后去切换状态，如果在地面会切换到Idle，在空中的话就会切换到Fall；退出的时候OnExit函数会去限制一下玩家最大水平速度，然后分发一下“退出冲刺”的事件。
	2. **下砸状态**总共分为两个阶段：第一个阶段会停滞在空中一小段时间，播放滞空动画，然后再下砸，碰到地面后会施加一个小小的向上的速度作为反弹力，然后切换到下落状态。两个阶段结束的时候分别都会分发时间给外部。
	3. **旋转攻击状态**内部OnStep函数就是通过计时来进行状态切换的，时间一到，在空中就切换为Fall状态，在地面就切换为Idle状态。

## 6、下落（Fall） / 地面检测（帧驱动，灵活但不好调试）

- 不在地面就会切换成Fall状态，是每个状态都每帧检测的。

- 地面检测的实现：从角色中心向下进行球型的射线检测，距离为角色半高+一个微小的偏移量，根据时候产生碰撞和前一帧的isGround是否在地面，可以分为三种状态，分别是进入地面EnterGround、离开地面ExitGround、持续在地面UpdateGround，如果没有产生碰撞，说明离开地面，执行ExitGround；如果有碰撞且前一帧不在地面，那么说明进入地面，执行EnterGround；否则前一帧也在地面，执行UpdateGround函数。
	1. 进入地面时，就会更改isGround为true，然后分发一下角色的“进入地面”的事件，比如说播放落地动画还有一些音效啥的，还要重置一下跳跃。
	2. 离开地面时，就会更改isGround为false，然后分发一下角色的“离开地面”的事件。
	3. 持续在地面时，就会记录碰撞到地面的RaycastHit信息，然后计算地面坡度、法线等等信息，用于其他的计算，比如脚底下是什么材质，根据不同的材质发出不同的脚步声。这也可以实现移动平台，就去检测碰撞的地面是否带有某个Tag，然后将角色设置为平台的子物体，这样角色就可以跟着平台移动了。然后在离开地面的时候就可以解除这种父子关系。



## 5、攻击碰撞检测（HitBox）/可破坏物（Breakable）

我这个游戏攻击手段不算特别多，总共就三种攻击方式：

1. 旋转攻击，玩家主动按下攻击键，进入旋转攻击状态进行攻击。
2. 踩踏攻击，当玩家踩到怪物头上时，对他进行攻击。
3. 下砸攻击，玩家在空中按下下砸键，进入下砸状态，造成大范围攻击。

这三种攻击都是通过碰撞检测来进行，角色的GameObject下面会挂三个碰撞盒，大小和位置都不同，下砸的范围会大一些，在脚底，而旋转攻击就在角色手部那个高度。

- HitBox：碰撞盒身上会去写一个HitBox的脚本，在OnTriggerEnter里去处理碰撞发生时要进行什么样的逻辑处理。【触发的核心前提（必须满足，否则不触发）：
	1. 至少一个对象的 Collider 勾选了 `Is Trigger`（触发器模式）；
	2. 另一个对象必须带有 **Rigidbody**（即使是 Kinematic 刚体也可以，PlayerController也行）；
	3. 两个对象的 Layer 不在碰撞矩阵中被排除（Edit → Project Settings → Physics）。】
- 可以发生HitBox碰撞的物体总共有两种：
	1. 一个是Enemy，它是继承EntityBase的，当我们他与我们的碰撞体产生碰撞时，就会执行三个函数，首先是对物体造成伤害，然后是当我们踩到怪物时，产生一个垂直向上反弹，最后是产生一个攻击方向的反方向的后坐力。但是这三个效果并不是每种攻击都有的，所以我们会暴露一些参数出去，比如是否造成伤害，伤害是多少，是否会有向上的反弹，是否会有后坐力这些；然后不同的碰撞盒检测的攻击方式也不同，就可以自己去配置不同的设置。
		- 碰撞检测时机的问题：像我们踩怪物这个攻击的碰撞检测是时刻都在进行的，而我们的旋转攻击和下砸攻击都是当我们按下按键的时候才回去进行碰撞检测，平时碰撞盒都是disable的。所以这是通过挂载一个PalyerEventListener，去监听玩家的状态，玩家进入状态时才把碰撞盒打开，退出状态时就把碰撞盒子disable。想玩家的事件可以分装成一个PlayerEvent统一管理，然后PalyerEventListener本地存一个public的实例，在初始化的时候将 Player 内部的事件（如跳跃、受伤、死亡等）转发到本地的 events 中，这样就可以方便在 Inspector 中自定义回调函数。
	2. 一个是**可破坏物Breakable**，比如木箱，打破了会掉金币，写一个Breakable脚本去处理，对外暴露一个Break接口。内部的核心逻辑就是停止物理模拟、禁用碰撞器、隐藏模型、播放音效，分发“破环事件”。

- UnityEvent 的 **Inspector 绑定**和**代码 AddListener** 两者都是 绑定回调的方式，核心区别是‘静态配置’和‘动态控制：
	1.  Inspector 绑定是编辑时固定的，适合无逻辑判断的固定回调（比如打碎木箱生成金币），基础逻辑（如木箱破碎播放音效、生成基础道具）用 Inspector 绑定，方便策划调整；
	2. 代码 AddListener 是运行时动态的，适合带条件判断的灵活逻辑（比如根据玩家状态调整加分），动态逻辑（如根据玩家装备 / 游戏难度调整加分、触发特效）用代码 AddListener，保证灵活性；同时注意代码绑定后，在 OnDestroy 中移除监听，避免内存泄漏。



## 6、角色重叠（碰撞）检测

角色的重叠（碰撞）检测是每帧都会进行的，在基类的Update函数中会去封装一个函数每帧调用。

1. 在这个函数内部，我们肯定要先去获**取与角色产生重叠（碰撞）的物体**，在player中会去持有一个碰撞体数组，然后每帧都会去进行一个角色胶囊体大小的范围检测，也就是OverlayCapsuleNonAlloc（NoGC版本），然后碰撞的的结果会存在之前的碰撞体数组中，采用这种NoGC的方法可以避免每帧都去创建一个新的数组。
2. **处理角色的碰撞逻辑**：然后因为每个继承PlayerState的具体状态都会实现一个OnContact抽象方法，来去处理角色重叠时的逻辑，比如我们在飞翔或下落状态时，角色与墙体产生了重叠，就会进入蹬墙跳状态，就是通过每帧调用OnContact来处理的。所以我们会遍历碰撞数组中的所有元素，通过Player身上的PlayerStateManager状态管理器去调用当前状态的OnContact函数，并传入碰撞体，不同状态会有不同的处理。
3. **处理对方的碰撞逻辑**：碰撞是双向的，我们除了要处理角色碰撞时的逻辑，还要去看看被碰撞的物体。我们去检测一下他是否实现了我们自定义的一个用于进行碰撞处理的接口，如果实现了，就执行它的碰撞回调。比如陷阱、可拾取物、弹簧等等物体都会在与玩家产生碰撞时有自己的逻辑，就让它继承接口，强制实现碰撞逻辑。

- 推箱子：在可以对刚体产生物理推动的状态的OnContact函数中，封装一个函数用于对刚体施加力的作用，与物体产生碰撞时，检测他是否为刚体，是的话，施加一个力，物体在根据自己的刚体质量mass来计算一个速度。



## 20、抓取 / 投掷 / 可抓取物（Pickable）

1. 玩家具有抓取物体并将其投掷出去的能力，只要该物体上带有可抓取物（Pickable）的脚本。玩家抓取着一个物体时，玩家并不会切换状态，但玩家上半身会播放抓取物体的动画。
2. 抓取 / 投掷 ：在能够进行抓取和投掷的状态中，比如行走、待机和下落（也可设置为空中无法抓取）的OnStep函数中，会去封装一个函数，用来每帧去检测是否进行了抓取 / 投掷。如果按下抓取键，会去判断手里是否有物体，如果有则进行投掷，如果没有就会在前方做一个射线检测，如果检测到的物体是可抓取物（Pickable）时，则会执行抓取操作。抓取函数内部会去标记当前已抓取、记录抓取的物品，并把物品的位置设在手部的一个slot上，然后发送抓取物体的事件。投掷函数内部会标记当前未抓取，清空物体的引用，发送投掷物体的事件，并根据玩家的水平速度向正前方投射出去，如果命中怪物，会对其造成伤害。
3. 可抓取物（Pickable）：它会在被抓取时，把自己的parent设为玩家手部插槽，重置局部位置和旋转，关闭物理、设置为Trigger，发送被抓取事件，投掷时会脱离玩家父物体，恢复物理，发送事件，最后还会以特定速度投掷出去。它会继承一个用于碰撞的接口，再产生碰撞时会检测他撞到的是不是enemy，是的会回对enemy造成伤害。



## 7、受伤（Hurt）/陷阱

- Player 类持有一个 Health 类型的实例，负责玩家生命值的所有管理逻辑，比如当前血、最大血、扣血回血这些、还有受伤后的无敌时间，还有血量改变时分发的事件，来去通知UI。
- 我这个项目受到伤害的途径也比较少，主要就是一些陷阱和只会移动的怪物，当陷阱与玩家产生碰撞时陷阱就会对玩家施加伤害。
- 如果此时玩家不处于无敌状态，玩家就会减少HP，并且根据碰撞点和玩家的位子计算一个受击方向，然后面向这个方向向后添加一个击退速度。并且将状态转换为受伤状态，并分发角色的“受伤”事件。如果血量降为0了，则玩家进入死亡状态，最后分发角色的“死亡”事件。

## 8、下蹲（Crouch）

- 角色的Idle、Walk、Run状态都可以切换到下蹲Crouch状态，每帧都去检测是否按下了下蹲键，是的话就切换到下蹲状态。
- OnEnter函数，即进入到下蹲状态时会改变角色的碰撞体的高度；OnExit函数，即退出下蹲状态时会恢复碰撞体高度。OnStep函数中会改变记录下角色的移动方向，并施加一定阻力，这个阻力是要小于Walk状态的阻力的，所以当我们下蹲时更能保持惯性，停止移动后也会滑行一小段距离。
- 状态改变：
	1. 如果在下蹲状态下，速度降到0的话，就会切换到爬行状态；
	2. 如果在下蹲状态下，按下跳跃键的话，就会执行后空翻；
	3. 如果停止按下下蹲键，就会切换到Idle待机状态。



## 9、爬行（Crawling）

- 相同点：爬行状态和下蹲状态很像，在进入状态和出状态时都会改变碰撞体的高度，这里设置的和蹲伏一致；如果停止按下爬行键，就会切换到Idle待机状态。
- 不同点：下蹲状态的时候不能主动移动加速，而爬行时可以移动，还有就是动画不同。爬行状态的的移动速度和转向速度都是可以单独设置的，比walk要慢。



## 10、后空翻（Backflip）

- 在下蹲状态和刹车状态时按下跳跃键才能进行后空翻。
- 执行后空翻的时候：
	1. 先要检测一下是否可以后空翻，比如拿着物品就不能进行后空翻。
	2. 然后施加一个向上的速度和一个向后的速度，这样就会产生类似抛物线的轨迹，值是可以自己配置的，可以到player的stas统计数据管理器里去拿。
	3. 然后进入后空翻状态，分发角色的后空翻事件。
- OnEnter进入后空翻状态：
	1. 会设置跳跃次数，分发一下角色的跳跃事件。
	2. 禁止移动一段时间，就是在空中不能改变移动方向。
- OnStep处于后空翻状态时：
	1. 应用自定义的重力，和其他状态应用的重力不太一样，模拟不同的重力效果。
	2. 整个后空翻分为三个阶段，一开始上升，然后是下降，最后是落地。一开始上升阶段是不能进行状态切换的，当速度的y值小于0时，就可以切换成其他状态，比如滑翔、下砸还有旋转攻击。最后当落地isGrounded为true时，切换成Idle，水平速度设为0.



## 11、多线程和协程

1. **多线程Thread：**unity支持多线程，但有几点需要注意，一是开启多线程后要注意在OnDestroy的时候终止线程，否则当你运行完游戏再停止后，线程还会继续执行，可能产生不必要的错误；第二点是副线程无法访问unity相关对象的内容的，只有主线程可以访问，副线程访问时会报错，所以一般都会用副线程来处理一些计算复杂但不需要访问场景物体的问题，比如一般会开一个副线程去计算A*的寻路算法，或者处理网络的收发消息，然后把计算完的结果或者消息放到一个共享的内存空间（可以声明一个公共容器）中，主线程再去共享内存（公共容器）里去拿。
2. 协程：协同程序，它是假的多线程，不是真的多线程，它的主要作用就是将代码分时执行，不去卡主线程，简单理解就是把可能会让主线程卡顿的耗时逻辑分时分步的去执行，主要的使用场景比如异步加载场景、文件，异步下载、异步的批量创建。
	- 继承自MonoBehavior的脚本都可以使用协程Coroutine。
	- 语法上协程函数一定是要返回IEnumrator迭代器接口的，然后使用yield return语法糖进行时序上的分割（继承IEnumrator迭代器接口后，需要实现Current属性，MoveNext函数和Reset重置函数，yield return返回的值会用Current去接收，这个语法糖相当于让编译器帮我们实现了这三个东西，简化了代码），return的内容主要用来限定什么时候执行接下来的内容。然后通过StartCoroutine函数和StopCorotine函数来执行和关闭协程。（Coroutine coroutineHandle = StartCoroutine(IEnumrator对象);StopCorotine最好传入句柄去停止）
		- yield return null：下一帧执行；
		- yield return new WaitForSeconds（）:等几秒后在Update和LateUpdate间执行。
		- 。。。
	- 当物体或者组件销毁时，协程会停止；当物体失活时也会停止，组件失活时不会停止。
3. 协程调度器：协程本身是一个C#中的迭代器，Unity定义了协程调度器，它有一系列规则，根据yiled return返回的值，来决定何时执行协程内的代码。当我们使用StartCoroutine函数，并把协程对象传进入时，就相当于让Unity的协程调度器去管理协程。
4. 多线程和协程的区别：多线程相当于新开了一个管道，和主线程并行执行；协程是在主线程上开启的，将复杂任务分为多步去计算，每次循环都是等满足条件才去执行下一步，也就做到了分时，这里分多少步和等待条件都是可以自己去设置的。

<img src="https://tuchuanglpa.oss-cn-beijing.aliyuncs.com/tuchuanglpa/20260225140923259.png" style="zoom:50%;" />

## 11、悬挂（Hanging）&攀爬（Climbing）

- **悬挂状态**：实现的功能是当处于Fall下落状态或者Backfilp后空翻状态时，按下悬挂键，并且有悬崖的话就会实现悬挂，并且可以在悬崖边缘左右移动。

	- 悬挂检测：封装一个检测悬挂的函数，下落状态和后空翻状态的Onstep中去每帧检测是否可以悬挂。悬挂函数内部会先去检测一下是否满足悬挂条件，比如是否垂直速度小于0，角色是否拿着物体，等等一些条件，还有最主要的是三个射线检测：

		1. 第一个射线是从角色中心位置向上打一个距离，这个距离要超过角色头顶，再加上一个小偏移量，检测角色头顶是否有顶，有顶也不能悬挂；
		2. 第二个射线是从角色胶囊体头顶+一个偏移量的那个点往前打一段距离，这个距离要超过角色胶囊体的半径+一个小偏移量，用来检测手要去扒拉的那个地方是否有墙，如果是墙，那肯定不能悬挂；
		3. 第三个射线是从角色手扒拉的那个点出发往下打一个小距离，检测一下是否有悬崖，是空气的话，也肯定不能悬挂。

		- 值得注意的是，在进行这三个射线检测的时候，还要去看看它们的LayerMask，特别是向下检测是否是悬崖的时候，检测的是我们专门为悬崖配置的一个Layer值，它使用类似位图的形式来保存，可以代表多个层级，我们要自己去配置，并且要给模型设置好Layer。
		- 然后如果检测成功，就设置角色的位置，把它吸附到悬挂位置，并把速度设为0，然后切换到悬挂状态，分发进入“悬挂状态”事件。

	- 悬挂状态内部：

		- 进入悬挂状态时：肯定要把跳跃、冲刺、攻击的次数重置一下。
		- 悬挂状态内部：需要去支持在悬崖边缘左右移动。首先和之前类似的还是回去做两个射线检测去确保处于悬崖边缘，然后去获取玩家的输入方向，如果是往右边，就从右肩膀边缘再往外面一点向前做一个射线检测，检测成功的话就把玩家速度加上一个向右的值，否则不能向右移动，向左也同理。如果输入方向是向前的话，就会检测时候允许攀爬，进而决定是否进入攀爬状态。除此之外，如果按下了跳跃键或者释放键就会进入下落fall状态。

- **攀爬状态**：只有当玩家处于悬挂状态时，按下前进键，才回去进行攀爬检测。

	- 攀爬检测：主要看产生碰撞的悬崖边缘是否MaskLayer是否与攀爬的layers匹配，还有就是悬崖上方的位置空间时候那能容纳下角色的胶囊体。
	- 使用**协程**完成攀爬位置的分帧插值：攀爬主要分为两个阶段，第一个阶段是玩家垂直位置的改变，第二个阶段是水平位置的改变。这里主要使用协程来进行分帧的插值处理，来实现丝滑视觉效果；在进入攀爬状态时，开启协程，退出攀爬状态时结束协程。协程内部先计算初始位置和最终的目标位置，然后分时的去执行Lerp，完成第一阶段再去完成第二阶段的位移。攀爬完成后切换到Idle状态。
	- 协程优点：
		1. 适合实现 “分帧执行”的逻辑，如果写在Update 中代码可能会有点混乱；
		2. 逻辑内聚，所有相关代码封装在一个方法里，易维护。



## 12、游泳（Swimming）

- 功能：游泳实现的功能就是当玩家碰撞体和水体产生碰撞时，就进入游泳状态；不再产生碰撞时就进入其他状态。
- 实现：玩家的OnTriggerStay函数中去检测与玩家产生碰撞的物体是否是水体，具体就是判断它的Tag是不是我们设置的水体，如果是水体，就要去检测我们是不是第一次进入水中，是的话，就要去放下持有物，然后切换到游泳状态，并分发一下进入“游泳”状态的事件；如果不是第一次进入水中的话，我们要看看我们是否离开了水面，去判断角色的中心点是否离开了水体的碰撞盒。
- 游泳状态内部：刚进入游泳状态时，OnEnter函数中会去衰减一下我们玩家的速度，模拟水中的阻尼感。在游泳状态时，OnStep函数中会先去改变一下玩家加速、转向的手感，没有输入的话会去简述，都是去模拟玩家在水中的阻尼感。玩家按下潜水键，就会向下加速，在这期间只要玩家处于水体中，都会受到一个向上的浮力，所以玩家如果下潜到一半不继续按键的话就会上浮。当玩家到了运动到边界时，比如水底或者刚好到水面上时，就会限制玩家的垂直速度为0。这时只有玩家跳跃离开水面时，才能离开游泳状态。
- Volume：与玩家碰撞的水体，除了要把Tag设置为水体，还要去挂一个Volume脚本，用来在玩家进入水面和离开水面时播放音效，也可以分发相应的事件。



## 13、滑翔（Gliding）

- 功能：滑翔实现的功能是在下落状态或者后空翻状态时，按下滑翔键，打开滑翔伞，然后进入滑翔状态。
- 实现：在玩家下落状态或者后空翻状态的OnStep中去封装一个函数，检测是否能进入滑翔状态，比如时候按下滑翔键、是否垂直速度小于0，进而判断是否进入滑翔状态。
- 滑翔状态内部：OnEnter进入滑翔状态时，会把垂直速度暂时设为0，停止快速的下落，然后分发“进入滑翔”状态的事件；OnExit退出时，分发“退出滑翔”状态事件，外部的滑翔伞去监听这个事件，用来播放开伞、收伞的声音、特效和动画。OnStep处于滑翔状态的时候，会去二次封装玩家的加速、转向、重力这些函数，传入滑翔状态时的参数，用来区分不同状态下的手感。
- Glider：滑翔伞物体会去挂一个脚本Glider，用来监听玩家进入开伞和退出开伞的事件，播放指定的音效和特效，开伞、收伞的动画是用协程分帧做的，一开始把伞的tranform设为0，开伞时把伞的transform从0到1做一个lerp，就会有伞慢慢打开的效果，比直接设置Active要丝滑一点。



## 14、蹬墙跳（WallDrag）

- 功能：蹬墙跳实现的功能是，当我们在下落、飞翔等不在地面的状态时，我们如果碰到了墙体，我们会扒着墙缓慢的下落，在此期间如果我们按下跳跃键，会执行蹬墙跳，然后进入下落状态。如果有两堵墙离得很近，我们就可以在两个墙之间进行来回的蹬墙跳。
- 实现：蹬墙跳的代码实现是，在那些可以转换成蹬墙跳状态的状态的OnContact碰撞函数中，封装一个函数去每帧的检测是否发生碰撞，该函数内部会先去检测是否能够切换到蹬墙跳状态，比如检测速度是否小于0（这代表着角色在下落）、还比如角色是否拿着东西、碰撞体的Tag是否是墙或平台等等，然后设置一下父对象（如果是平台的话，把它设置为父对象，可以实现跟随平台进行移动）、记录一下墙体的法线方向用于之后蹬墙跳的水平方向，最后切换到蹬墙跳状态。
- 状态内部：OnEnter进入时会先设置玩家的皮肤偏移，防止穿模，这需要我们自己设置一个合适的值，然后还要设置我们角色的朝向为墙面的法线方向的水平投影，为后面蹬墙跳做准备，最后还要重置一下跳跃、冲刺、攻击的次数。OnExit退出状态时要恢复皮肤偏移，把父对象置空。OnStep轮询时会先把垂直速度设为一个向下的值，如果按下跳跃键的话，会给玩家施加一个面朝向的水平速度和一个向上的速度，然后切换到下落状态。如果每按键的话，会去检测玩家是否在地面、是否与墙体还有碰撞，否则会切换到Idle状态。



## 15、关卡

1. 关卡启动器：LevelStarter继承自泛型单例，他会持有关卡Level的实例，由他来进行关卡的启动。一开始它会在Start的时候开启一个协程，协程内部：

	```
	1. 锁定鼠标光标
	2. 禁用玩家控制和输入
	3. 等待指定的延迟时间
	4. 开始计时（关卡时间计数开始）
	5. 启用玩家控制和输入
	6. 允许关卡暂停
	7. 触发关卡开始事件
	```



## 16、暂停面板

1. 功能：关卡启动后，当玩家按下ESC暂停键时，游戏会暂停，暂停面板会弹出，并播放动画。
2. 实现：LevelPauser关卡暂停的脚本会挂在关卡上，它对外提供一个暂停接口Pause，Player通过持有一个脚本PlayerPauser的实例，而PlayerPauser内部又会持有LevelPauser的实例，所以当Player按下暂停键时，会通过PlayerPauser这个中间层脚本调用到LevelPauser上的暂停接口Pause。Pause接口内部，当我们第一次按下暂停键时会去 1）解除鼠标锁定 2）把timeScale设为0，把状态改为暂停 3）显示暂停面板、播放暂停面板显示动画  4）分发暂停开始事件；当我们再按下暂停键时就会去 1）锁定鼠标 2）把timeScale设为1，把状态改为非暂停 3）关闭面板、播放面板隐藏动画 4）分发暂停结束事件。
3. 暂停面板的动画：因为大部分面板的显示都会有显示和隐藏的动画，所以用一个通用的UIAnimator脚本来控制大部分面板的显示和隐藏动画，他会去持有面板物体上的Animator实例，对外提供显示和隐藏的接口，当显示或隐藏时，会让Animator实例去SetTrigger来播放动画，并且分发显示或隐藏的事件，然后这两个事件我们可以在Inspector窗口里可视化的去设置。



## 17、脚步声/落地声/角色音效

1. 功能：玩家行动时会播放脚步声，落地时播放落地声，并且会根据脚底的地面材质播放不同的音效。
2. 实现：定义一个角色脚步声的脚本挂载在角色身上，这个脚本内部定义一个内部类Surface，用一个string tag来代表不同材质的地面，再存一个走路音效数组和一个落地音效数组，用数组是为了后面播放音效的时候随机选一个播放，脚步声不会特别的单一。该脚本内部就会持有这么一个Surface类的数组，我们可以再Inspector面板中进行配置编辑。Start的时候会去遍历surface数组，在本地用两个字典分别存储脚步声和落地声，以达到加速查找的目的，key时tag，value是声音数组。然后还会监听角色落地事件，用来播放落地声音。然后脚步声是再Update函数中进行的，每帧会去累计水平距离，每当玩家水平移动超过了设定的距离，就会随机播放脚步声，材质的区分是通过与脚底下的碰撞盒产生碰撞的地面的tag，来去查字典实现播放特定材质的声音的。

## 18、可收集物（Collectable）

1. 高复用性：像金币这种可收集物身上都会一个可收集物（Collectable）脚本，这种可拾取物在游戏中很常见，所以给它设置了很多可配置的参数，比如模型、音效、粒子这种常规参数，还有刚开始是否隐藏、拾取动画的高度和时间等等视觉效果相关的，还有消失时间、存活时间、幽灵时间这种生命周期相关的，还有一些物理相关的，比如碰撞体大小、初始旋转、速度，是否随机初始方向发射等等，用来实现大量金币四处弹射的效果。
2. 碰撞时：会先去检测碰撞到的物体是否是玩家，是的话执行收集，先会进行一些是否可以收集的判断，然后播放音效、分发收集事件，然后通过协程播放物品上升到角色头顶然后停一会的动画，最后消失隐藏，并禁用碰撞。（放到道具池中？）
3. Update时：我们每帧都会去处理可收集物的生命周期和物理，生命周期的处理就是简单的进行计时，判断与配置的值之间的大小来决定是否消失。物理的处理主要是施加一个重力，来改变垂直速度，此外，还会进行碰撞检测，如果是player的话就会被收集，如果不是player的话，就会反弹，获取碰撞物的法线方向，然后给一个速度，当速度小于都一个值得时候，停止物理。最后根据速度设置物体位置。

## 19、道具池





## 21、平台/路径点



## 22、检查点



## 23、数据持久化

### 1. 数据持久化层：GameSaver

我封装了一个统一的 **GameSaver** 类，作为游戏数据持久化的核心模块，它支持三种序列化 / 反序列化方式：

- **PlayerPrefs**：轻量、快速，适合存储少量关键配置；
- **Json**：可读性强，便于调试和跨平台兼容；
- **Binary**：体积小、读写快，适合大量存档数据。

通过统一的接口对外提供服务，上层逻辑无需关心底层存储方式。

------

### 2. 数据模型与运行时转换

游戏中主要有两类需要持久化的数据：

- **GameData**：代表整个游戏的存档数据（如重试次数、时间戳、所有关卡的进度等）；
- **LevelData**：代表单个关卡的存档数据（如星星收集状态、是否锁定等）。

为了保证运行时数据与持久化数据的隔离，我设计了清晰的转换关系：

- **Game 类**：负责将运行时的全局游戏状态，与 GameData 存档模型进行双向转换；
- **GameLevel 类**：负责将运行时的单个关卡状态，与 LevelData 关卡存档模型进行双向转换。

这种设计确保了：运行时逻辑只操作 “运行时对象”，不直接读写存档；存档数据只在需要持久化 / 加载时才进行转换，保证了数据一致性和安全性。

------

### 3. UI 层：存档与关卡选择界面

在 UI 层面，我实现了存档选择和关卡选择两套界面，采用了**分层组件化**的设计：

- **底层卡片组件**：`UISaveCard` 和 `UILevelCard`，负责单个槽位的数据加载、显示和交互（如显示存档时间、关卡锁定状态等）；
- **上层管理组件**：`UISaveList` 和 `UILevelList`，作为列表容器，在 ScrollView 中动态生成并管理所有槽位卡片，处理列表刷新、选中、切换等逻辑。

这样的分层结构，让 UI 代码职责清晰、复用性强。



## 24、角色重生/检查点



## Tag 和 Layer 的区别？

> “Tag 用于「逻辑分类」（区分玩家 / 金币 / 敌人），侧重‘识别物体类型’；Layer 用于「物理 / 渲染筛选」（比如相机只渲染玩家层、物理碰撞忽略敌人层），侧重‘过滤检测 / 渲染’。
>
> 实际项目中，我会结合使用：比如玩家 Tag=Player、Layer=PlayerLayer，触发器中先用 Layer 过滤（性能更高），再用 Tag 精准判断，既保证性能又保证逻辑准确。”

